# جرد ما تبقّى — تالي شام

مُولَّد بمراجعةٍ آلية موازية على ستة جوانب (جاهزية، شيفرة، بنية، واجهات، ما يحتاجه المالك، أمان)،
بتاريخ 2026-07-30. عدد البنود: 220.

> **حدُّ الثقة.** جانبا «الجاهزية» و«الشيفرة» مرّا بتدقيقٍ عدائيّ يحاول دحض كل بند.
> والأربعة الباقية لم تُدقَّق (سقط المدقّقون بحدّ الجلسة)، فبنودها مرشَّحة لا مؤكَّدة —
> ما عدا ما تُحقّق منه يدوياً ومؤشَّراً أدناه. البند غير المدقَّق يُفحص قبل العمل به.


## مانعة للإطلاق (52)

### لا يوجد في المتجر كلّه مسارٌ واحد يُدخل منتجاً إلى السلة — زرّ «أضف للسلة» بلا مستمع، والتطبيق لا ينادي مسار الإضافة أبداً
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/site/src/pages/p/[slug].astro:180 زرّ `.btn--buy` بلا `addEventListener` (المستمعان الوحيدان في الملف على سطر 260 لاختيار النسخة و297 للمقارنة). وفي الواجهتين: `grep -rn "/items" apps/app/src apps/admin/src apps/site/src` يعطي apps/app/src/App.tsx:101 فقط وهو `POST /carts/:token/items/:sku` (تعديل كمية سطر قائم)، بينما مسار الإضافة `POST /carts/:token/items` (apps/api/src/http/routes.ts:88) لا يُستدعى من أي ملف واجهة.`
- **الأثر**: الواجهة البرمجية للسلة والطلب مكتملة ومختبَرة، لكن الزبون لا يستطيع بلوغها: يصل إلى صفحة المنتج، يضغط «أضف للسلة»، ولا يحدث شيء. المتجر يعرض ولا يبيع.

### لا زبون يستطيع تسجيل الدخول في الإنتاج: رمز الدخول يُطبَع في سجلّ الـWorker ولا يُرسَل، ولا يُعاد في الردّ
- **الحال**: محجوب · **ينفّذه**: المالك · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/api/src/modules/channels.ts:16-17 `console.log('[واتساب · محاكاة]…'); return { ok: true, detail: 'simulated' }` وكذلك SMS في :38-39. وapps/api/src/modules/auth.module.ts:85 `...(process.env.NODE_ENV === 'production' ? {} : { devCode: code })` وwrangler.jsonc يضبط `"NODE_ENV": "production"`.`
- **الأثر**: كل ما خلف الحساب ميت عملياً: طلباتي، المفضّلة، تنبيه السعر، كتابة مراجعة، تذاكر الدعم. والردّ يقول `sent: true` فيبدو النظام سليماً بينما لا رسالة خرجت — وهو أسوأ من خطأ صريح.

### الموقع العام يُبنى من بذرة الشيفرة دائماً ولا يقرأ القاعدة أبداً: PUBLIC_API_URL غير مضبوط في أي مكان
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/site/src/lib/catalog.ts:9 `const API = import.meta.env.PUBLIC_API_URL ?? process.env.PUBLIC_API_URL ?? ''` و:93 `if (!API) return { items: fromSeed(), source: 'seed' }`. و`grep -rn PUBLIC_API_URL apps scripts .github .env.example` لا يجد ضبطاً — scripts/build-worker.sh:16 يمرّر PUBLIC_SITE_URL وحده، و.github/workflows/deploy.yml كذلك.`
- **الأثر**: إطفاء DEMO_MODE وإدخال منتجات حقيقية من اللوحة لن يغيّرا حرفاً في talisham.com: صفحات المنتجات والفئات والبحث كلها مبنية من seed/catalog.demo.json وقتَ البناء. والإصلاح ليس ضبط متغيّر فقط — يلزم أيضاً إعادة بناء ونشر عند كل تغيير في الكتالوج.

### DEMO_MODE غير معرَّف في wrangler.jsonc فيسقط إلى القيمة الافتراضية `true` في الـWorker الحيّ
- **الحال**: بيد المالك · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/api/src/modules/catalog.module.ts:5 `const demoMode = () => (process.env.DEMO_MODE ?? 'true') === 'true'` و apps/api/src/modules/search.module.ts:60 مثله. وقسم `"vars"` في wrangler.jsonc يحوي PUBLIC_SITE_URL وNODE_ENV فقط.`
- **الأثر**: الافتراضي الآمن مقلوب: الغياب يعني «اعرض البضاعة الوهمية» لا «أخفِها». ومتجرٌ حيّ يعرض ٢٢ منتجاً تجريبياً على نطاقٍ مفهرَس يبني سمعةً يصعب تصحيحها.

### مفتاح «وضع البيانات التجريبية» في لوحة التحكم يُكتب في القاعدة ولا يقرؤه أي سطر شيفرة
- **الحال**: جزئي · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/admin/src/App.tsx:1072 يعرضه بتلميح «أطفئه قبل الافتتاح»، وapps/api/src/modules/admin.module.ts:248 يقبله ويحفظه في store_settings. لكن `grep -rn demo_mode apps/api/src --include="*.ts" | grep -v generated | grep -v admin.module` لا يعطي شيئاً — الترشيح يقرأ process.env لا الإعداد.`
- **الأثر**: صاحب المتجر سيطفئ المفتاح قبل الافتتاح، وسترد اللوحة «حُفظ»، وستبقى المنتجات التجريبية معروضة. مفتاحٌ يكذب أخطر من مفتاحٍ غائب.

### صور المنتجات لا تصل إلى الواجهة مهما رُفعت: لا مستدعٍ يمرّر `src` إلى مكوّن الصورة، ولا الكتالوج يحمل حقل الوسائط
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/site/src/components/ProductMedia.astro:26 يقبل `src = null`، والمستدعيان الوحيدان لا يمرّرانه: apps/site/src/components/ProductCard.astro:38-45 وapps/site/src/pages/p/[slug].astro:103. وواجهة `Product` في apps/site/src/lib/catalog.ts:31-42 بلا حقل media، ودالة `fromApi` (:73) لا تُسقط `p.media`.`
- **الأثر**: حتى بعد منح صلاحية R2 ورفع الصور من اللوحة، سيظلّ الموقع يرسم SVG مولَّداً لكل منتج. متجر هواتف بلا صور فوتوغرافية لا يُشترى منه.

### ربط R2 معطَّل، فمسار رفع الصور في اللوحة يردّ MEDIA_BUCKET_MISSING عند كل محاولة
- **الحال**: محجوب · **ينفّذه**: المالك · **✔ مُدقَّق** · *code*
- **الدليل**: `wrangler.jsonc: قسم `r2_buckets` معلَّق بالكامل. وapps/api/src/modules/storage.ts:82-86 `if (!bucket) throw Errors.badRequest('MEDIA_BUCKET_MISSING', …)`. وواجهة الرفع موجودة وتنادي المسار: apps/admin/src/App.tsx:1283 `api.post('/admin/catalog/product/${f.slug}/media', { dataUrl })`.`
- **الأثر**: معروف مسبقاً، لكن الأثر أوسع مما يبدو: بلا صور لا يُنشر منتج حقيقي مقنع، وبند الجاهزية CONTENT_IMAGES_DESC (readiness.module.ts:32) لا يمكن تأشيره ناجحاً — فيمتنع الإطلاق بحسابٍ لا برأي.

### القاعدة الحيّة بلا جدول d1_migrations — فمنح صلاحية D1 وحده لن يُصلح الترحيل بل سيُفشله من أول جملة
- **الحال**: محجوب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `استعلام على قاعدة الإنتاج d46eb676-…: SELECT count(*) FROM sqlite_master WHERE name='d1_migrations' ⇒ 0، بينما 52 جدولاً و138 فهرساً و2 محفِّز موجودة فعلاً (أنشأها bootstrap لا wrangler). و apps/api/src/modules/bootstrap.module.ts:52-59 يطبّق المخطَّط بلا تسجيل أي ترحيل.`
- **الأثر**: يوم يُمنح المفتاح D1:Edit سيرى wrangler دفتراً فارغاً فيحاول تطبيق 0001 من الصفر على قاعدة مأهولة، فيفشل عند «table users already exists» — ويبقى الترحيل معطَّلاً بعد إزالة سببه. اللازم: إنشاء d1_migrations وإدراج 0001 و0002 كمطبَّقَين قبل أول تشغيل للأداة.

### لا مسار قائم لتطبيق أي ترحيل جديد على الإنتاج — الطرق الثلاثة مسدودة معاً
- **الحال**: محجوب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `(1) سجل النشر 30511167255 في 03:26:13 — «code: 7403» لأمر d1 migrations apply --remote؛ (2) scripts/bundle-sql.mjs:11 يحزم 0001_d1_init.sql وحده فلا يعرف 0002_new_pound.sql ولا ما بعده؛ (3) apps/api/src/modules/bootstrap.module.ts:44-48 يرفض العمل على قاعدة فيها جدول products.`
- **الأثر**: أي تعديل مخطَّط قادم (عمود جديد، قيد جديد) لن يصل إلى الإنتاج بأي وسيلة، وأول نصّ يقرأ عموداً غير موجود يفشل عند أول طلب زبون. النجاة الحالية صدفة: 0001 عُدِّل رجعياً ليحمل قيد الليرة الجديدة قبل آخر تهيئة.

### JWT_SECRET لا يُضبط بأي مسار آلي، والشيفرة تسقط صامتةً إلى مفتاح مكتوب في المستودع
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `apps/api/src/common/jwt.ts:3 — `process.env.JWT_SECRET ?? 'dev_only_change_me'`. و grep -rn "secrets\." .github/workflows/ يعيد CLOUDFLARE_API_TOKEN و CLOUDFLARE_ACCOUNT_ID فقط. وقائمة ارتباطات الـWorker في سجل النشر 03:26:41 تذكر KV وDB وASSETS وPUBLIC_SITE_URL وNODE_ENV — ولا سرّ واحد.`
- **الأثر**: إن لم يُضبط السرّ يدوياً فكل جلسة في الإنتاج موقَّعة بمفتاح منشور في مستودع عام: من يقرأ الملف يصنع رمز ADMIN صالحاً. ولا شيء في المشروع يكشف الحالة — لا فحص إقلاع يرفض المفتاح الافتراضي، ولا مسار صحّة يقول إن السرّ مضبوط.

### لا نسخ احتياطي ولا تمرين استعادة — لا نصّ ولا سير عمل ولا حتى الأمر الذي توثّقه الوثيقة
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `grep -rni "backup|نسخ احتياطي|استعادة|restore" على apps و scripts و .github و package.json لا يعيد إلا نصّ بند جاهزية في apps/api/src/modules/readiness.module.ts:24. و docs/11-devops.md:208 يوجب اختبار استعادة أسبوعياً، و docs/11-devops.md:269 يستدعي `pnpm ops:restore-drill` — وهو غير معرَّف في أي package.json.`
- **الأثر**: متجرٌ يحصّل نقداً عند التسليم يحمل في قاعدته دفتر التحصيلات والتسويات. حذفٌ خاطئ أو ترحيل فاشل يعني ضياع الدفتر بلا نسخة، واعتمادُ Time Travel في D1 وحده رهانٌ لم يُختبر مرة واحدة — واستعادةٌ لم تُجرَّب ليست استعادة.

### لا منتج حقيقي واحد في المتجر — الكتالوج كله اثنان وعشرون منتجاً تجريبياً
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `seed/catalog.demo.json:9 ("products": 22) وكلها is_demo=true · التحقق حيّاً: curl -s "https://talisham.com/api/v1/catalog/products?limit=3" يردّ منتجات البذرة (solar-charger-24w …) · يُدخَل من لوحة التحكم: /admin/catalog ثم زر «تحويل إلى منتج حقيقي» (apps/admin/src/App.tsx:1602 → POST /admin/catalog/products/:slug/promote)`
- **الأثر**: المتجر حيّ الآن ويعرض بضاعة لا وجود لها؛ أي طلب عليها يُرفض بـDEMO_PRODUCT_NOT_ORDERABLE (apps/api/src/modules/orders.module.ts:71-72)، فالزائر يرى متجراً ثم يُصدّ عند الشراء.

### لا صور منتجات إطلاقاً — النظام يعرض رسماً مولَّداً بديلاً لكل جهاز
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `seed/README.md:58 «الصور غير مضمَّنة هنا… مصدرها قرار يخصّ صاحب المشروع قبل الإطلاق» · لا حقل images في أي منتج بالبذرة · البديل: apps/site/src/components/ProductMedia.astro (SVG مولَّد) · تُرفع من: /admin/catalog → محرّر المنتج → رفع صورة (apps/admin/src/App.tsx:1282)`
- **الأثر**: جهاز مستعمل أو مجدَّد بلا صورة حقيقية لا يُشترى عن بُعد في سوق حاجزُه الأول الخوف من النصب؛ وبند الجاهزية CONTENT_IMAGES_DESC مانع للإطلاق (readiness.module.ts:32).

### مفتاح النشر بلا صلاحية R2: Edit — رفع الصور مستحيل تقنياً حتى لو توفّرت
- **الحال**: محجوب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `wrangler.jsonc: كتلة r2_buckets معلَّقة بالكامل مع تعليل الصلاحية · apps/api/src/modules/storage.ts:88 يردّ MEDIA_BUCKET_MISSING · التحقق حيّاً: curl -o /dev/null -w "%{http_code}" https://talisham.com/media/products/test.jpg → 503 · تُضاف من dash.cloudflare.com/profile/api-tokens على المفتاح نفسه`
- **الأثر**: البند السابق (الصور) معلَّق عليه: يستحيل على المالك رفع صورة واحدة قبل منح هذه الصلاحية، ولا يستطيع المبرمج منحها لأنها في حساب المالك.

### سعر الصرف 128.5 تجريبي وصلاحيته أربع وعشرون ساعة — يلزم إدخال يومي وإلا توقّف البيع
- **الحال**: بيد المالك · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `seed/catalog.demo.json:18-19 (rate 128.5, valid_hours 24, note «سعر تجريبي… يُستبدل بالسعر الحقيقي من اللوحة قبل الإطلاق») · حيّاً: curl -s https://talisham.com/api/v1/fx/current → rate 128.5, hoursLeft 21.4 · التوقف: apps/api/src/modules/fx.module.ts:20-21 (STALE_HALT) و requireSellable():71 · يُدخَل من: /admin/fx (POST /api/v1/admin/fx)`
- **الأثر**: بعد اثنتي عشرة ساعة من انتهاء الصلاحية يتوقف استقبال الطلبات كلياً؛ والسعر التجريبي الآن يعني أن كل ثمن معروض على الموقع خطأ.

### أجر التوصيل ثابت على دولارين لكل طلب مهما كانت المحافظة
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/src/modules/orders.module.ts:101 `let shipping = 200;` (سنتات) · جدول التعريفة معرَّف في apps/api/prisma/schema.prisma:484-496 ولا يُستعلَم في أي ملف: grep -rn "shippingRate\|ShippingRate" apps/api/src → لا نتيجة`
- **الأثر**: طلبٌ إلى حلب يُحاسَب بأجر دمشق، فالفارق يخرج من ربح المتجر في كل طلب بعيد؛ والأجور التي يعطيها المالك لن يكون لها أثر ما دام الرقم مكتوباً بيده في الشيفرة.

### لا مندوب واحد ولا منطقة توصيل في القاعدة
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/prisma/seed-d1.mjs لا يبذر couriers ولا delivery_zones (تنتهي البذرة عند shipping_rates وstore_settings وuser واحد) · تُنشأ من: /admin/delivery (apps/admin/src/App.tsx:904 و951)`
- **الأثر**: بند الجاهزية OPS_COURIER_DAMASCUS_3 يطلب ثلاثة مندوبين فعّالين بمناطق محددة؛ وبلا مندوب لا يُسلَّم طلب ولا يُحصَّل نقد.

### وضع البيانات التجريبية يُطفأ بمتغيّر بيئة غير معرَّف، وزرّه في اللوحة بلا أثر
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/src/modules/catalog.module.ts:5 و search.module.ts:60 و apps/site/src/lib/catalog.ts:140 تقرأ process.env.DEMO_MODE (افتراضه 'true') · wrangler.jsonc "vars" فيه PUBLIC_SITE_URL وNODE_ENV فقط، بلا DEMO_MODE · إعداد demo_mode مبذور (seed-d1.mjs:181) ومعروض (App.tsx:1072) وغير مقروء`
- **الأثر**: المالك سيضغط «أطفئ وضع البيانات التجريبية» في اللوحة فلا يتغيّر شيء، والمتجر يبقى يعرض بضاعة وهمية بعد الافتتاح.

### الموقع الساكن يُبنى من البذرة لا من القاعدة — منتجات المالك لن تظهر ولو أدخلها
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `scripts/build-worker.sh:15-16 يمرّر PUBLIC_SITE_URL وحده بلا PUBLIC_API_URL · apps/site/src/lib/catalog.ts:96 `if (!API) return { items: fromSeed(), source: 'seed' }` · .github/workflows/deploy.yml:52-54 كذلك`
- **الأثر**: اثنتان وستون صفحة على النطاق الحيّ مبنيّة من ملف البذرة؛ كل ما يُدخله المالك في اللوحة يبقى محبوساً في القاعدة ولا يراه زائر.

### طبقة الصور غير موصولة بالموقع الساكن — لا حقل صور في نوع المنتج أصلاً
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/site/src/lib/catalog.ts:36-46 واجهة Product بلا media/images، وdefine mapVariant بلا صور · apps/site/src/pages/p/[slug].astro:103-114 وapps/site/src/components/ProductCard.astro:38-45 يستدعيان ProductMedia بلا خاصية src`
- **الأثر**: حتى بعد صلاحية R2 ورفع المالك للصور، صفحات المنتجات ستبقى تعرض الرسم المولَّد — الصورة تصل إلى القاعدة ولا تصل إلى الصفحة.

### واتساب التجاري ممنوع على سوريا — القناة محاكاة ولا بديل منفَّذ
- **الحال**: محجوب · **ينفّذه**: خارج سيطرتنا · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `docs/13-syria-reality.md:19 «ميتا تستثني سوريا صراحةً من WhatsApp Business Platform إرسالاً واستقبالاً» و:53 «الدراسة أبطلت القرار كلياً» · apps/api/src/modules/channels.ts:16-19 يكتب في السجل ويردّ detail: 'simulated'`
- **الأثر**: طلبُ حساب Meta/WhatsApp Business لن يُقبل مهما حاول المالك؛ البديل الموثَّق عقدٌ مباشر مع سيرياتيل بـSender ID باسم TaliSham، وهو تفاوضٌ تجاري لا يفعله المبرمج.

### مزوّد SMS غير مضبوط وغير مذكور في ملف المتغيّرات النموذجي
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/src/modules/channels.ts:10-12 يقرأ SMS_PROVIDER_TOKEN وSMS_PROVIDER_URL وSMS_SENDER · .env.example لا يذكر أياً منها (فيه واتساب وVAPID فقط) · بلا مفتاح: channels.ts:36-38 محاكاة`
- **الأثر**: SMS هي القناة الافتراضية بعد سقوط واتساب (docs/13-syria-reality.md:13.3 بند 3)؛ وبلا عقد لا يصل رمز تحقق ولا إشعار تسليم ولا تنبيه — والعقد بيد المالك.

### حساب الإدارة مبذور برقم وهمي +963900000001
- **الحال**: بيد المالك · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/prisma/seed-d1.mjs:189 `const adminPhone = process.env.ADMIN_PHONE ?? '+963900000001'` و.env.example:31 الافتراض نفسه · كلمة السرّ الأولى تُضبط مرة واحدة عبر POST /api/v1/bootstrap/admin-password (apps/api/src/worker.ts)`
- **الأثر**: صاحب المتجر لا يملك حساباً باسمه في لوحته، والحساب القائم برقم لا يخصّ أحداً — فلا استرجاع كلمة سرّ ولا مساءلة في سجل التدقيق.

### الكتالوج الحيّ كله بيانات تجريبية: 22 منتجاً تجريبياً وصفر منتج حقيقي
- **الحال**: محجوب · **ينفّذه**: المالك · **✔ مُدقَّق** · *readiness*
- **الدليل**: `curl -s "https://talisham.com/api/v1/catalog/products?limit=100" | grep -o '"isDemo":true' | wc -l → 22 ، و"isDemo":false → 0 ؛ apps/api/src/modules/catalog.module.ts:5`
- **الأثر**: المتجر يعرض بضاعة لا وجود لها على رفّه، ويستقبل عليها طلبات دفعٍ عند الاستلام. أي زائر يصل اليوم يرى «شاحن شمسي 24 واط» لا مخزون المحل.

### الموقع الساكن لا يقرأ القاعدة أصلاً — يُبنى من ملف البذرة لأن PUBLIC_API_URL غير مضبوط في أي مكان
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/site/src/lib/catalog.ts:6,9 (import seed from seed/catalog.demo.json ؛ const API = import.meta.env.PUBLIC_API_URL ?? '') ؛ grep -rn PUBLIC_API_URL scripts/ .github/ → لا نتيجة`
- **الأثر**: نشر منتج من اللوحة لا يظهر على الموقع مهما فُعل. صفحات المنتج والفئات وخريطة الموقع كلها مجمَّدة على البذرة، فإطفاء DEMO_MODE في الواجهة البرمجية وحده لا يغيّر ما يراه الزبون.

### السعر لا يظهر إطلاقاً مع تعطيل JavaScript — الوسم فارغ ويُملأ بنص في المتصفح
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/site/src/components/Price.astro:17 (<span class="price__main" data-usd={usdCents}></span>) و apps/site/src/layouts/Base.astro:104-120 ؛ curl -s https://talisham.com/p/samsung-galaxy-a35/ | grep -o 'price__main[^>]*>[^<]*<' → 'price__main" data-usd="23000">'<' فارغ ؛ grep -c noscript = 0`
- **الأثر**: بند الجاهزية TECH_NOJS_BROWSE وصفّ «الاكتشاف» في 19.1 يشترطان صفحة كاملة المحتوى بلا انتظار JavaScript. متجر هواتف بلا سعر مقروء هو صفحة بلا معنى، ومحرك البحث يفهرس السعر فارغاً.

### وحدة الجهاز المسلَّمة تُختار عشوائياً بمعرّفها لا بالـIMEI الذي بيد الزبون
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/api/src/modules/courier.module.ts:167-176 (findMany({state:'IN_STOCK'}, orderBy:{id:'asc'}, take: item.qty) ثم state:'SOLD') ؛ دالة collect لا تقبل حقل imei أصلاً (التوقيع في السطر 69-73)`
- **الأثر**: الكفالة تُفعَّل على جهاز غير الذي استلمه الزبون (warranty.module.ts يقرأ item.unit)، فالتحقق العام بالـIMEI في /warranties/verify سيقول «غير موجود» لجهاز مبيع فعلاً. والفصل 13.5 بند 13 يشترط مطابقة IMEI أمام الزبون قبل قبض المبلغ.

### مفتاح توقيع الجلسات له قيمة افتراضية منشورة في الشيفرة
- **الحال**: غائب · **ينفّذه**: المالك · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/api/src/common/jwt.ts:3 — const SECRET = process.env.JWT_SECRET ?? 'dev_only_change_me'`
- **الأثر**: إن لم يُضبط JWT_SECRET بـwrangler secret put على الـWorker الحيّ فكل رمز دخول موقَّع بمفتاح مكتوب في مستودع عام — أي أحد يصنع رمز مدير. ولا شيء في الشيفرة يمنع الإقلاع بالقيمة الافتراضية ولا يُنبّه إليها.

### مسارا مطالبات الكفالة بلا مصادقة: أي أحد يقرأ مطالبات أي رقم هاتف
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/api/src/http/routes.ts:151-156 — app.post/get(`${P}/me/warranty-claims`) بلا any() ولا guest() ؛ apps/api/src/modules/warranty.module.ts:187 myClaims(phone) يستعلم where:{phone} مباشرةً`
- **الأثر**: تعداد الأرقام السورية محدود المدى، فالمسار يكشف اسم الجهاز ووصف العطل وحالة المطالبة لكل زبون. والمصفوفة في 19.3 تعطي العميل نطاق own حصراً.

### واجهة المندوب تعطي كل مندوب كل الطلبات ببيانات عملائها كاملة، ولا حقل إسناد مندوب في جدول الطلبات
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/api/src/modules/courier.module.ts:19-38 (findMany بلا أي مرشّح مندوب، ويعيد phone وaltPhone وneighborhood وlandmark) ؛ grep -n 'courier' apps/api/prisma/schema.prisma → لا حقل إسناد على model Order (السطور 389-448)`
- **الأثر**: 19.3 تعطي المندوب نطاق «طلبه فقط» في users:read_pii. عملياً: مندوب واحد يحمل عناوين كل زبائن المتجر وأرقامهم في هاتفه، وأي مندوب يستطيع تحويل حالة أي طلب لا طلبه.

### واتساب ما زال القناة الأولى لرمز الدخول وكل الإشعارات، والفصل 13 يقرّر أن ميتا تستثني سوريا إرسالاً واستقبالاً
- **الحال**: محجوب · **ينفّذه**: المالك · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/api/src/modules/notifications.service.ts:29-31 (channelsFor يضع WHATSAPP قبل SMS) و 62-72 ؛ apps/api/src/modules/auth.module.ts:75-80,88 (channel: 'WHATSAPP_THEN_SMS') ؛ apps/app/src/App.tsx:411 «أرسلنا الرمز على واتساب» ؛ docs/13-syria-reality.md:57 «الدراسة أبطلت القرار كلياً»`
- **الأثر**: في الإنتاج الحقيقي لن يصل الرمز أبداً، فلا أحد يدخل حسابه ولا يستلم إشعار خروج المندوب. والقرار المكتوب (عقد سيرياتيل + Sender ID باسم TaliSham) لم يُتخذ ولم تُضبط له متغيّرات: SMS_PROVIDER_URL وSMS_PROVIDER_TOKEN غير مذكورين حتى في .env.example.

### لا نسخ احتياطي ولا تمرين استعادة — وهو بند مانع للإطلاق مؤشَّر في الشيفرة نفسها
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `grep -rniE 'backup|نسخ احتياطي' scripts/ .github/ apps/api/src package.json → المطابقة الوحيدة نصّ البند في apps/api/src/modules/readiness.module.ts:24 ؛ docs/11-devops.md:208 يشترط استعادة أسبوعية موثَّقة خارج المزوّد وخارج البلد`
- **الأثر**: D1 عليها كل الطلبات والتحصيل النقدي والمخزون. Time Travel من Cloudflare ليس نسخة خارج المزوّد، ولا شيء يصدّر البيانات ولا يتحقق من سلامتها. البند لا يُؤشَّر PASSED بلا إثبات، وقانون الشيفرة يرفض التأشير بلا evidenceUrl.

### مسار `GET /orders/:orderNo` بلا مصادقة إطلاقاً ويعيد اسم المستلم وهاتفه ومعلمه وعنوانه، ورقم الطلب متسلسل قابل للتعداد
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/http/routes.ts:114 (`app.get(`${P}/orders/:orderNo`, async (c) => ...)` بلا `protect` ولا `guest`) + apps/api/src/modules/orders.module.ts:288-293 (يعيد recipientName/phone/landmark) + apps/api/src/common/money.ts:16-19 (`TS-YYMM-NNNNNN` متسلسل). أمر الإثبات: `curl https://talisham.com/api/v1/orders/TS-2607-000001``
- **الأثر**: الوثيقة 9.4 A01 تشترط شرط المالك داخل استعلام Prisma نفسه. اليوم يستطيع أي شخص أن يعدّ الأرقام من 000001 صعوداً ويحصد قاعدة عملاء المتجر كاملة: الاسم والجوال والمعلم القريب — أي عناوين منازل زبائن يحملون أجهزة عالية القيمة. هذا تسريب بيانات شخصية جماعي، لا ثغرة نظرية.

### مسار `GET /me/warranty-claims?phone=` بلا مصادقة — الهوية تُؤخذ من معامل استعلام يكتبه المهاجم
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/http/routes.ts:154-155 (`app.get(`${P}/me/warranty-claims`, async (c) => ... c.req.query('phone'))` بلا حارس) + apps/api/src/modules/warranty.module.ts:188-196 (`where: { phone }` بلا أي ربط بالمستخدم الداخل). وكذلك `POST /me/warranty-claims` في routes.ts:150-153 بلا حارس`
- **الأثر**: مسار تحت البادئة `/me/` يفترض هوية، لكنه يقبلها من الطلب. أي رقم يُخمَّن يكشف مطالبات كفالة صاحبه ووصف عطل جهازه. ويسمح `POST` بفتح مطالبة باسم رقم غيرك.

### CORS يعكس أي أصل مع `credentials: true` — لا قائمة أصول مسموحة
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/http/app.ts:117-123 — `origin: (o) => o ?? '*'` مع `credentials: true``
- **الأثر**: أي موقع خبيث يفتحه الزبون يستطيع نداء الواجهة نيابةً عنه ويقرأ الاستجابة. تعليق worker.ts:20 يقول «أصلٌ واحد يعني بلا CORS» — لكن الشيفرة تفتح CORS للجميع بدل إغلاقه، وهو عكس النية المكتوبة.

### لا رؤوس أمان إطلاقاً: لا CSP ولا HSTS ولا X-Content-Type-Options ولا Permissions-Policy ولا frame-ancestors
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `لا مطابقة واحدة في المستودع كله: `grep -rn "Content-Security-Policy\|Strict-Transport-Security\|X-Content-Type-Options\|Permissions-Policy\|frame-ancestors" apps/ packages/ wrangler.jsonc` ← فارغ. ولا ملف `_headers` (`find . -name _headers` ← فارغ)`
- **الأثر**: الوثيقة 9.10(ب) تكتب سياستَي CSP كاملتين بالحرف — إحداهما بتجزئات للموقع الساكن والأخرى بـnonce للتطبيقين — ولا شيء منهما منفَّذ. بلا CSP لا حاجز أمام XSS، وبلا `frame-ancestors` الموقع قابل للتأطير (clickjacking) على صفحة إتمام الطلب، وبلا HSTS يبقى أول طلب قابلاً للاعتراض.

### رموز الدخول والتحديث تُخزَّن في `localStorage` بدل ذاكرة التطبيق وكوكي `__Host-ts_rt`
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/app/src/lib/api.ts:10-19 و apps/admin/src/lib/api.ts:10-13 — `localStorage.getItem('access_token')` و`localStorage.getItem('refresh_token')`. ولا `Set-Cookie` في المستودع كله (`grep -rn "Set-Cookie\|setCookie" apps/` ← فارغ)`
- **الأثر**: الوثيقة 9.2 صريحة: رمز الوصول «يُحفظ في ذاكرة التطبيق فقط (لا localStorage)»، ورمز التحديث في كوكي `HttpOnly`. الوضع الحالي يعني أن أي XSS واحد — وليس ثمة CSP يوقفه — يسرق رمز تحديث صالحاً ثلاثين يوماً، أي استيلاء كامل على الحساب، وعلى حساب اللوحة إن أصاب اللوحة.

### لا تدوير لرمز التحديث ولا `family_id` ولا كشف لإعادة الاستخدام — الرمز القديم يبقى صالحاً بعد التجديد
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/modules/auth.module.ts:143-162 — `refresh()` يتحقّق ثم `return issue(...)` بلا إبطال للرمز الوارد ولا تسجيل لجزّه. ورمز التحديث JWT لا سلسلة عشوائية مجزّأة في `auth_sessions` (apps/api/src/common/jwt.ts:30)`
- **الأثر**: الوثيقة 9.2 «التدوير»: كل استدعاء يُبطل الرمز الحالي، وإعادة استخدام رمز مُبطَل تعني سرقة فتُبطَل العائلة كاملة. اليوم رمز مسروق يعمل ثلاثين يوماً بالتوازي مع رمز صاحبه دون أن يترك أثراً واحداً يكشف السرقة.

### سرّ التوقيع له قيمة افتراضية مكتوبة في الشيفرة، ولا بوابة إقلاع ترفض غيابها
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/common/jwt.ts:3 — `const SECRET = process.env.JWT_SECRET ?? 'dev_only_change_me'` يُقرأ في نطاق الوحدة. و wrangler.jsonc:أسفل الملف يذكر `npx wrangler secret put JWT_SECRET` في تعليق فقط — لا دليل على ضبطه. و apps/api/src/common/boot-guard.ts غير مستورد في أي ملف (`grep -rn "boot-guard" apps/api/src` ← الملف نفسه فقط)`
- **الأثر**: إن لم يُضبط السرّ فعلياً على الـWorker، فكل رموز الإنتاج موقّعة بسلسلة منشورة في المستودع العام — أي أن أي شخص يستطيع تزوير رمز `role: "ADMIN"` والدخول إلى اللوحة. والفشل صامت تماماً: لا شيء في الشيفرة يمنع الإقلاع بلا سرّ.

### خرق فصل الواجبات: الأدوار الإدارية تملك مسار التحصيل النقدي
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/http/routes.ts:437 — `const courierRoles = () => protect(d.prisma, 'COURIER', 'OPS_MANAGER', 'ADMIN')` مطبَّقة على routes.ts:441 `POST /courier/orders/:orderNo/collect`. وفي المقابل routes.ts:297 `POST /admin/settlements/:id/settle` بـ`adminOnly()``
- **الأثر**: الوثيقة 9.3 قاعدة (1): «`payments:collect:own` لا يملكها أي دور إداري إطلاقاً»، وقاعدة (2): لا يجتمع الإقفال مع التحصيل في حساب واحد. حساب ADMIN اليوم يُسجّل التحصيل ويُقفل التسوية معاً — أي أنه يستطيع اختلاق قيد نقدي وإقفاله على نفسه بلا شاهد. وكل المال في هذا المتجر نقد.

### مسارات المندوب غير مقيّدة بالإسناد: `courier.tasks()` تُعيد كل طلبات المتجر ببيانات عملائها كاملة
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/modules/courier.module.ts:18-38 — `findMany({ where: { status: { in: [...] } } })` بلا `assignedCourierId` ولا `shiftId`، ويعيد `phone` و`altPhone` و`landmark` لكل طلب. و`collect()` في courier.module.ts:69-107 لا تفحص إسناد الطلب إلى المُحصِّل. والمخطَّط بلا حقل إسناد أصلاً: `grep -n "assignedCourier\|shiftId" apps/api/prisma/schema.prisma` ← لا نتيجة`
- **الأثر**: الوثيقة 9.10(ج) الصف الأخير: «كل نداء مندوب مفلتر بـ`assigned_courier_id` و`shift_id` في الاستعلام نفسه»، و9.4 A01 تكرّرها. أي مندوب — على هاتفه الشخصي الذي قد يُفقد أو يُباع — يرى اليوم عناوين وهواتف كل زبائن المتجر، ويستطيع تسجيل تحصيل على طلب ليس له.

### لا CAPTCHA ولا متغيّر `CAPTCHA_PROVIDER` على `POST /auth/otp/request`
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: ``grep -rni "captcha\|turnstile\|altcha" apps/` ← فارغ. و apps/api/src/http/routes.ts:44 يستقبل الطلب مباشرة بلا رمز تحدٍّ`
- **الأثر**: الوثيقة 9.1 تحسبها بالدولار: «هجوم بمعدل 10 رسائل/ثانية = 36,000 رسالة في الساعة بتكلفة تتجاوز 1,000 دولار في الساعة — تهديد مالي مباشر قبل أن يكون أمنياً». الحماية الوحيدة القائمة هي 20 طلباً/ساعة لكل IP (rate-limit.ts:16)، وهي بلا معنى أمام شبكة عناوين. ولا قاطع دارة يومي عند 3,000 رسالة كما تشترط الوثيقة.

### قناة Web Push مخطَّطة في الموجّه ولا تُرسَل أبداً: `NotificationsService` لا يستدعي `PushService` إطلاقاً
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/modules/notifications.service.ts:26-31 (`channelsFor` تُرجع `WEB_PUSH` لـP1/P2/P3) مقابل الأسطر 62-80: الفروع الموجودة هي `WHATSAPP` و`SMS` فقط، ولا استيراد لـ`PushService` في الملف. و apps/api/src/http/app.ts:76 يبني `notify` بلا تمرير `push``
- **الأثر**: النتيجة أن كل إشعارات P2 (`['IN_APP','WEB_PUSH']`) وP3 (`['WEB_PUSH']`) لا تخرج على أي قناة إطلاقاً: تنبيه التوفّر، انخفاض السعر، السلة المتروكة، طلب التقييم، إجابة السؤال — كلها في الفصل 20.6 وكلها ميتة صامتة. الموجّه يُبلّغ نجاحاً وهو لم يرسل شيئاً.

### لا جدول `notifications` ولا مركز إشعارات `/app/notifications`
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: ``grep -n "model .*Notification" apps/api/prisma/schema.prisma` ← `PushSubscription` فقط (السطر 876)، ولا نموذج `Notification`. ولا مسار `/notifications` في apps/api/src/http/routes.ts (`grep -n "notification" routes.ts` ← تفضيلات فقط، السطران 223-224)`
- **الأثر**: الوثيقة 20.5 تسمّيه «السجل الدائم: كل ما أُرسل بأي قناة يُكتب في `notifications` بحقول `type, level, channel[], read_at, entity_ref, dedupe_key` ويُحتفظ به 90 يوماً». وهو أيضاً السطح الوحيد في 20.6 لأربعة عشر حدثاً («مركز»). بلا الجدول لا شارة غير مقروء ولا سجل يرجع إليه الزبون حين يفوته إشعار — وهو الحال الغالب في سوق شبكته متقطعة.

### لا نسخ احتياطي لقاعدة D1 ولا تمرين استعادة
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: ``grep -rn "backup\|d1 export" .github/workflows/ scripts/` ← لا شيء سوى `bundle-sql.mjs`. و .github/workflows/ يحوي ci.yml و deploy.yml و maintenance.yml فقط`
- **الأثر**: الوثيقة 9.6 تفرض حفظ `cash_settlements` وسجلات الطلبات خمس سنوات «PostgreSQL + نسخ احتياطي»، و9.11 تشترط «تمرين هجرة نصف سنوي: استعادة نسخة احتياطية… الهدف استعادة الخدمة كاملة خلال 24 ساعة». اليوم لا نسخة واحدة: حذفٌ خاطئ أو عطبٌ في D1 يمحو دفتر الصندوق كلّه بلا رجعة.

### لا `pii_access_logs` — قراءات البيانات الشخصية من اللوحة وواجهة المندوب غير مقيَّدة
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `لا نموذج في المخطَّط ولا استدعاء في الشيفرة: `grep -rn "piiAccess\|pii_access" apps/` ← فارغ. و apps/api/src/modules/courier.module.ts:30-31 يكشف `phone` و`altPhone` لكل مهمة بلا قيد`
- **الأثر**: الوثيقة 9.6 «تسجيل الوصول»: كل قراءة لبيانات P1 تُقيَّد بالفاعل والحقل والسبب وتُراجع شهرياً، وتجاوز 50 كشفاً في يوم يطلق تنبيهاً. وهذا هو الضابط الوحيد الذي يكشف موظفاً يبيع قاعدة الأرقام — وهو الخطر الأقرب واقعاً في متجر صغير.

### لا محفِّزات قاعدة تمنع تعديل حقول التحصيل والتسويات المقفلة بأثر رجعي
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/migrations/0001_d1_init.sql:1171 و1192 — المحفِّزان الوحيدان هما `trg_units_match_levels` و`trg_reserved_le_onhand` (المخزون). لا وجود لـ`forbid_collection_rewrite` ولا `trg_settlements_locked` المكتوبَين حرفياً في الوثيقة 9.9(ج)`
- **الأثر**: الوثيقة تقول بالنص: «القاعدة مفروضة في قاعدة البيانات لا في التطبيق وحده». اليوم أي كتابة تمرّ خارج مسار `collect()` — أو خطأ في الشيفرة نفسها — تستطيع إعادة كتابة `collected_amount_syp` بلا قيد عكسي. وهذا أخطر بند في نظام كل نقده يمرّ بيد مندوب.

### زر «أضف للسلة» في صفحة المنتج بلا أي معالج — لا سبيل لإضافة منتج إلى السلة في الواجهة كلها
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/site/src/pages/p/[slug].astro:180 `<button class="btn btn--buy" type="button">` بلا onclick؛ سكربتا الصفحة (السطور 254–307) يغطيان المتغيّر والمقارنة فقط. `grep -rn "/carts" apps/site/src` = صفر نتائج، و`POST /carts/:token/items` (apps/api/src/http/routes.ts:89) لا يستدعيه أي ملف واجهة.`
- **الأثر**: قمع الشراء مقطوع من جذره: لا يمكن لأي زائر وضع منتج في السلة، فلا سلة ولا إتمام طلب ولا طلب واحد. كل ما بعده (checkout، الطلب، التتبّع، المندوب، التسوية) شيفرة لا تُبلَغ أبداً.

### واجهة المندوب بلا حقل للمبلغ المحصَّل — ترسل المبلغ المتوقَّع ثابتاً
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/admin/src/App.tsx:304-306 `collect(t)` يرسل `{ amountSyp: t.cashDueSyp }` كما جاء من الخادم؛ لا حقل إدخال ولا `inputMode="decimal"` ولا حالة PARTIAL. docs/10-admin.md:170 يشترط `collected_amount_syp` بلوحة أرقام و`PARTIAL` مع `reason_code`.`
- **الأثر**: التحصيل الجزئي أو المختلف عن المتوقَّع (وهو الشائع مع تقريب النقد وشح الفكّة) يُسجَّل كتحصيل كامل، فتخرج التسوية اليومية مطابقة كذباً ويُبتلع الفرق النقدي بصمت — وهو ما تبنى عليه كل شاشة التسويات.

### «تعذّر التسليم» بلا سبب مصنَّف — لا في الواجهة ولا في الخادم
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/admin/src/App.tsx:378 يرسل الحالة وحدها عبر `step(t.orderNo, 'DELIVERY_FAILED')`؛ وapps/api/src/modules/courier.module.ts:39 توقيعها `status(no, b: { to })` بلا `reason`. docs/10-admin.md:169 يجعل `reason_code` إلزامياً (NO_ANSWER/REFUSED/BAD_ADDRESS/POSTPONED).`
- **الأثر**: تقرير رفض الاستلام (10.6) و`phone_blocklist` وسياسة المحاولات الثلاث كلها مبنية على السبب. بلا سبب لا يُعرف أهو عنوان رديء أم رفض، ولا تُقيَّد أرقام المماطلين، وتضيع أخطر تكلفة في الدفع عند الاستلام بلا قياس.

### لا مسار /courier/task/:id ولا إثبات تسليم (صورة/توقيع)
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `لا شرط `path === '/courier/task'` في apps/admin/src/App.tsx:2445-2462؛ الشاشة الوحيدة `Courier()` (App.tsx:238) قائمة مسطّحة. لا مسار `/courier/orders/:id/proof` في apps/api/src/http/routes.ts (آخر مساري المندوب: السطران 486 و488).`
- **الأثر**: شاشة 16 في docs/05-mobile-ux.md:113 وبند «إثبات التسليم» في docs/10-admin.md:171 يشترطان إثباتاً إلزامياً للطلبات فوق 300 دولار. بلا إثبات، كل نزاع «سلّمتُه» / «لم يصلني» يُحسم بلا مستند وعلى حساب المتجر.

### عامل خدمة المندوب لا يُسجَّل أبداً: الشرط والمسار كلاهما خاطئ
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/admin/src/main.tsx:17 `location.pathname.startsWith('/courier')` بينما المسار الحيّ `/admin/courier` (apps/admin/vite.config.ts:6 `base: '/admin/'`)؛ والتسجيل على `'/sw.js'` بينما الملف يُبنى في `dist-worker/admin/sw.js`.`
- **الأثر**: الوعد الأساسي للمندوب — يفتح التطبيق في قبو بلا إشارة — لا يتحقّق. الشيفرة موجودة كاملة وتبدو صحيحة في المراجعة، لكنها لا تُنفَّذ سطراً واحداً في الإنتاج.


## عالية (79)

### أجرة الشحن مثبَّتة على تعريفة دمشق لكل المحافظات، وجدول shipping_rates المبذور لا يُقرأ أبداً
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/api/src/modules/orders.module.ts:101 `let shipping = 200;` وapps/api/src/modules/cart.module.ts:249 `let shipping = lines.length ? 200 : 0; // تعريفة دمشق الافتراضية`. والبذرة تضع سبع تعريفات مختلفة: apps/api/prisma/seed-d1.mjs:156-157 (حلب 400، اللاذقية 400، حمص 350…). و`grep -rn "ShippingRate\|shippingRate" apps/api/src` فارغ.`
- **الأثر**: كل طلب خارج دمشق يُحاسَب أقلّ من كلفته الحقيقية بدولارٍ إلى دولارين، ويأكل الفرقُ الهامشَ بصمت. والنموذج والبيانات جاهزان — الناقص سطرُ قراءةٍ واحد.

### سبعة إعدادات من ثلاثة عشر معروضة في اللوحة تُكتب ولا يقرؤها أحد
- **الحال**: جزئي · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `لا مستهلك لأيٍّ من: `free_shipping_above_usd_cents`, `confirmation_window_hours`, `order_hold_hours`, `order_hold_ext_hours`, `rare_stock_threshold`, `rare_hold_ext_hours`, `cash_rounding_step_syp` (فحصها واحداً واحداً بـgrep في apps/api/src خارج admin.module.ts وgenerated). والقيم مثبَّتة في الشيفرة: apps/api/src/modules/orders.module.ts:14-16 `ORDER_HOLD_HOURS = 2; CONFIRM_WINDOW_HOURS = 48; RARE_THRESHOLD = 2;`.`
- **الأثر**: لوحةٌ تعد بضبطٍ لا تملكه. تعديل مهلة الحجز أو عتبة الشحن المجاني يُحفَظ ويُسجَّل في التدقيق ولا يغيّر سلوك الطلب — فيظنّ صاحب المتجر أنه ضبط شيئاً ولم يضبط.

### عامل خدمة المندوب لا يُسجَّل أبداً: الشرط يفحص مساراً غير موجود، والملف يُطلَب من عنوان خاطئ
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/admin/src/main.tsx:17 `if ('serviceWorker' in navigator && location.pathname.startsWith('/courier'))` — واللوحة تُخدَم من `/admin/` (apps/admin/vite.config.ts:6 `base: '/admin/'`)، فالمسار الحقيقي `/admin/courier` والشرط لا يتحقّق أبداً. ولو تحقّق، :19 يسجّل `/sw.js` بينما الملف يُبنى إلى `dist-worker/admin/sw.js`. وapps/admin/public/sw.js:14 يخزّن `['/', '/index.html']` — وكلاهما خارج نطاق `/admin/`.`
- **الأثر**: عملُ الطابور غير المتصل (apps/admin/src/lib/offline.ts) مكتوب بعناية ومعطَّل بالكامل: تطبيق المندوب لا يفتح بلا شبكة. وبند الجاهزية TECH_COURIER_OFFLINE (readiness.module.ts:41) لا يمكن تأشيره صادقاً.

### المراجعات والأسئلة على صفحات المنتجات فارغة دائماً مهما كُتب في القاعدة
- **الحال**: محجوب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/site/src/lib/catalog.ts:180 `async function apiJson<T>(path, fallback) { if (!API) return fallback; … }` وAPI فارغ دوماً (البند أعلاه). فـ`reviewsFor` (:193) تُرجع EMPTY_REVIEWS و`questionsFor` (:196) تُرجع []. والصفحة تُخفي القسم كلّه: apps/site/src/pages/p/[slug].astro:196 `{reviews.count > 0 && …}`.`
- **الأثر**: طابور الإشراف في اللوحة، ونموذج كتابة المراجعة في التطبيق (apps/app/src/App.tsx:920-960)، وجداول القاعدة — كلها تعمل، ولا شيء منها يظهر للزائر. الدليل الاجتماعي أقوى محرّك بيع في سوقٍ يُدفَع فيه نقداً، وهو منقطع.

### «فحص IMEI: مطابق ونظيف» مكتوبةٌ ثابتةً على كل جهاز غير جديد بلا أي بيانات تسندها
- **الحال**: دَين · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/site/src/pages/p/[slug].astro:136-146 — الشرط الوحيد `{v.condition !== 'NEW' && (…)}` ثم نصّ ثابت `<b>فحص IMEI: مطابق ونظيف</b>` و«الرقم غير مبلَّغ عنه». ولا حقل IMEI في واجهة `Variant` (apps/site/src/lib/catalog.ts:14-30) ولا في البذرة.`
- **الأثر**: ادّعاءٌ فحصٍ لم يقع، معروضٌ كحقيقة على صفحة بيع. وتعليق apps/site/src/lib/seo.ts:8-11 يضع القاعدة بنفسه: «بياناتٌ منظَّمة كاذبة أسوأ من غيابها» — والقاعدة نفسها تنطبق على نصّ الصفحة.

### مكاتب النقل البري موثَّقة كمسار وواجهة وقيمة في القاعدة، وغائبة كلياً عن الشيفرة
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `docs/17-delivery-ops.md:199 يوثّق `GET/POST /api/v1/admin/transport-offices` — ولا وجود له في apps/api/src/http/routes.ts، ولا نموذج له في schema.prisma (`grep -n "^model"` يعطي ٤٩ نموذجاً ليس بينها واحد). ومع ذلك `collector_type` يقبل 'TRANSPORT_OFFICE' (apps/api/migrations/0002_new_pound.sql) والموقع يَعِد به: apps/site/src/lib/help.ts:150 وapps/site/src/pages/prices.astro:77.`
- **الأثر**: الموقع يَعِد الزبون خارج دمشق بالتوصيل عبر مكاتب النقل، ولا سبيل في النظام لتسجيل مكتب ولا لإسناد طلبٍ إليه ولا لتسوية نقده. والوعدُ المنشور بلا تنفيذٍ خلفه يتحوّل إلى نزاعٍ عند أول طلب من حلب.

### تهيئة القاعدة من الـWorker لا تُسجَّل في d1_migrations، فأول `wrangler d1 migrations apply` بعد منح الصلاحية سيفشل على قاعدة مليئة
- **الحال**: دَين · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/api/src/modules/bootstrap.module.ts:52-60 ينفّذ جُمل MIGRATION_SQL واحدةً واحدةً ولا يكتب شيئاً في أي جدول بكشف حساب. و`grep -rn d1_migrations apps scripts .github` فارغ تماماً. وscripts/bundle-sql.mjs:11 يحزم `0001_d1_init.sql` وحده — 0002 خارج الحزمة.`
- **الأثر**: اللحظة التي تُصلَح فيها الصلاحية الناقصة هي اللحظة التي ينكسر فيها الترحيل: الأداة ستحسب أن صفر ترحيلات طُبّقت، فتعيد `CREATE TABLE` على جداول قائمة وتفشل. وفخٌّ يُنصب لليوم الذي يُظنّ فيه أن المشكلة حُلّت.

### سعر الصرف التجريبي 128.5 محزوم في نصّ الـWorker وفي بذرة الموقع، وليس رقماً يُغيَّر من مكان واحد
- **الحال**: بيد المالك · **ينفّذه**: المالك · **✔ مُدقَّق** · *code*
- **الدليل**: `seed/catalog.demo.json:18 `"rate": 128.5` مع ملاحظة «سعر تجريبي… يُستبدل بالسعر الحقيقي من اللوحة قبل الإطلاق»، وapps/api/src/generated/bootstrap-sql.ts يحمله في جملة INSERT، وapps/site/src/lib/catalog.ts:132 `currentFx()` يقرؤه من البذرة لبناء كل صفحة.`
- **الأثر**: معروف مسبقاً، والمهمّ أنّ ضبط السعر من اللوحة يصلح الـAPI وحقن الحافة فقط؛ أما القيمة الاحتياطية المخبوزة في كل صفحة ساكنة فتبقى 128.5 حتى إعادة البناء — فيرى من عُطّل عنده جافاسكربت سعرَ أمسٍ بعيد.

### بوابة ميزانية الحزمة في النشر لا تقيس شيئاً: تُبلّغ 20 بايت في كل نشرة
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `.github/workflows/deploy.yml:60 — `find dist-worker -maxdepth 2 -name '*.js' -not -path 'dist-worker/admin/*' …`. محلياً هذا الأمر يلتقط dist-worker/admin/sw.js وحده وهو مستبعَد بالشرط نفسه، فالمجموعة فارغة و gzip للفراغ = 20 بايت. وسجل النشر 03:26:32 يقول حرفياً: «JavaScript المضغوط في الموقع الساكن: 20 بايت».`
- **الأثر**: ميزانية الفصل 5 غير مفروضة أصلاً: جافاسكربت الموقع الساكن مضمَّن داخل HTML (ثلاثة وسوم script في dist-worker/index.html بحجم 51 كيلوبايت) ولا يمرّ بالقياس. بوابةٌ تمرّ دائماً أسوأ من لا بوابة: تُعطي ثقةً بلا فحص.

### سير عمل الصيانة لم يُنفَّذ قط بنسخته الحالية — خمسة عشر تشغيلاً كلها فشل إقلاع
- **الحال**: محجوب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `list_workflow_runs لـ maintenance.yml: 15 تشغيلاً، كلها conclusion=failure، وlist_workflow_jobs لأحدثها (30503544238) يعيد total_count=0 — أي أن الملف لم يُقرأ أصلاً. السبب معرّف وظيفة عربي (`jobs: فحص:` في git show 9aa67f5:.github/workflows/maintenance.yml)، أُصلح لاحقاً — وآخر تشغيل مسجَّل 00:43 بينما الملف عُدِّل 00:54.`
- **الأثر**: كل ما هو حصريّ في هذا السير — الترحيل من الصفر، البذر، الفحص من طرف إلى طرف على Worker حقيقي، ميزانيتا app وadmin، فتح مسألة عند الفشل — لم يعمل ولا مرة. أول تشغيل حقيقي سيكون السبت القادم على شيفرةٍ لم تُختبر بهذا المسار.

### متغيّرات وظيفة الصيانة تُضبط على العدّاء لا على الـWorker، فالفحص من طرف إلى طرف سيصطدم بحدود الإنتاج
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `.github/workflows/maintenance.yml:26-29 تضبط JWT_SECRET وNODE_ENV=development وRATE_LIMIT_FACTOR=100 على مستوى الوظيفة، بينما `npx wrangler dev` (السطر 59) يقرأ متغيّراته من wrangler.jsonc:60-63 (NODE_ENV="production") ومن .dev.vars المستبعَد في .gitignore:12 — وهو غير موجود على العدّاء.`
- **الأثر**: داخل الـWorker ستكون NODE_ENV=production، وapps/api/src/common/rate-limit.ts:40 يُلغي المضاعِف في الإنتاج مهما ضُبط. فحصٌ يُنشئ عشرات الطلبات على /auth/otp/request (الحدّ 5 في الساعة) سيرتدّ بـ429 ويُعلَن فشلاً كاذباً — أو يُخفي فشلاً حقيقياً تحته.

### عدّاد حدّ المعدل على KV يُجدِّد مهلته مع كل زيادة، فالنافذة لا تنتهي أبداً
- **الحال**: دَين · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `apps/api/src/common/kv.ts:85-93 — `incr` يستدعي `this.set(key, next, ttlSec)`، و`set` في السطر 80 يمرّر expirationTtl جديدة في كل مرة. قارنها بـ MemoryKv في السطر 56 الذي يحفظ المهلة الأولى عمداً («المهلة تُضبط عند أول زيادة فقط»).`
- **الأثر**: في الإنتاج (السائق cf-kv) العدّاد يتراكم بلا انقضاء ما دام الطلب متواصلاً: زبونٌ تجاوز حدّ /orders (5 في الساعة) يبقى محجوباً إلى الأبد ما لم يصمت ساعةً كاملة. والسلوك يخالف تعليق الشيفرة نفسه، ولا يظهر محلياً لأن سائق الذاكرة يفعل الصواب.

### عامل خدمة المندوب لا يُسجَّل أبداً: الشرط يفحص مساراً لا وجود له، والملف على عنوان آخر
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `apps/admin/src/main.tsx:17 يشترط `location.pathname.startsWith('/courier')` بينما اللوحة كلها تُخدَم من /admin/* (apps/admin/vite.config.ts:6 — `base: '/admin/'`). والسطر 19 يسجّل '/sw.js': `curl -o /dev/null -w %{http_code} https://talisham.com/sw.js` ⇒ 404، بينما /admin/sw.js ⇒ 200. والخطأ يُبتلع بـ`.catch(() => {})`.`
- **الأثر**: طابور المزامنة بلا إنترنت — بند الجاهزية TECH_COURIER_OFFLINE — لا يعمل في الإنتاج. المندوب في قبوٍ بلا إشارة سيفقد كل تسجيل تسليم وتحصيل أدخله، وعطبٌ مبتلَع بـcatch فارغ لا يترك أثراً يُقرأ.

### لا مراقبة ولا تنبيه بمستقبِل بشري — سجلّات فقط، وأحدٌ لا يقرؤها
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `wrangler.jsonc:58 فيه `observability: { enabled: true }` وحده. grep -rni "sentry|tail_consumer|logpush|alarm|pagerduty|uptime" على apps و scripts و .github و wrangler.jsonc لا يعيد شيئاً. ولا فحص حياة خارجي لـ/api/v1/health في أي سير عمل.`
- **الأثر**: العطل يُكتشف اليوم بشكوى زبون. الكنّاس في worker.ts:219 يبتلع أخطاءه إلى console، وحقن سعر الصرف في worker.ts:105 يفعل المثل — فتوقّفٌ كامل للمهام المجدوَلة أو لسعر الصرف يمرّ أياماً بلا إشعار واحد. بند TECH_MONITORING يشترط «مستقبِلاً بشرياً» ولا مستقبِل.

### ربط R2 معطَّل: الحاوية جاهزة، و/media/* يردّ 503 حيّاً، ورفع أي صورة يردّ MEDIA_BUCKET_MISSING
- **الحال**: محجوب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `wrangler.jsonc:37-39 القسم معلَّق. r2_buckets_list يُظهر talisham-media منشأةً في 2026-07-30T00:14. `curl -o /dev/null -w %{http_code} https://talisham.com/media/x.jpg` ⇒ 503 (worker.ts:174). وقائمة الارتباطات في سجل النشر 03:26:41 بلا env.MEDIA. ورفع صورة يرتدّ من apps/api/src/modules/storage.ts:87.`
- **الأثر**: لا صورة منتج واحدة يمكن رفعها أو خدمتها في الإنتاج ما بقي المفتاح بلا R2:Edit. وبند الجاهزية CONTENT_IMAGES_DESC يشترط صورة أساسية لكل منتج منشور — فالمتجر لا يمكن أن يخرج من وضع العرض التجريبي أصلاً.

### مسارا bootstrap يتجاوزان حدّ المعدل وسجلّ التدقيق معاً لأنهما يُعالَجان قبل تطبيق Hono
- **الحال**: دَين · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `apps/api/src/worker.ts:143-164 يعالج /api/v1/bootstrap و/api/v1/bootstrap/admin-password ويعود، قبل السطر 166-168 حيث يُركَّب KV ثم `app.fetch` — وحدّ المعدل مركَّب داخل التطبيق في apps/api/src/http/app.ts:125. و apps/api/src/modules/bootstrap.module.ts:91-117 يضبط كلمة سرّ الإدارة بلا أي auditLog.create.`
- **الأثر**: أخطر إجراءٍ في النظام — منح كلمة سرّ أولى لحساب ADMIN — يجري بلا سقف محاولات وبلا سطر واحد في سجلّ التدقيق. الحارس الحالي (كلمة سرّ غير مضبوطة) أُغلق لحساب اليوم، لكن أي حساب ADMIN جديد يُنشأ لاحقاً يفتح النافذة ثانيةً لمن يعرف رقمه.

### مفاتيح VAPID غير مضبوطة، فالإشعارات تُتخطّى بصمت ولا شيء يقول ذلك للمشترك
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `apps/api/src/modules/push.module.ts:16-20 و115 — `if (!pushConfigured()) return { sent: 0, skipped: 'VAPID_NOT_CONFIGURED' }`. ولا ذكر لـVAPID في .github/workflows ولا في قائمة ارتباطات الـWorker في سجل النشر. والمسار /push/key معروض في apps/api/src/http/routes.ts:196 ويعيد سلسلة فارغة.`
- **الأثر**: زر التفعيل يعمل، والاشتراك يُسجَّل، ولا يصل شيء — وهو أسوأ من زرٍّ معطَّل صراحةً. تنبيهات التوفّر والسعر التي يُشغّلها الكرون كل خمس دقائق (worker.ts:229) تُحسب «أُرسلت» ولم تُرسَل.

### لا واتساب ولا SMS مضبوطان، فلا رمز دخول يصل إلى أي زبون في الإنتاج
- **الحال**: محجوب · **ينفّذه**: خارج سيطرتنا · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `apps/api/src/modules/channels.ts:8-11 و15-17 و36-40: بلا WHATSAPP_PROVIDER_TOKEN تُكتب الرسالة في السجل وتُعاد `{ ok: true, detail: 'simulated' }`؛ والقناة الاحتياطية SMS_PROVIDER_* غائبة هي الأخرى. و apps/api/src/modules/auth.module.ts:82 يعلن channel: 'WHATSAPP_THEN_SMS'.`
- **الأثر**: تسجيل الدخول كلّه برمز واتساب. بلا مفتاح Meta لا يستطيع زبونٌ واحد إنشاء حساب ولا إتمام طلب يحتاج حساباً — والواجهة تقول «أُرسل الرمز» فيقف الزبون ينتظر رسالةً كُتبت في سجلّ لا يقرؤه أحد.

### مفتاح النشر بلا صلاحية D1: Edit — الترحيل يُخفق في كل نشرة
- **الحال**: محجوب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `.github/workflows/deploy.yml:31-46 (continue-on-error: true + تحذير «CLOUDFLARE_API_TOKEN بلا صلاحية D1:Edit») · docs/DEPLOY.md «صلاحيتان ناقصتان في مفتاح النشر»`
- **الأثر**: كل تعديل على المخطَّط يحتاج تهيئة يدوية عبر مسار bootstrap مكشوف؛ وأحمرُ السجل الدائم يُخفي العطل الحقيقي حين يقع.

### مصدر سعر الصرف وآلية تحديثه غير موثَّقين ولا مسنَدين لشخص واحد
- **الحال**: بيد المالك · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/src/modules/readiness.module.ts:56 FIN_FX_SOURCE_DOCUMENTED (بند مانع للإطلاق) · docs/13-syria-reality.md:13.5 بند 1 يوصي: «السوق الموازية في دمشق + هامش 3% يُحدَّث يومياً من مكان واحد»`
- **الأثر**: سعرٌ يُدخله كل يوم شخص مختلف من مصدر مختلف يُنتج أسعاراً متضاربة على الرفّ وعند الباب، والفرق في المال لا يُغتفر في الدفع عند الاستلام.

### أجور التوصيل المبذورة سبعة أرقام مفترضة لا تعرفة حقيقية
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/prisma/seed-d1.mjs:155-158 (دمشق 200، ريف دمشق 250، حلب 400، حمص/حماة 350، اللاذقية/طرطوس 400 سنتاً) · لا مسار إداري لتعديلها: grep -n "shipping\|rates" apps/api/src/http/routes.ts → لا مسار`
- **الأثر**: الأجور من النواقل المحلية لا تُنشَر ولا API لها (docs/13-syria-reality.md:16)، فلا سبيل إليها إلا اتصال المالك بالناقل؛ ثم لا مكان يُدخلها فيه حتى يُبنى.

### مكاتب النقل البري غير موجودة ككيان في النظام أصلاً
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/src/modules/readiness.module.ts:39 OPS_TRANSPORT_OFFICES (بند مانع) · TRANSPORT_OFFICE يظهر في settlements.module.ts:46 كنوع محصِّل فقط، بلا جدول ولا مسار ولا شاشة: grep -rn "TRANSPORT_OFFICE" apps → موضعان لا ثالث`
- **الأثر**: التسوية تعرف نوع محصِّل «مكتب نقل» ولا تعرف مكتباً واحداً، فبيانات المكاتب التي يجمعها المالك لا وعاء لها.

### وحدة التقريب النقدي متضاربة: البذرة ألف والشيفرة عشرة والإعداد ميت
- **الحال**: جزئي · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/prisma/seed-d1.mjs:172 ['cash_rounding_step_syp', 1000] مقابل packages/ui/src/money.ts:16 CASH_ROUNDING_STEP = 10 وapps/api/src/common/money.ts:3 CASH_STEP = 10 · لا قارئ للإعداد: grep -rn "cash_rounding_step_syp" apps → اللوحة والبذرة والتحقق فقط`
- **الأثر**: docs/13-syria-reality.md:13.5 بند 2 يقول صراحةً «إن كانت فكّة العشرات غير متوفّرة عملياً فالرقم يُرفع»؛ القرار للمالك وحده لأنه من يواجه الفكّة عند الباب، والرقم اليوم لا يُغيَّر من اللوحة مهما ضُبط.

### بوابة الإقلاع التي تمنع نشر التجريبي في الإنتاج معرَّفة ولا تُستدعى
- **الحال**: دَين · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/src/common/boot-guard.ts:7 assertNoPublishedDemoInProd معرَّفة · grep -rn "assertNoPublishedDemoInProd" apps/api/src → موضع التعريف وحده`
- **الأثر**: شبكةُ الأمان الوحيدة التي تمنع افتتاحاً بمنتجات وهمية منزوعة، فالخطأ يمرّ صامتاً إلى الزبون.

### خريطة الموقع تنشر اثنين وعشرين رابط منتج وهمي إلى محركات البحث
- **الحال**: دَين · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/site/src/pages/sitemap.xml.ts:25 يُدرج كل products بلا ترشيح isDemo · حيّاً: curl -s https://talisham.com/sitemap.xml | grep -c "<loc>" → 62، وفيها /p/iphone-15-pro-max/ وأخواتها`
- **الأثر**: غوغل يفهرس صفحات لبضاعة غير موجودة؛ وحين تُحذف تصير روابط ميتة في نتائج البحث تُصحَّح بعد أشهر — وبند CONTENT_SITEMAP_SCHEMA مانع للإطلاق.

### سرّ توقيع الجلسات في الإنتاج غير مؤكَّد الضبط
- **الحال**: بيد المالك · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `.dev.vars:1 JWT_SECRET=dev_only_local_check · .env.example:15 JWT_SECRET="dev_only_change_me" · wrangler.jsonc التذييل: npx wrangler secret put JWT_SECRET · لا وسيلة للتحقق من الخارج`
- **الأثر**: مفتاح تطوير معروف في الإنتاج يعني أن أي أحد يُوقّع رمز جلسة مدير؛ والتحقق والضبط لا يتمّان إلا من حساب Cloudflare للمالك.

### بنود الجاهزية الاثنان والثلاثون لم تُبذَر ولم يُؤشَّر منها بند
- **الحال**: بيد المالك · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/src/modules/readiness.module.ts:14-58 (البنود) و:88 seed() لا تُستدعى إلا من POST /admin/readiness/seed (routes.ts:429) · seed-d1.mjs لا يبذرها · الزرّ في apps/admin/src/App.tsx:2167-2168 «لم تُبذَر بنود الجاهزية بعد»`
- **الأثر**: launch_ready حاصلُ جمعٍ لا رأي (readiness.module.ts:summary)، وبلا بذر وتأشير يبقى الافتتاح قراراً بالحدس؛ وكل بند مانع يلزمه إثبات مكتوب من مسؤوله.

### عمولة المندوب غير مقرَّرة — الشيفرة تفترض خمسة بالمئة
- **الحال**: بيد المالك · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/src/modules/settlements.module.ts:30-32 `return typeof row?.value === 'number' ? row.value : 500;` · courier_commission_bp غير مبذور: grep -n "courier_commission_bp" apps/api/prisma/seed-d1.mjs → لا نتيجة · يُضبط من /admin/settings`
- **الأثر**: العمولة تُقتطع من كل تسوية نقدية يومية بقيمة افتراضية لم يقرّرها أحد، والفرق يظهر في الصندوق آخر الشهر لا يوم التسوية.

### سياسة الضريبة صفر في المخطَّط بينما الدراسة تُلزم بخمسة بالمئة
- **الحال**: بيد المالك · **ينفّذه**: خارج سيطرتنا · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/prisma/schema.prisma:401 `taxRateBp Int @default(0)` و:402 taxAmountUsdCents @default(0) · docs/13-syria-reality.md:13.5 بند 11 «القيمة الافتراضية 5% لا صفر، والفاتورة تفصل: السعر، الضريبة، الإجمالي، رقم تسلسلي، رقم تسجيل ضريبي» · docs/18-content-settings.md:86 tax_rate_bp افتراضه 0`
- **الأثر**: رسم الإنفاق الاستهلاكي 5% مؤكَّد من الجمارك ومشروع ضريبة المبيعات معلن النفاذ؛ الحسم يحتاج محاسباً قانونياً سورياً ونصّ المرسوم، لا قراراً برمجياً.

### لا رقم تسجيل ضريبي ولا بيانات فاتورة نظامية في النظام
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/src/modules/readiness.module.ts:52 LEGAL_INVOICE_FIELDS «الفاتورة الورقية تحمل البيانات المطلوبة نظامياً» (بند مانع) · لا حقل tax_registration ولا invoice_footer في apps/api/prisma/schema.prisma`
- **الأثر**: الفاتورة تُسلَّم مع كل جهاز؛ نقصُ بياناتها النظامية مخالفةٌ تُكتشف عند أول تفتيش، والبيانات (السجل التجاري، الرقم الضريبي) لا يملكها إلا المالك.

### الأوراق النقدية القديمة تفقد قوتها الإبرائية والنظام لا يعرف بذلك
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `docs/13-syria-reality.md:16 «الأوراق القديمة تفقد قوتها الإبرائية من 31/7/2026» و13.5 بند 3 «وعاجلاً: رفض أي ورقة قديمة من 31/7/2026 مع بانر في صفحة الدفع» · grep -rn "الأوراق القديمة\|31/7" apps → لا نتيجة في الشيفرة`
- **الأثر**: التاريخ غداً، والتحصيل نقدي بالكامل؛ مندوبٌ يقبض ورقة لا تُصرَف يخسر المتجر قيمتها كاملةً — والتعليمات للمندوبين والبانر قرارٌ تشغيلي للمالك.

### صفحة المساعدة تعد الزبون برسالة واتساب لا يمكن أن تصل
- **الحال**: جزئي · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/site/src/lib/help.ts:31 «تصلك رسالة واتساب فيها زرّا أؤكد وألغِ» مقابل apps/api/src/modules/channels.ts:16-19 (محاكاة) وdocs/13-syria-reality.md:19 (حظر ميتا) · وقاعدة الملف نفسه في legal.ts:5 «لا يُكتب إلا ما ينفَّذ فعلاً»`
- **الأثر**: وعدٌ منشور بلا شيفرة خلفه: الزبون ينتظر رسالة لا تأتي فيظن الطلب ضائعاً — والقرار عن القناة البديلة (مكالمة أم SMS) للمالك قبل تعديل النص.

### مسار التهيئة مكشوف على الإنتاج بلا مصادقة ويكشف بنية القاعدة
- **الحال**: دَين · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `curl -s https://talisham.com/api/v1/bootstrap → {"data":{"initialized":true,"tables":51}} · apps/api/src/worker.ts (المسار قبل طبقة الخدمات والمصادقة) · docs/DEPLOY.md «وبعدهما… واحذف مسار /api/v1/bootstrap — الأداة أولى بذلك العمل»`
- **الأثر**: مسارُ تهيئةٍ يبقى بعد التهيئة سطحُ هجومٍ بلا فائدة، وحذفه معلَّق على صلاحية D1 التي بيد المالك.

### قبول Cloudflare لحساب سوري غير مؤكَّد ولا نسخة DNS احتياطية
- **الحال**: بيد المالك · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `docs/13-syria-reality.md:13.5 بند 10 «لا مصدر يؤكد ولا ينفي قبول Cloudflare لحساب سوري في 2026 — ثقة منخفضة… حساب مجاني يُختبر قبل نقل النطاق، نسخة DNS احتياطية، وبديل أوروبي جاهز لا نظري، والنطاق عبر مسجّل دولي ببطاقة خارج سوريا»`
- **الأثر**: المتجر كله على أصل واحد عند مزوّد واحد؛ إغلاقُ الحساب يُسقط الموقع والتطبيق واللوحة والقاعدة معاً، والنسخة الاحتياطية وترتيب المسجّل والبطاقة قرارات وحسابات تخصّ المالك.

### صحة المنتجات المستعملة ودرجاتها وأرقام IMEI بيانات لا مصدر لها إلا الجرد
- **الحال**: بيد المالك · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/src/modules/readiness.module.ts:33 CONTENT_USED_GRADES و:34 CONTENT_WARRANTY_SHOWN و:44 OPS_OPENING_STOCKTAKE (بنود مانعة) · docs/13-syria-reality.md:13.5 بند 13 يُلزم بـimei لكل قطعة مباعة ولصاقة SYTRA وcustoms_status وصحة البطارية`
- **الأثر**: الإفصاح الكامل هو التمايز الأول في سوق ربعُ أجهزته «غير معروفة» (docs/13-syria-reality.md:13.3 بند 5)؛ ولا يعرف حالة كل جهاز على الرفّ إلا من جرده بيده.

### لا ترويسات أمان على الإطلاق: لا CSP ولا HSTS ولا X-Frame-Options ولا Permissions-Policy
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `grep -rn 'Content-Security-Policy|Permissions-Policy|Strict-Transport|X-Frame-Options' apps/ scripts/ wrangler.jsonc → لا نتيجة ؛ لا ملف dist-worker/_headers ولا apps/site/public/_headers`
- **الأثر**: الفصل 9 §9.10 وجدول A05 يشترطانها. الموقع يحمّل نصوصاً inline ويحقن سعر الصرف عبر HTMLRewriter، فغياب CSP يجعل أي ثغرة XSS واحدة كافية لسرقة رموز الدخول المخزَّنة في localStorage.

### CORS يعكس أي أصل طالب مع credentials: true
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/api/src/http/app.ts:126-127 — cors({ origin: (o) => o ?? '*', credentials: true, … })`
- **الأثر**: كل شيء على أصل واحد فالـCORS غير محتاج إليه أصلاً، ووجوده بهذه الصيغة يفتح كل مسارات الواجهة لأي موقع خارجي يحمل رمز الضحية.

### رمزا الوصول والتحديث في localStorage، بلا كوكي HttpOnly وبلا CSRF
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/app/src/lib/api.ts:9-22 (localStorage.getItem/setItem لـaccess_token وrefresh_token) ؛ apps/admin/src/lib/api.ts ؛ docs/09-security.md:73-74,179 تشترط الذاكرة فقط وكوكي __Host-ts_rt وترويسة X-CSRF-Token`
- **الأثر**: رمز تحديث عمره ثلاثون يوماً في تخزين يقرؤه أي نص يعمل في الصفحة. والانحراف عن الفصل 9 هنا ليس تفصيلاً أسلوبياً بل إسقاط لطبقة الحماية المكتوبة كاملةً.

### لا تدوير لرمز التحديث ولا كشف لإعادة استخدامه
- **الحال**: جزئي · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/api/src/modules/auth.module.ts:143-163 — refresh() تتحقق ثم تُصدر زوجاً جديداً بلا إبطال القديم؛ الرمز JWT بلا حالة، فلا family_id ولا jti مخزَّن`
- **الأثر**: docs/09-security.md:77 يشترط إبطال الرمز عند كل تجديد وإبطال العائلة عند إعادة استخدام رمز مُبطَل. حالياً رمز مسروق يبقى صالحاً ثلاثين يوماً بالتوازي مع رمز صاحبه بلا أي أثر.

### لا وجود لبوت تيليغرام لا في الشيفرة ولا في الخطة التنفيذية
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `grep -rniE 'telegram|تيليغرام' apps/ packages/ --include=*.ts --include=*.tsx --include=*.astro → صفر نتيجة ؛ المطلوب في docs/13-syria-reality.md:32 و 57`
- **الأثر**: القناة الوحيدة المتاحة مجاناً وبلا حظر قُطري موثَّق، والعرف المحلي قائم بقنوات أسعار يومية. صفحة /prices مبنيّة أصلاً ولا شيء ينشرها إلى حيث يقرأ السوق.

### الضريبة صفر في كل موضع، والفصل 13 أبطل هذا الافتراض وقرّر 5% افتراضاً
- **الحال**: غائب · **ينفّذه**: المالك · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/api/prisma/schema.prisma:401 (taxRateBp Int @default(0)) ؛ apps/api/migrations/0001_d1_init.sql:288 ؛ grep -n 'tax' apps/api/src/modules/orders.module.ts apps/api/src/modules/cart.module.ts → صفر نتيجة ؛ docs/13-syria-reality.md:61`
- **الأثر**: رسم الإنفاق الاستهلاكي 5% مؤكَّد من الجمارك على الهواتف. البيع بسعر لا يتضمنه يعني أن الهامش المحسوب خطأ على كل طلب، ولا حقل مملوء يمكن أن تُبنى عليه فاتورة نظامية.

### لا شيفرة إصدار فاتورة أو إيصال ورقي في اللوحة
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `grep -rn 'طباعة|print|receipt|Invoice' apps/admin/src/App.tsx → مطابقة واحدة فقط في السطر 1987 وهي إدخال IMEI لاستلام أمر شراء ؛ بند الجاهزية LEGAL_INVOICE_FIELDS في apps/api/src/modules/readiness.module.ts:49`
- **الأثر**: 19.2 تجعل «طباعة الإيصال» مهمة يومية، و13.5 بند 11 يشترط فصل السعر والضريبة والإجمالي ورقم تسلسلي ورقم تسجيل ضريبي. لا شيء من ذلك موجود، فالبند المانع للإطلاق لا يمكن تأشيره بإثبات.

### لا جدول شحنات ولا رقم بوليصة ولا مكتب نقل — فوعد التتبع بين المحافظات بلا سند
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `grep -n 'shipment|Shipment|waybill|office_name' apps/api/prisma/schema.prisma → صفر نتيجة ؛ docs/03-data-model.md:228 يعرّف office_name وwaybill_no وreceipt_media_id وupdated_source ؛ 19.1 صفّ التتبع يعد بـ«مكتب النقل ورقم البوليصة»`
- **الأثر**: التوصيل خارج دمشق يقوم على مكاتب النقل البري حصراً. بلا الجدول لا يُسجَّل رقم بوليصة ولا صورة إيصال، فلا الزبون يتتبّع ولا المتجر يثبت الإرسال عند النزاع.

### لا إسناد مندوبين ولا بناء مسارات ولا عهدة — ثلاث مهام يومية في 19.2 بلا شاشة ولا مسار
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `routes.ts فيه /admin/delivery/orders/:orderNo/suggest (السطر 362) فقط؛ لا /admin/assignments ولا /admin/routes ولا /admin/handovers ولا /admin/shipments ؛ grep -n 'handover' على apps/api/src و apps/admin/src → صفر`
- **الأثر**: الاقتراح يُحسب ولا يُحفَظ في أي مكان: لا يوجد حقل إسناد على الطلب أصلاً. فيوم العمليات (تعيين، مسار، تسليم عهدة، استلامها) يظل يُدار خارج النظام، وتسوية النقد تفقد طرفها الأول.

### لا قائمة تقييد للدفع عند الاستلام رغم أنها مهمة يومية وضابط مخاطرة أساسي
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `grep -rn 'blocklist' apps/ → صفر نتيجة ؛ 19.2 يوميّاً: «فحص طلبات الخطر … /admin/cod/blocklist» ؛ docs/09-security.md:171 يذكر phone_blocklist ضمن ضوابط A04`
- **الأثر**: في متجر يشحن بلا ضمان مسبق، تكرار رفض الاستلام من الرقم نفسه هو الخسارة الأولى. الموجود فقط سقف قيمة (cod_max_order_usd_cents) وحدّ طلبات مفتوحة، وهما لا يمنعان المتكرر.

### مهام الصيانة الدورية غير موجودة: لا مطابقة مخزون ولا تدقيق نقد ولا طلبات عالقة ولا فحص طزاجة سعر الصرف ولا تمرين القناة الاحتياطية
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/api/src/worker.ts scheduled() ينفّذ ثلاثة أشياء فقط: sweep و alerts.scan و runDueDeletions ؛ docs/11-devops.md:281-288 يعرّف maint.reindex-verify و maint.stock-reconcile و maint.cash-audit و maint.stuck-orders و maint.fallback-channel-drill و maint.fx-freshness ؛ لا مسار /admin/maintenance/last-run في routes.ts`
- **الأثر**: 19.2 أسبوعياً يجعل «تقرير الصيانة الآلي» بنداً آلياً بالكامل بشاشة تعرض آخر تشغيل. غيابه يعني أن انحراف المخزون عن الرفّ — أي البيع الزائد — لا يُكتشف إلا حين يقف المندوب أمام باب بلا جهاز.

### الموقع يعرض السعر الخام أثناء فترة هامش الأمان بينما السلة تحتسبه +3%
- **الحال**: جزئي · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/api/src/worker.ts دالة liveFx: SELECT rate, valid_until, safety_margin_bp … تُعيد rate كما هو بلا health وبلا هامش ؛ مقابل apps/api/src/modules/fx.module.ts:24-25 (rate = health==='STALE_MARGIN' ? base*(1+safetyMarginBp/10000) : base) ؛ ونص العرض في apps/site/src/layouts/Base.astro:110 يضرب بـfx.rate مباشرةً`
- **الأثر**: نافذة اثنتي عشرة ساعة يرى فيها الزبون رقماً على البطاقة ويُطلَب منه رقمٌ أعلى عند الباب. في متجر يُدفع فيه نقداً هذا بالضبط ما يُقرأ تلاعباً، وهو العطل الذي حُقن سعر الصرف من الحافة لتفاديه.

### صفحة الفئة بلا ترشيح ولا فرز ولا ترقيم صفحات — تتجاهل معاملات الاستعلام كلها
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/site/src/pages/c/[slug].astro:24 يفرز بالسعر ثابتاً ويعرض sorted كاملاً في السطر 71؛ لا Astro.url.searchParams في الملف كله ؛ 19.1 صفّ «التصفح والتصنيف» يعد بـ/c/[slug]?brand=…&condition=… بلا JS، بالعلامة والسعر والحالة وصحة البطارية والتوفر`
- **الأثر**: هذا صفّ M0 صريح في مصفوفة الاكتمال. الروابط الموعودة تعمل شكلاً وتتجاهَل مضموناً، وهو أسوأ من غيابها: تُشارَك وتُفهرَس ثم تعطي نتيجة لا علاقة لها بالمرشِّح.

### رفض الأوراق النقدية القديمة اعتباراً من 31/7/2026 بلا أي أثر في الشيفرة — والتاريخ غداً
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `grep -rniE 'ورقة قديمة|الأوراق القديمة|banknote' apps/ → صفر نتيجة ؛ docs/13-syria-reality.md:53 «وعاجلاً: رفض أي ورقة قديمة من 31/7/2026 مع بانر في صفحة الدفع» ؛ وسطر 21: الأوراق القديمة تفقد قوتها الإبرائية في ذلك التاريخ`
- **الأثر**: لا بانر في صفحة الدفع ولا تعليمة في واجهة المندوب. أول مندوب يقبض بأوراق قديمة بعد غد يحمل نقداً لا يُقبل في أي مصرف، وخسارته على المتجر لا على الزبون.

### docs/compliance/ غير موجود رغم أنه بند إلزامي مكتوب قبل فتح المتجر للعموم
- **الحال**: بيد المالك · **ينفّذه**: المالك · **✔ مُدقَّق** · *readiness*
- **الدليل**: `ls docs/compliance → No such file or directory ؛ docs/09-security.md:224 «بند إلزامي قبل الإطلاق … يُوثَّق ناتج المراجعة في docs/compliance/ قبل فتح المتجر للعموم»`
- **الأثر**: السجل التجاري وترخيص النشاط الإلكتروني ومتطلبات الفوترة السارية ومدة حفظ البيانات كلها معلَّقة على مراجعة محامٍ سوري. ولا يمكن لأي شيفرة أن تسدّ هذا، والفصل 9 ينصّ صراحةً على أنه ليس بديلاً عنها.

### خطوة ترحيل D1 تُتجاوَز بصمت عند فشلها، فقد ينحرف مخطَّط الإنتاج عن ملفات الترحيل
- **الحال**: دَين · **ينفّذه**: المالك · **✔ مُدقَّق** · *readiness*
- **الدليل**: `.github/workflows/deploy.yml — الخطوة «ترحيل قاعدة D1» بـcontinue-on-error: true، والنتيجة تُطبع كـ::warning:: فقط ؛ والتهيئة تمّت مرة عبر POST /api/v1/bootstrap (apps/api/src/worker.ts، مسارا bootstrap ما زالا في الشيفرة)`
- **الأثر**: القرار صحيح مؤقتاً لأن المفتاح بلا صلاحية D1، لكن أثره أن أي ترحيل جديد (0003 وما بعده) يُرفَع نصّه ولا يُطبَّق على القاعدة الحيّة، فتقرأ الشيفرة عموداً غير موجود عند أول طلب. لا شيء اليوم يقيس الفرق بين الترحيلات المطبَّقة والمكتوبة.

### لا فصل أصل للوحة ولا مطالبة `scope`/`aud` في الرمز — رمز واجهة المتجر يصلح للوحة
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/common/jwt.ts:8-17 — `Claims` بلا `aud` ولا `scope` ولا `perms`. و apps/api/src/worker.ts:186-187 يخدم `/app` و`/admin` من الأصل نفسه. والحارس يفحص الدور فقط: apps/api/src/http/auth.mw.ts:62`
- **الأثر**: الوثيقة 9.2 «عزل لوحة التحكم»: `admin.talisham.com` أصل مستقل بكوكي خاص ومطالبة `scope:"admin"` «فرمز واجهة المتجر لا يُرقّى إلى صلاحية إدارية مهما سُرق». مع الأصل الواحد وlocalStorage المشترك، XSS في صفحة منتج عامة يصل إلى رمز اللوحة مباشرة.

### لا مصادقة ثنائية (TOTP) على حسابات لوحة التحكم رغم أنها إجبارية في الوثيقة
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: ``grep -rni "totp\|2fa\|otpauth\|two.factor" apps/ packages/` ← لا نتيجة. ودخول الموظّف بكلمة سرّ وحدها: apps/api/src/modules/auth.module.ts:171-214`
- **الأثر**: الوثيقة 9.3 آخر فقرة: «كل حسابات لوحة التحكم تتطلب تفعيل TOTP إجبارياً». اليوم كلمة سرّ واحدة تفصل بين مهاجم وبين تغيير سعر الصرف وإقفال التسويات — والقفل بعد خمس محاولات لا يمنع تسريب كلمة السرّ نفسها.

### نموذج الصلاحيات بالأدوار الجامدة لا بصيغة `مورد:فعل:نطاق`، ولا جدولا `roles` و`role_permissions`
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/http/auth.mw.ts:59-69 — `protect(prisma, ...roles)` تقارن `roles.includes(claims.role)` فقط. و apps/api/src/http/routes.ts:19-25 يعرّف ستة تجمّعات أدوار ثابتة. ولا `perms` في الرمز: apps/api/src/common/jwt.ts:8-17`
- **الأثر**: جدول الصلاحيات في الوثيقة 9.3 يعدّ 24 صلاحية بأربعة أدوار متمايزة، ولا شيء منه قابل للتعبير بالأدوار الجامدة. النتيجة العملية أن كل توسيع صلاحية يعني تعديل شيفرة ونشرة، وأن صلاحيات لا يجوز اجتماعها (`payments:collect:own` و`settlements:write:any`) لا يوجد في النظام ما يمنع اجتماعها أصلاً.

### حدود المعدل الحسّاسة تُحسب بالـIP بدل المستخدم/المندوب
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/common/rate-limit.ts:21 `POST /orders` بـ`by: 'ip'` (الوثيقة: 5 لكل مستخدم/ساعة)؛ السطر 26 `courier/*` بـ`by: 'ip'` (الوثيقة: لكل مندوب)؛ السطر 25 `admin/fx` بـ`by: 'ip'` (الوثيقة: لكل مستخدم). ولا موضوع `'user'` مستعمل في أي قاعدة رغم تعريفه في السطر 6`
- **الأثر**: IP واحد في سوريا قد يخدم مقهىً كاملاً خلف NAT، فحدّ 5 طلبات/ساعة يمنع زبائن شرعيين؛ وفي المقابل مهاجم واحد بشبكة عناوين يتجاوزه كلّه. جدول 9.5 يحدّد الموضوع لكل نقطة عمداً، والانحراف عنه يُفسد الحماية والخدمة معاً.

### تسعة مسارات من جدولَي 9.5 و9.11 بلا قاعدة حدّ خاصة، فترث الحدّ الافتراضي الأوسع بكثير
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `قائمة القواعد في apps/api/src/common/rate-limit.ts:14-27 لا تذكر: `/auth/refresh` (routes.ts:57، الوثيقة 60/ساعة)، `/reviews` (routes.ts:140، 5/يوم)، `/support/tickets` (routes.ts:168، 5/يوم)، `/support/tickets/*/reply` (routes.ts:173، 30/ساعة)، `/me/warranty-claims` (routes.ts:150، 3/يوم)، `/catalog/products/*/questions` (routes.ts:136، 5/يوم)، `/me/alerts/*` (5/يوم)، `/admin/settlements` (routes.ts:289-297)، `/admin/cod/blocklist`، `/rum`. والافتراضي `{ limit: 60, windowSec: 60 }` في rate-limit.ts:30`
- **الأثر**: الافتراضي 60/دقيقة = 86,400/يوم، أي أوسع بـ17,000 مرة من حدّ «3 مطالبات كفالة يومياً». والوثيقة تحذّر بنصّها: «المسار غير المذكور هنا يرث الحدّ الافتراضي العام وهو حد غير مقصود لأي عملية حساسة». إغراق الدعم والمراجعات المزيّفة مفتوحان اليوم.

### `audit_logs` بلا عنوان IP ولا `X-Request-Id` ولا قيمة قبل/بعد منظَّمة — حقل `ip` موجود في المخطَّط ولا يُملأ أبداً
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/prisma/schema.prisma:509-521 يعرّف `ip String?` ولا حقل `requestId`. وفي 27 موضع استدعاء لـ`auditLog.create` عبر apps/api/src/modules/ لا يمرَّر `ip` في أي منها (`grep -rn -A6 "auditLog.create" apps/api/src/modules | grep -c ip` ← 0). والحارس لا يمرّر سياق الطلب إلى الخدمات أصلاً: apps/api/src/http/auth.mw.ts:67`
- **الأثر**: الوثيقة 9.3 و9.9(3) تشترطان «الفاعل، الدور، الإجراء، الكيان، القيمة قبل/بعد، IP، `X-Request-Id`». سجلّ بلا IP ولا معرّف طلب لا يصلح للتحقيق في اختلاس نقدي — وهو الغرض الوحيد الذي كُتب لأجله. كما أن «محاولة الكتابة المرفوضة» لا تُقيَّد إطلاقاً رغم النص عليها.

### نقطتا bootstrap مكشوفتان قبل طبقة حدّ المعدل، وتفرّقان بين رسائل الفشل فتصلحان أداة تعداد لحسابات الإدارة
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/worker.ts:143-164 — الفرعان يُعالَجان ويُعاد منهما قبل `useCloudflareKv` و`app.fetch` (السطران 166-168) أي خارج `rateLimit`. و apps/api/src/modules/bootstrap.module.ts:97-105 يردّ ثلاث رسائل متمايزة: `NOT_FOUND` / `NOT_ADMIN` / `PASSWORD_ALREADY_SET``
- **الأثر**: المستودع نفسه يذكر أن مسارات bootstrap ما زالت في الشيفرة. الأخطر أنها بلا حدّ معدل: يمكن تجريب الأرقام بلا سقف، والفرق بين الرسائل يكشف أي رقم هو حساب ADMIN. وإن وُجد حساب إدارة بلا كلمة سرّ بعد، فالسبق لمن يصل أولاً — والفائز يملك اللوحة. وهذا نقض مباشر لقاعدة الرسالة الموحّدة التي تطبّقها auth.module.ts:172.

### `publicId()` مولَّد بـ`Math.random` — وهو المعرّف الذي يحمله `sub` في كل رمز
- **الحال**: دَين · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/common/money.ts:24-28 — `s += B32[Math.floor(Math.random() * B32.length)]`. ويُستعمل لمستخدم كل حساب: apps/api/src/modules/auth.module.ts:118`
- **الأثر**: الوثيقة 9.1 تنصّ صراحةً على `crypto.randomInt` «وليس `Math.random`» لتوليد ما يخصّ الهوية. `Math.random` غير مصمّم للأمان وحالته قابلة للاستنتاج، والمعرّف هنا ليس زينة: هو ما يُحمَل في `sub` وما يُقبل في مسارات تُطابق عليه.

### لا تدوير أسرار من أي نوع: لا مفتاحان متزامنان ولا جدول تدوير ولا تنبيه انتهاء
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/common/jwt.ts:3 — سرّ واحد ثابت بلا `kid` ولا مجموعة مفاتيح، فتدوير المفتاح يُخرج كل الجلسات القائمة دفعة واحدة. ولا مسار JWKS في apps/api/src/http/routes.ts. ولا مهمة تنبيه انتهاء في .github/workflows/maintenance.yml`
- **الأثر**: جدول 9.8 يعدّ سبعة أسرار بمُدد تدوير 30–365 يوماً، وآلية مفتاحَين متزامنَين في JWKS بفترة تداخل 24 ساعة كي لا يُطرد المستخدمون عند التدوير. اليوم لا يوجد مسار تدوير أصلاً، فالسرّ الذي يُشتبه بتسريبه يبقى — لأن تغييره يعني إخراج كل الزبائن في لحظة واحدة.

### لا فحوص أمنية مانعة للدمج: لا gitleaks ولا osv-scanner ولا Trivy، و`pnpm audit` غير مانع
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `.github/workflows/maintenance.yml:79 — `pnpm audit --audit-level high || echo "::warning::..."` (الـ`||` يبتلع الفشل). و`grep -n "gitleaks\|osv\|trivy" .github/workflows/*.yml` ← لا نتيجة. وإجراءات GitHub غير مثبَّتة بالتجزئة: .github/workflows/deploy.yml:19-21 و35 و64 كلها `@v3`/`@v4``
- **الأثر**: الوثيقة 9.8 تشترط «فحص gitleaks مانع للدمج»، و9.4 A06 «osv-scanner وTrivy مانعان للدمج»، وA08 «تثبيت إصدارات إجراءات GitHub بالتجزئة (SHA)». وسمٌ متحرك مثل `@v4` يعني أن اختراق مستودع الإجراء يضع شيفرة غريبة داخل خط نشرٍ يملك مفتاح Cloudflare.

### محاكاة واتساب تعيد `ok: true` دائماً، فقاطع الدارة والتصعيد إلى SMS لا ينفتحان أبداً
- **الحال**: محجوب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/modules/channels.ts:16-19 — بلا `WA_TOKEN` تُطبع الرسالة وتُعاد `{ ok: true, detail: 'simulated' }`. فالشرط `if (!wa.ok)` في apps/api/src/modules/notifications.service.ts:68 لا يتحقّق، و`waFailures` (السطر 34) يبقى فارغاً`
- **الأثر**: الوثيقة 9.12: «قناة SMS الاحتياطية لا تُعامل بوصفها موجودة ما لم تُختبر»، والتبديل يقع تلقائياً عند تجاوز الفشل 5% خلال 15 دقيقة. اليوم مسار التصعيد كله شيفرة لم تُنفَّذ مرة واحدة — سيُختبر لأول مرة يوم يسقط المزوّد الحقيقي، وهو أسوأ وقت للاختبار. يلزم مفتاح Meta لفتح هذا المسار.

### منع تكرار الإشعارات في ذاكرة العزلة لا في مخزن مشترك — يضيع مع كل عزلة
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/modules/notifications.service.ts:18 — `private sent = new Map<string, number>()` داخل نسخة الخدمة، والخدمة تُبنى لكل طلب: apps/api/src/worker.ts:41-43 (`buildDeps` داخل `api(env)`)`
- **الأثر**: الخريطة تُنشأ من جديد مع كل طلب، فمنع التكرار لا يعمل إطلاقاً — لا لأربع وعشرين ساعة ولا لثانية. والوثيقة 20.3 تفرض `dedupe_key` مخزَّناً 24 ساعة، وتعليق kv.ts:5-7 يشرح المبدأ نفسه ويطبّقه على الرموز والحدود دون الإشعارات. النتيجة: زبون يتلقى الرسالة نفسها مرات، وهو بالضبط ما يجعله يكتم القناة.

### تفضيلات الإشعار لا تُفحص عند الإرسال — إشعارات P3 تخرج بلا موافقة
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/modules/notifications.service.ts:42-87 — `send()` لا تقرأ `notifyPrefs` ولا تستقبل معرّف مستخدم أصلاً (توقيعها يأخذ `to: string` فقط). والتفضيلات تُخزَّن وتُقرأ في apps/api/src/modules/account.module.ts:189-212 ولا تُستهلك في أي مكان (`grep -rn notifyPrefs apps/api/src` ← المخطَّط وaccount.module فقط)`
- **الأثر**: الوثيقة 20.3: «P3 لا يُرسل أبداً لمن لم يوافق صراحةً، ولا يُرسل لعميل لديه طلب نشط أو شكوى مفتوحة»، وحدّه 4 أسبوعياً. الشاشة موجودة والزبون يضبطها، ولا شيء يقرأ ما ضبطه — وعدٌ معروض في الواجهة لا يفي به الخادم.

### عامل الخدمة بلا مستمع `push` ولا `notificationclick`، ومقصور على `/courier` وحدها
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/admin/public/sw.js — المستمعون الثلاثة هم `install` و`activate` و`fetch` فقط، لا `push`. ولا عامل خدمة في apps/app ولا apps/site (`find apps -name sw.js` ← apps/admin فقط). والتسجيل مشروط: apps/admin/src/main.tsx:17 `location.pathname.startsWith('/courier')``
- **الأثر**: الوثيقة 20.4: «عامل خدمة واحد لكل من `apps/site` و`apps/app`»، وتحدّد الحمولة بـ`tag` و`renotify` و`actions` وسلوك النقر (تركيز تبويب مفتوح). بلا مستمع `push` لا يظهر أي إشعار متصفح حتى لو أُرسل، وبلا عامل في `apps/app` لا قناة Push للزبائن إطلاقاً.

### لا تدفّق طلب إذن الإشعارات: لا طلب لطيف ولا تأجيل 30 يوماً ولا إرشاد تثبيت iOS
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: ``grep -rn "Notification.requestPermission\|pushManager" apps/app/src apps/site/src apps/admin/src` ← لا نتيجة. ومفاتيح VAPID غير مضبوطة فـ`pushConfigured()` تعيد false: apps/api/src/modules/push.module.ts:16-20، فـ`GET /push/key` (routes.ts:196) يعيد null`
- **الأثر**: الوثيقة 20.4 تسمّي التوقيت «أهم قرار في هذا القسم» لأن الرفض في المتصفح دائم: «طلبه في اللحظة الخاطئة يحرق القناة للأبد». وتشترط أربع خطوات مرقّمة. اليوم لا يوجد أي زر يطلب الإذن، فالقناة الثالثة الوحيدة المجانية غير مفعّلة أصلاً — وهو ما يبقي كل الحمل على واتساب المدفوع.

### تجهيل الحساب عند الحذف جزئي: العناوين وبيانات الشحن تبقى بالاسم والهاتف والمعلم القريب
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/modules/account.module.ts:306 — `address.updateMany({ data: { deletedAt: now } })` حذف ناعم يُبقي `recipientName` و`phone` و`landmark` و`geo` كما هي. والسطر 316 يستبدل الهاتف بـ`+000...` لا بالصيغة المقنَّعة `+9639****4417` التي تحدّدها الوثيقة، ولا بريد `deleted-{uuid}@anon.local`، ولا مسح لصور التسليم`
- **الأثر**: الوثيقة 9.6 «حق الحذف» تعدّ ما يُخفى بالاسم: تقنيع الرقم، وتجهيل البريد، و«مسح صور التسليم». وصور التسليم تحديداً «تحمل إحداثيات منازل العملاء» بحسب 9.4. حسابٌ حُذف ولا يزال عنوان بيت صاحبه ومعلمه القريب مقروءَين في القاعدة ليس محذوفاً — والوعد مكتوب في سياسة الخصوصية المنشورة.

### لا بيان (manifest) لواجهة المندوب رغم أن تثبيتها إلزامي
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: ``ls apps/admin/public/` = `sw.js` فقط؛ وapps/admin/index.html بلا `<link rel="manifest">`. docs/05-mobile-ux.md:503 يطلب بياناً منفصلاً بـ `start_url: "/courier"`، و:504 «تثبيت واجهة المندوب إلزامي ضمن تدريب المندوب».`
- **الأثر**: بلا بيان لا يظهر خيار «إضافة إلى الشاشة الرئيسية»، فيبقى المندوب داخل تبويب متصفح: يفقد وضع standalone، ولا يُثبَّت العامل، وأول إغلاق للتبويب يعني إعادة تسجيل دخول في الشارع.

### مهام المندوب غير مقيَّدة بالمندوب: كل من يحمل دور COURIER يرى كل الطلبات وأرقام أصحابها
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/api/src/modules/courier.module.ts:18-24 `tasks()` تستعلم `where: { status: { in: [...] } }` بلا أي قيد على المندوب، وتُعيد `phone` و`altPhone` كاملين. docs/10-admin.md:26 يشترط فرض `own` «داخل استعلام Prisma نفسه لا في الواجهة». ولا شاشة إسناد في اللوحة: `grep -n "suggest\|assign" apps/admin/src/App.tsx` = صفر.`
- **الأثر**: مندوبٌ في اللاذقية يرى عناوين وأرقام زبائن دمشق، وقائمة «مهام اليوم» تصير قائمة الطلبات كلها فلا تصلح للعمل. ومسار الاقتراح `/admin/delivery/orders/:orderNo/suggest` قائم في الخادم ولا يستدعيه أحد.

### لا مسار /app/orders/:id/tracking — والموجّه يرفض أي مسار من أربعة مقاطع أصلاً
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/app/src/lib/router.tsx:26 `if (a.length !== b.length) return null;` والمطابقة الوحيدة `match(path, '/app/orders/:orderNo')` في App.tsx:1073. شاشة 11 في docs/05-mobile-ux.md:108 تطلب خطاً زمنياً وبطاقة مكتب النقل و`waybill_no` بزر نسخ وصورة الإيصال.`
- **الأثر**: «أين طلبي؟» هو أكثر سبب تواصل في الدعم (وهو أول خيار في قائمة REASONS بـ App.tsx:974). بلا شاشة تتبّع، كل زبون ينتظر يتحوّل إلى تذكرة أو مكالمة.

### أي مسار مجهول تحت /app يعرض السلة بلا 404، وأي مسار إداري مجهول يعرض لوحة المؤشرات
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/app/src/App.tsx:1095-1100 سلسلة شرطية تنتهي بـ `: <CartPage nav={nav} />`؛ apps/admin/src/App.tsx:2445-2462 تنتهي بـ `: <Dashboard />`؛ وapps/api/src/worker.ts:53-67 يردّ 200 لكل `/app/*` و`/admin/*`.`
- **الأثر**: رابط مكسور أو مشارَك أو محفوظ (مثل /app/wishlist المذكور في الوثيقة) يفتح شاشة أخرى بحالة 200 بدل أن يقول «غير موجودة» — فيظن الزبون أن حسابه ضاع، ولا يظهر العطل في أي مراقبة لأنه ليس خطأً تقنياً.

### لا صفحة 404 عربية إطلاقاً رغم أن الإعداد يطلبها
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `wrangler.jsonc:45 `"not_found_handling": "404-page"`؛ و`ls dist-worker/404.html` → لا يوجد؛ ولا `apps/site/src/pages/404.astro`.`
- **الأثر**: كل رابط قديم أو مطبعي يعطي صفحة Cloudflare الافتراضية بالإنجليزية بلا شريط تنقّل ولا بحث ولا هوية — في متجر عربي كامل هذا يقرأه الزائر كأن الموقع تعطّل، فيغادر بدل أن يبحث.

### أيقونات PWA مفقودة فيسقط تثبيت المتجر كله
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/site/public/manifest.webmanifest يشير إلى `/icons/192.png` و`/icons/512.png`؛ و`ls dist-worker/icons` → غير موجود. المحتوى الوحيد في apps/site/public هو manifest.webmanifest وog.png.`
- **الأثر**: المتصفح يرفض معايير التثبيت عند فشل جلب الأيقونات، فلا يظهر `beforeinstallprompt` ولا خيار «إضافة إلى الشاشة الرئيسية» — ويسقط معه كل ما بُني عليه في القسم 5.8.

### لا مدخل للمرتجعات ولا للمراجعات: المكوّنان معرَّفان ولا يُستعملان في أي مكان
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/app/src/App.tsx:859 `function ReturnBox` و:921 `function ReviewBox`؛ و`grep -n "<ReturnBox\|<ReviewBox" apps/app/src` = صفر. OrderPage (App.tsx:261-326) لا تعرضهما.`
- **الأثر**: سياسة الإرجاع سبعة أيام منشورة في /legal/return-policy/ وفي شاشة الدعم، والمسار `/returns` جاهز في الخادم — لكن الزبون لا يجد زراً واحداً لاستعماله، فيتحوّل كل إرجاع إلى مكالمة، وتبقى صفحات المنتج بلا مراجعات جديدة إلى الأبد.

### إتمام الطلب بلا حفظ مسودة وبلا اختيار من العناوين المحفوظة
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/app/src/App.tsx:149-258 `CheckoutPage` بحالة React فقط، لا `localStorage` ولا مؤقّت؛ ولا استدعاء لـ`/me/addresses` (المتوفّر في routes.ts:204) داخلها. docs/05-mobile-ux.md:15 يشترط حفظ المسودة كل 800ms واستعادتها بعد إعادة التشغيل، و:106 يذكرها في الشاشة نفسها.`
- **الأثر**: انقطاع التيار في منتصف تعبئة العنوان — وهو الحالة الطبيعية لا الاستثناء — يمحو النموذج كاملاً. والعميل العائد يُجبَر على كتابة عنوانه من جديد رغم أنه محفوظ عندنا، وهو ما يمنعه معيار 3.3.7.

### لا حالة «غير متصل» ولا طابور إرسال في تطبيق الزبون إطلاقاً
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `لا مجلد `apps/app/src/offline/`؛ apps/app/src/main.tsx (9 أسطر) بلا تسجيل عامل خدمة؛ و`grep -rn "navigator.onLine\|outbox" apps/app/src` = صفر. docs/05-mobile-ux.md:506-518 يوجب طابور IndexedDB بمفتاح تفرّد وشارة «قيد الإرسال (n)»، و:385 يوجب نص «أنت غير متصل — سنرسل طلبك تلقائياً عند عودة الشبكة».`
- **الأثر**: الطابور مُنفَّذ لواجهة المندوب وحدها (apps/admin/src/lib/offline.ts). أما الزبون على شبكة متقطعة فيضغط «أكّد الطلب» فيرى خطأً ويعيد الضغط — أو يفقد الطلب كلياً، وهو ما بُني كل القسم 5.8 لمنعه.

### لا شاشة شحنات في اللوحة: waybill_no وصورة الإيصال ومكاتب النقل بلا واجهة ولا API
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `تبويبات اللوحة في apps/admin/src/App.tsx:2412-2420 لا تتضمن الشحنات؛ و`grep -n "waybill" apps/api/src apps/admin/src` = صفر. docs/10-admin.md:73 يوجب الوحدة، و:156 «تحديث حالات الشحن يدوي من /shipments»، وdocs/05-mobile-ux.md:108 يعرض `waybill_no` للزبون.`
- **الأثر**: التوصيل بين المحافظات كله (مكاتب النقل، البوليصة الورقية، صورة الإيصال) بلا أي أثر في النظام. الطلب خارج دمشق ينتقل إلى SHIPPED بلا رقم بوليصة، فلا يستطيع الدعم ولا الزبون تتبّعه، ولا تُطابَق مستحقات المكتب.

### لا شاشة مخزون في اللوحة: on_hand/reserved/incoming وIMEI ووحدات الأجهزة
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `لا تبويب مخزون في apps/admin/src/App.tsx:2412-2420؛ والمسار الوحيد `POST /admin/inventory/sweep` (apps/api/src/http/routes.ts:245). docs/10-admin.md:76 يوجب الوحدة بكل حقولها، و:125 يشترط مسح IMEI قبل إغلاق بند التجهيز.`
- **الأثر**: لا سبيل لرؤية كم قطعة على الرفّ ولا كم محجوزاً، ولا لتسوية جرد، ولا لربط IMEI ببند طلب. وفحص IMEI المعروض للزبون في صفحة المنتج (p/[slug].astro:142) نصّ ثابت لا يسنده سجل.

### صف التأكيد الهاتفي بلا زر واتساب ولا تصحيح عنوان ولا ملاحظات مكالمة
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/admin/src/App.tsx:137-152 يرسل `{ outcome }` وحده؛ لا رابط `wa.me` ولا `tel:` ولا حقل ملاحظة ولا تحرير عنوان في الصف. docs/10-admin.md:104 يوجب زر واتساب، و:103 تصحيح العنوان داخل الصف قبل PROCESSING، و:110-117 يبيّن الحمولة بـ`confirmation_notes` و`address_patch`.`
- **الأثر**: مكالمة التأكيد هي «البوابة الوحيدة» للانتقال إلى التجهيز وأرخص ضابط ضد رفض الاستلام (10.3). موظف لا يستطيع تصحيح عنواناً خاطئاً وهو يسمعه من الزبون سيجهّز طلباً إلى عنوان يعرف أنه خطأ.


## متوسطة (79)

### إعداد وحدة التقريب النقدي مبذور بقيمة الليرة القديمة (1000) بينما الشيفرة تقرّب إلى 10 — والإعداد أصلاً لا يُقرأ
- **الحال**: دَين · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/api/prisma/seed-d1.mjs:172 `['cash_rounding_step_syp', 1000]` مقابل apps/api/src/common/money.ts:3 `export const CASH_STEP = 10;` وpackages/ui/src/money.ts:15 `CASH_ROUNDING_STEP = 10`. وترحيل apps/api/migrations/0002_new_pound.sql عالج قيد CHECK وسعر الصرف ولم يمسّ هذا الصفّ.`
- **الأثر**: اللوحة تعرض لصاحب المتجر «وحدة التقريب النقدي: 1000» — رقمٌ خاطئ بمئة ضعف يبني عليه قراراً في التسعير. وحين يُوصَل الإعداد بالشيفرة لاحقاً ستنقلب كل الأسعار إن بقيت القيمة.

### أيقونات التطبيق المُعلَنة في manifest غير موجودة، فيفشل التثبيت على الشاشة الرئيسية
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/site/public/manifest.webmanifest يشير إلى `/icons/192.png` و`/icons/512.png`. و`ls apps/site/public/` يعطي manifest.webmanifest وog.png فقط، و`ls dist-worker/icons` → No such file or directory.`
- **الأثر**: لا تثبيت على الشاشة الرئيسية. وإشعارات المتصفح على iOS مشروطة بهذا التثبيت تحديداً (كما يقول تعليق apps/api/src/modules/push.module.ts:8-11) — فيسقط نصف جمهور الإشعارات بملفَّي صورة ناقصين.

### إشعارات المتصفح مبنيّة بالكامل ولا تُرسَل أبداً: PushService.sendToPhone بلا مستدعٍ، ولا واجهة تشترك
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: ``grep -rn "sendToPhone" apps/api/src` يعطي التعريف وحده (apps/api/src/modules/push.module.ts:114). و`grep -rn "/push" apps/app/src apps/admin/src` فارغ — لا واجهة تنادي `/push/subscribe`. وapps/api/src/modules/notifications.service.ts لا يستورد PushService أصلاً.`
- **الأثر**: ١٦٥ سطراً من توقيع VAPID مكتوبة بلا مكتبة، وجدول PushSubscription في المخطَّط، ومساران في الموجّه — كلّها معلَّقة في الهواء. وأرخص قناة تنبيه لدى المتجر معطَّلة بينما الأغلى (واتساب) بلا مفتاح.

### قنوات IN_APP وWEB_PUSH وEMAIL معلَنة في موجّه الإشعارات ولا تُنفَّذ، فإشعارات P2 وP3 لا تصل أحداً
- **الحال**: جزئي · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/api/src/modules/notifications.service.ts:26-31 `channelsFor` تُرجع لـP2 `['IN_APP','WEB_PUSH']` ولـP3 `['WEB_PUSH']`. وجسم `send` (:60-80) لا يعالج إلا WHATSAPP وSMS، فتبقى `delivered` فارغة ويُسجَّل نجاحٌ صوريّ. ولا نموذج للإشعارات داخل التطبيق: `grep -n -i notification apps/api/prisma/schema.prisma` لا يعطي جدولاً.`
- **الأثر**: الموجّه يعلن سياسة توجيهٍ من الفصل 20 وينفّذ ثلثها، ويكتب في السجلّ ما يوحي بأن الرسالة خرجت. الأعطال التي تُخفيها السجلّات هي أطول الأعطال عمراً.

### بوابة الإقلاع ضدّ المنتجات التجريبية شيفرةٌ ميتة: لا تُستدعى، وتبني عميل Prisma بلا محوّل D1، وتحيل إلى سكربت غير موجود
- **الحال**: دَين · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/api/src/common/boot-guard.ts:7 `assertNoPublishedDemoInProd` — مستدعيها الوحيد apps/api/dist/main.js:17 وهو مخلَّف بناءٍ لعهد NestJS (لا src/main.ts في الشجرة). و:10 `new PrismaClient()` بلا محوّل يفشل داخل Worker. و:16 يوجّه إلى `pnpm db:purge-demo` — غير موجود في package.json.`
- **الأثر**: الحارس الذي وُضع ليمنع بيع بضاعةٍ وهمية غير موصولٍ بشيء. والوثيقة تعده من بنود الجاهزية (docs/07-catalog.md:345)، فيُظنّ محميّاً وهو ليس كذلك.

### مسارات bootstrap ما زالت مفتوحة على الإنتاج مع كامل المخطَّط والبذرة محزومَين في نصّ الـWorker
- **الحال**: دَين · **ينفّذه**: المالك · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/api/src/worker.ts:141-165 يعرّف `POST /api/v1/bootstrap/admin-password` و`GET|POST /api/v1/bootstrap` قبل طبقة المصادقة والحدود. وapps/api/src/generated/bootstrap-sql.ts حجمه 104 كيلوبايت يُشحن مع كل نشرة. وتعليق apps/api/src/modules/bootstrap.module.ts:23 يقول بنفسه: «ويُحذف حين يملك مفتاح النشر صلاحية D1».`
- **الأثر**: معروف مسبقاً، والباقي أنه معلَّقٌ على قرارِ صلاحيةٍ لم يُتَّخذ بعد. وكل يوم يمرّ يزيد احتمال أن يُنسى فيبقى مسارٌ يُهيّئ قواعد بيانات مفتوحاً على متجرٍ يعمل.

### عنوان المحل ما زال فارغاً في البيانات المنظَّمة، ولا ساعات عمل ولا إحداثيات
- **الحال**: بيد المالك · **ينفّذه**: المالك · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/site/src/lib/seo.ts:24 `streetAddress: null as string | null` و:38 `if (SITE.streetAddress) addr.streetAddress = SITE.streetAddress;` — فالعنوان يُسقَط من `PostalAddress`. ولا `openingHoursSpecification` ولا `geo` في organizationLd (:34-67).`
- **الأثر**: معروف مسبقاً. والأثر أن المتجر لا يظهر في «قربي» ولا في خرائط البحث المحلي — وهو أرخص مصدر زبائن لمتجرٍ مادّيّ في دمشق.

### منع تكرار الإشعارات وقاطع دائرة واتساب في ذاكرة العزلة، وعزلات الـWorker كثيرة وقصيرة العمر
- **الحال**: دَين · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/api/src/modules/notifications.service.ts:18 `private sent = new Map<string, number>()` و:34 `private waFailures: number[] = []`. والمشروع يعرف هذه القاعدة ويطبّقها في مكانٍ آخر: apps/api/src/common/rate-limit.ts:8-12 «العدّاد في KV لا في ذاكرة العملية… فالمخزن المشترك هناك شرطُ صحّةٍ لا تحسين».`
- **الأثر**: منعُ التكرار لا يمنع شيئاً عملياً: كل عزلة جديدة تبدأ بخريطة فارغة فيُرسَل الإشعار نفسه مراراً على حساب المتجر. وقاطع الدائرة لن ينفتح أبداً لأن عدّ الإخفاقات يُصفَّر قبل بلوغ الخمسة.

### النشر معلَّق على فرع عملٍ مؤقّت لا على فرعٍ رئيسي
- **الحال**: دَين · **ينفّذه**: المالك · **✔ مُدقَّق** · *code*
- **الدليل**: `.github/workflows/deploy.yml:7 `branches: [claude/website-remaining-plans-cjlncb]` — وهو الفرع النشط فعلاً (`git rev-parse --abbrev-ref HEAD`)، ولا فرع `main` في `git branch -a`.`
- **الأثر**: يعمل اليوم ويتوقّف صامتاً في أول يوم يُعمَل فيه على فرعٍ آخر أو يُدمَج فيه هذا الفرع. ونشرٌ لا يقع بلا خطأٍ يُعلَن هو أسوأ أشكال العطل.

### مسارَا bootstrap يعملان خارج طبقة حدود المعدل، فمسار كلمة سرّ المدير الأولى بلا سقفِ محاولات
- **الحال**: دَين · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/api/src/worker.ts:141 و:152 يعالجان الطلب ويعودان بالردّ قبل السطر :171 `useCloudflareKv(env.KV); const { app } = api(env);` — والحدّ يُركَّب داخل تطبيق Hono وحده: apps/api/src/http/app.ts:125 `app.use('/api/v1/*', rateLimit(env))`.`
- **الأثر**: الحارس المعلن في bootstrap.module.ts:88 هو أن الحساب بلا كلمة سرّ بعد؛ صحيحٌ منطقياً، لكن النافذة بين نشر الـWorker وأول ضبطٍ للكلمة مفتوحة بلا عدّاد محاولات ولا تسجيل — وهي المسار الوحيد إلى صلاحية ADMIN كاملة.

### خمس وعشرون مهمة دورية معرَّفة في الشيفرة بلا منفِّذ واحد
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `apps/api/src/modules/readiness.module.ts:58-84 يعرّف ROUTINES (عشر يومية، ثماني أسبوعية، سبع شهرية)، بينما apps/api/src/worker.ts:217-247 ينفّذ ثلاثاً فقط: الكنّاس والتنبيهات والحذف المستحق. والمسجِّل الوحيد يدوي: routes.ts:433. واستعلام الإنتاج: SELECT count(*) FROM ops_routine_runs ⇒ 0.`
- **الأثر**: جدول ops_routine_runs يعد بدفتر يقول من فعل ماذا ومتى، وحقل isAutomated لا يُضبط true من أي مكان. التسوية النقدية اليومية ومراجعة الأسعار الأسبوعية تبقى ذاكرةَ شخصٍ لا صفّاً في قاعدة — وهو بالضبط ما بُني الجدول ليمنعه.

### بنود الجاهزية الاثنان والثلاثون غير مبذورة في قاعدة الإنتاج، فلوحة الجاهزية فارغة
- **الحال**: بيد المالك · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `استعلام الإنتاج: SELECT count(*) FROM readiness_checks ⇒ 0. والبذر لا يجري إلا بضغطة زر في apps/admin/src/App.tsx:2168 على المسار routes.ts:429 — ولا يُستدعى من البذرة ولا من التهيئة.`
- **الأثر**: apps/api/src/modules/readiness.module.ts:172 يحسب launch_ready بشرط `blocking.length > 0` — ومع صفر صفوف تبقى النتيجة false بلا قائمة أخطاء تُقرأ. البوابة التي بُنيت لتمنع إطلاقاً غير جاهز لا تمنع ولا تُخبر: تصمت.

### بوابة الإقلاع ضد البضاعة التجريبية شيفرة ميتة، والأمر الذي تحيل إليه غير موجود
- **الحال**: دَين · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `apps/api/src/common/boot-guard.ts:7 — `assertNoPublishedDemoInProd` لا يُستدعى من أي ملف (grep على apps كلها يعيد التعريف وحده)، وهو يبني `new PrismaClient()` بلا محوّل D1 فلا يعمل داخل Worker أصلاً. والسطر 16 يحيل إلى `pnpm db:purge-demo` غير المعرَّف في أي package.json. واستعلام الإنتاج: 22 منتجاً is_demo=1 status='PUBLISHED'.`
- **الأثر**: البوابة التي وُضعت لتمنع «متجراً يعرض بضاعة لا وجود لها ويستقبل عليها طلبات» (نصّها في الملف) غير موصولة بشيء، ومخرج الطوارئ الذي ترشد إليه لا يوجد. الخروج من وضع العرض التجريبي بلا مسار مكتوب واحد.

### النشر من فرع ميزة، بلا فحص بعد النشر ولا مسار تراجع
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `.github/workflows/deploy.yml:7 — `branches: [claude/website-remaining-plans-cjlncb]`. والخطوة الأخيرة (السطر 64-68) هي wrangler deploy وينتهي الملف؛ لا استدعاء لـ/api/v1/health بعدها، ولا wrangler versions/rollback، ولا نشر تدريجي. وconcurrency في السطر 10-12 يلغي نشرةً جارية.`
- **الأثر**: نشرةٌ تكسر الموقع تبقى حيّةً حتى يلاحظها إنسان، والعودة تحتاج معرفةً يدوية بمعرّف النسخة السابقة (5db74ea5-… في سجل آخر نشرة). ودفعةٌ خاطئة إلى فرعٍ اسمه مؤقّت تصل إلى talisham.com مباشرةً بلا مراجعة.

### سير التحقق لا يشغّل أي فحص من طرف إلى طرف ولا ترحيلاً ولا بذراً
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `.github/workflows/ci.yml:24-26 — typecheck و test و build فقط. والفحص الوحيد الذي يلمس قاعدةً وWorker حقيقيَّين محبوس في maintenance.yml:50-65، وهو السير الذي لم يعمل قط.`
- **الأثر**: scripts/e2e.sh يفحص 45 حالة على الدورة الكاملة — المخزون والتسوية والتحصيل — ولا يعمل على أي دفعة. عطلٌ في تسوية نقدية يمرّ إلى الإنتاج ويُكتشف يوم يُغلق الصندوق.

### تدقيق الاعتماديات مُقنَّع: ثغرة عالية لا توقف شيئاً
- **الحال**: دَين · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `.github/workflows/maintenance.yml:79 — `pnpm audit --audit-level high || echo "::warning::تدقيق الاعتماديات وجد ملاحظات"`.`
- **الأثر**: تحذيرٌ في سجلٍّ لا يقرؤه أحد ليس اكتشافاً. والسير نفسه يفتح مسألة عند الفشل (السطر 82) — وهذا البند وحده استُثني من ذلك المسار، فثغرةٌ حرجة في اعتماد تمرّ بلا أثر يُقرأ.

### لا فحص انحراف بين schema.prisma والترحيلات — توليد الترحيل يدوي بالكامل
- **الحال**: دَين · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `apps/api/package.json:9 — `migration:new` هو الطريق الوحيد لتوليد ترحيل، ويُستدعى يدوياً. ولا استخدام لـ`prisma migrate diff --exit-code` في .github/workflows ولا في scripts/. و apps/api/prisma/apply-constraints.mjs (الذي يضيف 62 قيد CHECK ومحفِّزات prisma/sql/constraints.sql) لا يعمل إلا من ذلك الأمر اليدوي.`
- **الأثر**: تعديلٌ على schema.prisma بلا ترحيل مقابل يمرّ عبر typecheck وtest وbuild كلها خضراء، ثم يفشل عند أول طلب في الإنتاج. والقيود التي تحمي المال — قيد التقريب النقدي مثلاً — تُضاف بنصٍّ يدوي، فنسيانها لا يُكتشف بشيء.

### النطاقات المخصّصة معطَّلة في الإعداد بينما الوثيقة تقول إنها مضبوطة فيه
- **الحال**: محجوب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `wrangler.jsonc:78-82 قسم routes معلَّق بالكامل بانتظار صلاحية Workers Routes: Edit. و docs/DEPLOY.md في قسم النطاقات يقول: «مضبوطة في wrangler.jsonc كنطاقات مخصّصة على الـWorker نفسه».`
- **الأثر**: ربط النطاقات يعيش الآن في لوحة Cloudflare وحدها ولا أثر له في الشيفرة. من يعيد إنشاء الـWorker أو ينشره في حساب آخر يحصل على موقع بلا نطاق ولا يعرف السبب — والوثيقة تكذّب الملف فتضلّل من يبحث.

### مفتاح النشر بلا صلاحية Workers Routes: Edit — النطاقات مربوطة يدوياً من اللوحة
- **الحال**: محجوب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `wrangler.jsonc: كتلة routes معلَّقة مع تعليل code: 10000 وصلاحية Workers Routes: Edit`
- **الأثر**: ربط النطاق يبقى خارج الشيفرة، فإعادة إنشاء الـWorker أو نقله لا تعيد النطاقات معها ولا أثر لذلك في المستودع.

### مهمة تحديث سعر الصرف مسجَّلة أسبوعية بينما صلاحيته يومية
- **الحال**: دَين · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/src/modules/readiness.module.ts:74 WEEKLY_FX_UPDATE: 'WEEKLY' مقابل seed/catalog.demo.json:19 valid_hours: 24 وdocs/13-syria-reality.md:13.5 بند 14 «تحديث سعر الصرف يومياً»`
- **الأثر**: قائمة المهام تطمئن المالك أسبوعياً بينما المتجر يتوقف يومياً — الدفتر يكذّب الواقع.

### المحافظات المخدومة سبع من أربع عشرة — قرار غير محسوم
- **الحال**: بيد المالك · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/prisma/seed-d1.mjs:179 (سبع محافظات) مقابل apps/api/prisma/enums.json Governorate: أربع عشرة قيمة · يُضبط من: /admin/settings مفتاح served_governorates (admin.module.ts:246) · الرفض: orders.module.ts:46 GOVERNORATE_NOT_SERVED`
- **الأثر**: كل محافظة خارج القائمة تعني زبوناً يملأ عنوانه ثم يُرفض طلبه؛ والقائمة الحالية موروثة من البذرة لا من قرار.

### إعداد «شحن مجاني فوق» موجود في اللوحة ولا يقرؤه أحد
- **الحال**: دَين · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `free_shipping_above_usd_cents مُعرَّف في apps/api/src/modules/admin.module.ts:244 ومعروض في apps/admin/src/App.tsx:1075، وغير مقروء في orders.module.ts (الشحن يُصفَّر بالكوبون وحده: orders.module.ts:135)`
- **الأثر**: المالك يضبط الرقم ويظنّه نافذاً، والزبون يُحاسَب على الشحن فوق الحد — وعدٌ في اللوحة بلا شيفرة خلفه.

### أمر حذف البيانات التجريبية مذكور في ثلاثة مواضع وغير موجود
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `package.json: لا سطر db:purge-demo · مذكور في apps/api/src/common/boot-guard.ts:16 وseed/README.md:54 وdocs/07-catalog.md:345`
- **الأثر**: المالك يُوجَّه إلى أمر يفشل، فيبقى اثنان وعشرون منتجاً وهمياً في قاعدة الإنتاج بلا طريق لحذفها دفعةً.

### عنوان المحل فارغ في البيانات المنظَّمة
- **الحال**: بيد المالك · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/site/src/lib/seo.ts:24 `streetAddress: null as string | null` والتعليق أعلاه: «وعنوانُ المحل هو الفارغ الوحيد الباقي» · يُستهلك في organizationLd:40`
- **الأثر**: لا مصدر لعنوان محلٍّ إلا صاحبه؛ وبلا عنوان لا يُقبل المتجر في Google Business Profile ولا يظهر في نتائج «محل جوالات قريب مني».

### ساعات العمل مكتوبة في ثلاثة مواضع منفصلة بلا مصدر واحد ولا تصريح schema.org
- **الحال**: جزئي · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/src/modules/tickets.module.ts:32 (السبت–الخميس 10:00–20:00) · apps/site/src/pages/help/index.astro:33 · apps/app/src/App.tsx:1007-1008 · لا openingHours في apps/site/src/lib/seo.ts · docs/18-content-settings.md:69 يطلب إعداد business_hours وجدول holidays — وكلاهما غير منفَّذ`
- **الأثر**: البند OPS_SUPPORT_HOURS يطلب ساعات معلنة ومغطّاة بموظف؛ والمالك وحده يعرف دوامه الفعلي وعطله الرسمية، ثم يلزم توحيدها في مصدر واحد وإلا افترقت المواضع الثلاثة.

### الرقم المعلن وواتساب المتجر يحتاجان تأكيداً من المالك
- **الحال**: بيد المالك · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/site/src/lib/seo.ts:26-27 telephone '+963993223887' وwhatsapp '963993223887' — يُنشران في organizationLd:58-64 وفي رابط wa.me:66`
- **الأثر**: رقمٌ منشور في البيانات المنظَّمة وفي كل صفحة يتلقى مكالمات الزبائن؛ خطؤه يعني زبائن يتصلون بغير المتجر ولا يُكتشف إلا متأخراً.

### لا صفحة «من نحن» ولا «تواصل معنا» على الموقع
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `ls apps/site/src/pages/ → b, blog, c, compare, help, index, legal, p, prices, robots, search, sitemap فقط · docs/18-content-settings.md:58-59 يطلبهما بحقول branches وsupport_phone وwhatsapp_official وbusiness_hours`
- **الأثر**: في سوقٍ حاجزُه الأول الخوف من النصب، غيابُ صفحة تقول من نحن وأين نحن وكيف تصلنا هو أكبر ما يُنقص الثقة — والبيانات كلها عند المالك.

### أيقونتا التطبيق مفقودتان — البيان يشير إلى ملفين غير موجودين
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/site/public/manifest.webmanifest:13-22 يشير إلى /icons/192.png و/icons/512.png · ls apps/site/public/ → manifest.webmanifest وog.png فقط · حيّاً: curl -o /dev/null -w "%{http_code}" https://talisham.com/icons/192.png → 404`
- **الأثر**: «أضِف إلى الشاشة الرئيسية» تُنتج أيقونة فارغة على هاتف الزبون؛ والشعار قرار علامة يخصّ المالك لا يُخترع.

### مفاتيح VAPID غير مضبوطة — زرّ إشعارات المتصفح يعمل ولا يصل شيء
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/src/modules/push.module.ts:16-20 (pushConfigured يعود false بلا المفتاحين) · .env.example:24-25 السطران معلَّقان · تُضبط بـ npx wrangler secret put VAPID_PRIVATE_KEY (docs/DEPLOY.md)`
- **الأثر**: الاشتراك يُسجَّل ويُخزَّن ولا يُرسَل عليه شيء، فيظن الزبون أنه سيُنبَّه عند توفّر الجهاز ولا يُنبَّه — والضبط يحتاج مفتاح Cloudflare الذي بيد المالك.

### سقف الطلب الواحد ألف وخمسمئة دولار — رقم بذرة لا قرار
- **الحال**: بيد المالك · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/prisma/seed-d1.mjs:170 ['cod_max_order_usd_cents', 150000] · يُطبَّق في orders.module.ts:139-140 (Errors.codLimit) · يُضبط من /admin/settings`
- **الأثر**: في الدفع عند الاستلام السقف هو حدّ المخاطرة النقدية على المندوب والمتجر معاً؛ رقمٌ موروث من ملف بذرة لا يعبّر عن قدرة المتجر على تحمّل رفض استلام.

### لا موردون ولا كوبونات ولا حزم في القاعدة
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/prisma/seed-d1.mjs لا يبذر suppliers ولا coupons: grep -n "coupon" seed-d1.mjs → لا نتيجة · تُنشأ من /admin/procurement (procurement.module.ts:57 createSupplier) و/admin/coupons (App.tsx:704)`
- **الأثر**: أمر الشراء يُرفض بلا مورد مسجَّل (procurement.module.ts:99)، فلا يدخل جهازٌ واحد إلى المخزون بإيصال استلام فعلي — وهو الشرط الوحيد لتحويل منتج تجريبي إلى حقيقي.

### نافذة تثبيت السعر ومهل الحجز ثوابت في الشيفرة وإعداداتها في اللوحة ميتة
- **الحال**: دَين · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/src/modules/orders.module.ts:15 CONFIRM_WINDOW_HOURS = 48 و cart.module.ts:9 SOFT_HOLD_MINUTES = 15 · المفاتيح confirmation_window_hours وorder_hold_hours وorder_hold_ext_hours وrare_stock_threshold وrare_hold_ext_hours مقبولة في admin.module.ts:239-243 ولا يقرؤها أحد خارجه`
- **الأثر**: خمسة مقابض في لوحة المالك تُحفَظ في سجل التدقيق ولا تُغيّر سلوكاً — وهو أسوأ من غيابها لأنه يوهم بالتحكم.

### لا تحقّق ملكية في Google Search Console ولا ملفّ Google Business Profile
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/site/src/layouts/Base.astro:44-69 لا وسم تحقق (google-site-verification) ولا أي وسم مالك · لا ذكر لهما في docs · robots.txt يُعلن sitemap صحيحاً (apps/site/src/pages/robots.txt.ts:19)`
- **الأثر**: بلا Search Console لا يُعرَف ما فُهرس ولا تصل تحذيرات غوغل؛ وبلا Business Profile لا يظهر المحل في الخرائط ولا في البحث المحلي — وكلاهما يُنشأ من حساب المالك ويحتاج تحقق عنوان وهاتف.

### سقف النقد لكل مندوب وصندوق الاسترداد غير محدَّدين
- **الحال**: بيد المالك · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/src/modules/readiness.module.ts:58 FIN_COURIER_CASH_CAP و:59 FIN_REFUND_FLOAT (بندان مانعان) · الحقل موجود في اللوحة: apps/admin/src/App.tsx:828 cashCapUsdCents وdepositUsdCents، وبلا قيمة مقرَّرة`
- **الأثر**: مندوبٌ يحمل نقداً بلا سقف خطرٌ عليه وعلى المتجر؛ وبلا صندوق استرداد مخصَّص يُصرَف من إيراد اليوم فيختلّ الدفتر في أول أسبوع إرجاعات.

### منع تكرار الإشعارات وقاطع دائرة واتساب في ذاكرة العملية داخل Worker
- **الحال**: جزئي · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/api/src/modules/notifications.service.ts:20 (private sent = new Map()) و 34 (private waFailures: number[] = [])`
- **الأثر**: عزلات الـWorker تُهدَم وتُنشأ بلا إشعار، فمفتاح منع التكرار عمره عزلة لا 24 ساعة. النتيجة رسائل مكرَّرة للزبون، وقاطع دائرة لا يقطع شيئاً — وهو العطل نفسه الذي عُولج بالفعل في حدود المعدل بنقلها إلى KV.

### لا جدول notifications ولا مركز إشعارات /app/notifications
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `grep -in 'notification' apps/api/prisma/schema.prisma → لا model Notification (51 نموذجاً، آخرها OpsRoutineRun:1204) ؛ docs/20-notifications.md:78 يشترط الجدول بحقول type/level/channel[]/read_at/dedupe_key واحتفاظ 90 يوماً`
- **الأثر**: ما يُرسَل لا يُكتب في أي مكان، فلا سجل يُحتجّ به حين ينكر زبون وصول إشعار، ولا شارة غير مقروء، ولا أساس لـdedupe_key الذي يحل مشكلة التكرار أعلاه.

### تنبيه سعر الصرف المبكر لا يصل أحداً، ولا مهمة ساعيّة تفحص طزاجته
- **الحال**: جزئي · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/api/src/modules/fx.module.ts:19-21 تحسب health='EXPIRING' ولا ترسل شيئاً؛ لا استدعاء notify في الوحدة كلها ؛ docs/03-data-model.md:222 يشترط إشعاراً للمدير ولافتة في اللوحة قبل أربع ساعات`
- **الأثر**: التدرّج مكتوب في الشيفرة (FRESH→EXPIRING→STALE_MARGIN→STALE_HALT) لكن الحلقة الأولى مقطوعة: أحدٌ يجب أن يُخبَر قبل أن يتوقف البيع. وبلا مهمة ساعيّة يقع الاكتشاف حين يشتكي زبون.

### تعطيل طبقة الزجاج التكيّفي وهمي: المفتاح data-glass لا يضعه أحد ولا فحص لقدرات الجهاز
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `packages/ui/src/tokens.css:173 و packages/ui/src/base.css:356 يستعملان :root:not([data-glass="off"]) ؛ grep -rn 'data-glass|deviceMemory|hardwareConcurrency|effectiveType|saveData' apps/ packages/ → المطابقتان الوحيدتان هما تعريفا الـCSS نفسهما`
- **الأثر**: docs/13-syria-reality.md:69 يقلب المنطق صراحةً: التعطيل افتراضي والتفعيل استثناء بعد فحص القدرات. الواقع عكسه تماماً — الضبابية مفعّلة دائماً على أندرويد 11-12 إلا إن أعلن المتصفح prefers-reduced-transparency، وهو ما لا تفعله أغلب أجهزة السوق. وبند TECH_GLASS_FALLBACK مانع للإطلاق.

### لا مسار اقتراحات بحث ولا تسامح إملائي، والصفحة تعد بالاثنين في وصفها
- **الحال**: جزئي · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `لا GET /search/suggest في apps/api/src/http/routes.ts (البحث في السطور 67-75 فقط) ؛ apps/api/src/common/arabic.ts:49-56 expandQuery مرادفات وتطبيع بلا مسافة تحرير ؛ apps/site/src/pages/search.astro:37 description="… مع تسامح إملائي" ؛ docs/06-search.md:192 يشترط /search/suggest بتأخير 150ms وحدّ 8`
- **الأثر**: «سامسونح» لن تطابق «سامسونج» — وهو المثال المكتوب في الفصل 6 نفسه. ولا جزيرة SearchBar موجودة في apps/site/src/components رغم ذكرها في 19.1. والوصف المنشور يعد بما لا يقع.

### حدود المعدل ناقصة عن جدول الفصل 9.11، والنطاق «مستخدم» معرَّف ولا يُستعمل قطّ
- **الحال**: جزئي · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/api/src/common/rate-limit.ts:15-26 — أحد عشر قاعدة، وليس فيها /support/tickets ولا /support/tickets/{n}/messages ولا /me/warranty-claims ولا questions ولا /me/alerts ولا /admin/catalog/imports ولا فصل collect (120/س) عن status (300/س) ؛ النوع Rule يعرّف by:'user' في السطر 6 ولا قاعدة تستعمله`
- **الأثر**: الفصل 9 يقول إن كل مسار غير مذكور يرث الحد العام 60/دقيقة/IP «وهو حد غير مقصود لأي عملية حساسة». فتح خمسمئة تذكرة أو مطالبة كفالة في الساعة يمرّ اليوم بلا اعتراض، وحدّ الـIP بلا معنى خلف مزوّد يشارك عناوينه.

### نموذج الصلاحيات دورٌ فقط: لا صلاحيات مورد:فعل:نطاق ولا تقنيع بيانات للدعم
- **الحال**: جزئي · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/api/src/http/auth.mw.ts:60-70 — protect(prisma, ...roles) تقارن الدور فقط؛ لا حقل perms في Claims (apps/api/src/common/jwt.ts:8-17) ؛ مقابل مصفوفة 19.3 التي تعرّف عشرين صلاحية بنطاقات own/any و«ج (مقنّع)» للدعم`
- **الأثر**: المصفوفة المكتوبة لا يمكن تطبيقها بالأدوار وحدها: «الدعم يرى بيانات العملاء مقنّعة» و«المندوب يرى طلبه فقط» فروقٌ داخل الدور الواحد. عملياً كل من دوره SUPPORT يرى كل شيء بلا تقنيع.

### بوابة منع المنتجات التجريبية في الإنتاج شيفرة ميتة لا تُستدعى، وتبني عميلاً لا يعمل على D1
- **الحال**: دَين · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/api/src/common/boot-guard.ts:8 assertNoPublishedDemoInProd ؛ grep -rn 'boot-guard|assertNoPublishedDemo' apps/api/src/worker.ts apps/api/src/http/ → صفر استيراد ؛ والملف ينشئ new PrismaClient() مباشرةً وهو ما لا يعمل على ربط D1`
- **الأثر**: docs/07-catalog.md:345 يجعل «صفر منتجات تجريبية منشورة» بنداً من فحص الجاهزية، والفصل 7 §7.10 يجعل الفشل عند الإقلاع هو الحارس. الحارس موجود شكلاً ولا يُستدعى، فلا شيء يمنع فتح المتجر ببضاعة تجريبية — وهو ما يقع الآن فعلاً.

### الفصل 18 كله بلا مخطَّط: لا لافتات ولا إعلانات ولا مجموعات ولا صفحات ولا مقالات ولا نصوص واجهة في القاعدة
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `grep -n '^model' apps/api/prisma/schema.prisma → 51 نموذجاً ليس فيها banners/announcements/collections/collection_items/pages/posts/ui_strings/content_revisions/holidays/branches ؛ المحتوى مكتوب في apps/site/src/lib/legal.ts و help.ts و blog.ts ؛ docs/18-content-settings.md:175`
- **الأثر**: كل تعديل في سياسة إرجاع أو مقال مساعدة أو بانر يتطلب اليوم تعديل شيفرة وبناءً ونشراً. وهذا يناقض دورة PENDING_REVIEW ← موافقة content:publish المكتوبة، ويجعل صاحب المتجر عاجزاً عن تصحيح نصّ قانوني بنفسه.

### لا جدول سياسات كفالة ولا تثبيت policy_version على بند الطلب
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `لا model WarrantyPolicy في apps/api/prisma/schema.prisma ؛ model OrderItem يبدأ في السطر 449 وليس فيه policyVersion ؛ docs/15-aftersales.md:38 يشترط الخزن بإصدار مرقّم وإقراراً إلزامياً قبل إنشاء الطلب وتثبيت policy_version على البند`
- **الأثر**: بلا تثبيت الإصدار، تغييرُ نصّ الكفالة اليوم يغيّر أثراً رجعياً ما وُعد به مشترٍ قبل ستة أشهر. وهو الجدل الذي وُضع التثبيت أصلاً لمنعه، ويصير المتجر بلا حجّة مكتوبة عند الخلاف.

### لا خدمة عامل للموقع ولا لتطبيق العميل: لا Web Push للزبون ولا تصفّح دون اتصال
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `عامل الخدمة الوحيد هو apps/admin/public/sw.js وهو مقصور على واجهة المندوب بنصّه (السطر 2) ؛ grep -n 'serviceWorker|pushManager' apps/app/src/App.tsx → صفر ؛ مسارا /push/key و/push/subscribe موجودان في routes.ts:196-200 بلا عميل ينادِيهما ؛ VAPID_PUBLIC_KEY فارغ (push.module.ts:16)`
- **الأثر**: جدول القنوات في الفصل 20 يعلّق P1 وP2 على Web Push، ولا سبيل إلى اشتراك بلا عامل خدمة مسجَّل. و13.3 بند 6 يشترط عملاً دون اتصال في بلد بأربع ساعات كهرباء — الموقع اليوم يسقط كاملاً عند انقطاع الشبكة.

### عربون 10-20% خارج دمشق غير منفَّذ، والدفع مقفل على تحصيل كامل عند الباب
- **الحال**: غائب · **ينفّذه**: المالك · **✔ مُدقَّق** · *readiness*
- **الدليل**: `grep -rniE 'عربون|deposit|down_payment' apps/api/src/modules/orders.module.ts apps/api/src/modules/cart.module.ts → صفر ؛ الحقل depositUsdCents في schema.prisma:937 يخصّ كفالة المندوب لا الطلب ؛ docs/13-syria-reality.md:53`
- **الأثر**: القرار المكتوب يعلّل نفسه: العربون يغطي أجور الذهاب والإياب خارج دمشق حيث الرفض عند الباب أغلى. بدونه كل محاولة تسليم فاشلة في محافظة بعيدة خسارة كاملة على المتجر.

### عنوان المحل ما زال فارغاً في البيانات المنظَّمة
- **الحال**: بيد المالك · **ينفّذه**: المالك · **✔ مُدقَّق** · *readiness*
- **الدليل**: `apps/site/src/lib/seo.ts:25 — streetAddress: null as string | null، والسطر 39 يُسقط الحقل من PostalAddress حين يكون فارغاً`
- **الأثر**: بند الجاهزية CONTENT_SITEMAP_SCHEMA يشترط بيانات منظَّمة صحيحة. متجر هواتف بدفع عند الاستلام بلا عنوان معلن يخسر أهم إشارة ثقة في سوق حاجزه الأول الخوف من النصب — والفراغ هنا قرار مؤجَّل لا خطأ تقني.

### النشر معلَّق على فرع claude/website-remaining-plans-cjlncb لا على الفرع الرئيسي
- **الحال**: دَين · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `.github/workflows/deploy.yml — on.push.branches: [claude/website-remaining-plans-cjlncb] ؛ بينما docs/DEPLOY.md:52 يقول «أو ادفع إلى main فيتولّى deploy.yml النشر»`
- **الأثر**: الوثيقة تصف سلوكاً لا يقع. وأي مساهم يدفع إلى فرع آخر ظانّاً أن النشر آلي سيجد الإنتاج متجمّداً بلا رسالة خطأ — وهو الفشل الصامت الذي عولج مرة في هذا الملف نفسه.

### وعدان في مصفوفة M0 بلا مسار خلفهما: حفظ المقارنة في الحساب، وقائمة كفالات أجهزتي بتواريخها
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `لا /me/comparisons ولا /me/warranties في apps/api/src/http/routes.ts (مسارات الحساب في السطور 204-230) ؛ apps/site/src/pages/compare.astro مقارنة محلية بلا حفظ ؛ 19.1 يدرج /app/account/comparisons و/app/account/warranties كليهما في M0`
- **الأثر**: صفّان صريحان في مصفوفة الاكتمال بمرحلة M0. وقائمة الكفالات تحديداً هي ما يجعل الزبون يعود: بدونها يعرف تاريخ انتهاء كفالته من ورقة قد يفقدها، ولا يطالب بها في وقتها.

### نصوص الإشعارات مكتوبة داخل الشيفرة ولا مجلّد قوالب بنسختَي `ar`/`en`
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: ``ls apps/api/src/notifications` ← المجلّد غير موجود. والنص داخل منطق العمل: apps/api/src/modules/auth.module.ts:77-78 (`title: 'رمز الدخول'`, body بقالب حرفي)`
- **الأثر**: الوثيقة 20.10 §7 صريحة: «كل القوالب في `apps/api/src/notifications/templates/` بنسختين `ar`/`en`، ولا نص مكتوب داخل الكود». والأثر عملي لا شكلي: قالب واتساب المعتمد لدى المزوّد (BSP) يجب أن يطابق نصاً مسجَّلاً مسبقاً، ونصٌّ متناثر في عشرين ملفاً لا يمكن مطابقته ولا مراجعته قبل الاعتماد.

### لا `consent_logs` ولا لافتة كوكيز ولا كوكي `__Host-ts_consent`
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: ``grep -rni "consent" apps/ --include=*.ts --include=*.tsx --include=*.astro` ← لا نتيجة. ولا نموذج في apps/api/prisma/schema.prisma`
- **الأثر**: الوثيقة 9.7 تشترط «موافقة صريحة منفصلة بين موافقة الخدمة وموافقة التسويق مخزّنة في `consent_logs` (نوع الموافقة، النسخة، الطابع الزمني، IP، القناة)»، ولافتة بثلاث فئات، وإعادة موافقة عند تغيّر إصدار المستندات القانونية. وبلا سجلّ موافقة لا يمكن إثبات أساس معالجة أي رسالة تسويقية أُرسلت.

### لا اختبار آلي للوصول المتقاطع رغم أنه شرط في كل إصدار
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `ملفات الاختبار الأربعة كلها وحدات منطق: apps/api/src/modules/sla.test.ts و pricing.test.ts و apps/api/src/common/password.test.ts و arabic.test.ts. ولا Playwright في المستودع (`find . -name "playwright*" -not -path "*/node_modules/*"` ← لا نتيجة)`
- **الأثر**: الوثيقة 9.4 A01: «اختبار تلقائي (Playwright) للوصول المتقاطع على **كل** مسار يحمل معرّف مورد في كل إصدار». وهو الاختبار الذي كان سيكشف فوراً البندَين الأول والثاني في هذه القائمة — لا يوجد اليوم ما يمنع عودة الثغرة نفسها بعد إصلاحها.

### مقارنة رمز OTP ليست بزمن ثابت، والتجزئة SHA-256 لا argon2id
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/modules/auth.module.ts:102 — `if (this.hash(phone, code) !== rec.hash)` مقارنة سلاسل عادية. والتجزئة في السطر 44-46: `createHash('sha256')` بفلفلة بـ`JWT_SECRET``
- **الأثر**: جدول 9.1 يحدّد «argon2id (لا يُخزَّن نصاً صريحاً) — مقارنة بزمن ثابت». والمشروع يملك `timingSafeEqual` مستورداً أصلاً في common/jwt.ts:1 فالكلفة سطر واحد. وSHA-256 على مجال من مليون احتمال يُكسر لحظياً إن تسرّب مخزن KV، خلافاً لـargon2id.

### لا قائمة بادئات مشغّلين مسموحة ولا رفض لأرقام VoIP قبل استهلاك حصة الإرسال
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/modules/auth.module.ts:15 — `const SY_PHONE = /^\+9639[0-9]{8}$/` وحده، وهو ما يقبل `+96390…` و`+96391…` و`+96392…` و`+96397…` غير المخصّصة. والوثيقة تحصرها في `+96393/94/95/96/98/99``
- **الأثر**: الوثيقة 9.1: «أي رقم خارجها يُرفض بـ`VALIDATION_ERROR` قبل استهلاك أي حصة إرسال، وهذا وحده يقطع معظم محاولات استنزاف الرسائل». أربع بادئات مفتوحة تعني أربعين مليون رقم وهمي صالح للطلب — كل واحد منها يكلّف 3–5 سنتات.

### لا `otp_delivery_logs` ولا تتبّع تكلفة ولا قاطع دارة يومي
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `لا نموذج في apps/api/prisma/schema.prisma (`grep -n "OtpDelivery\|otp_delivery" schema.prisma` ← لا نتيجة). و apps/api/src/modules/channels.ts يعيد `SendResult` بلا `cost_usd_cents` ولا `provider_message_id` ولا كتابة في قاعدة`
- **الأثر**: الوثيقة 9.1 تعدّد حقوله بالاسم لغرضين: «لمطابقة فاتورة المزوّد شهرياً وقياس نسبة تسليم كل قناة لكل محافظة». وبلا هذا السجل لن يُعرف أن هجوم استنزاف يجري إلا حين تصل الفاتورة، ولن تُقاس مؤشرات 20.11 (نسبة التسليم ≥97%، نسبة التصعيد ≤5%) أصلاً.

### عدّاد حدّ المعدل غير ذرّي على KV، وموضوع «هاتف» بلا تطبيع يسقط إلى دلو مشترك
- **الحال**: دَين · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/common/kv.ts:85-93 — قراءة ثم كتابة، والتعليق يعترف بأن «طلبين متزامنين يقرآن العدّاد نفسه فيمرّ واحدٌ زائد». و apps/api/src/common/rate-limit.ts:66-70 — الرقم يُقرأ خاماً من الجسم وأي فشل يجعله `'anon'``
- **الأثر**: الوثيقة 9.5 تشترط عدّاداً متسقاً بين النسخ. الأثر العملي على OTP: طلبات متزامنة تتجاوز الحدّ الخمسي، وكل من يرسل الرقم بصيغة مغايرة أو خارج JSON يتشارك دلواً واحداً باسم `anon` — فيحجب بعضهم بعضاً أو يمرّ الجميع، وكلاهما خطأ. الحلّ المتاح على Cloudflare هو Durable Object لا KV.

### رسائل الخطأ تكشف بنية الأدوار، والسجل يطبع الاستثناء كاملاً إلى سجلات المزوّد
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/http/auth.mw.ts:63-65 — `FORBIDDEN` يُرفق `{ required: roles, actual: claims.role }` في جسم الاستجابة عبر ApiError.body() (errors.ts:25-27). و apps/api/src/http/app.ts:132 `console.error('[api] خطأ غير متوقَّع:', err)` مع `"observability": { "enabled": true }` في wrangler.jsonc`
- **الأثر**: معالج الخطأ العام يحسن الصنع بإخفاء الأثر عن العميل، لكن الحارس يسرّب من الباب الآخر: `required` تعطي المهاجم خريطة أسماء الأدوار لكل مسار. وطباعة الاستثناء خاماً قد تحمل معه معطيات الطلب — بينها أرقام هواتف — إلى سجلات المزوّد، والوثيقة 9.11 تشترط «لا تُرسل أي بيانات P1 إلى الرصد في الحالتين».

### لا تجريد EXIF ولا إعادة ترميز للصور، والحدّ 3MB بدل 8MB، ولا روابط رفع موقّعة
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/modules/storage.ts:15 (`MAX_BYTES = 3 * 1024 * 1024`) و:99 (`bucket.put(key, buf, ...)` يكتب البايتات الخام كما وصلت). ولا استدعاء لأي معالجة صورة في الملف. والرفع عبر data URL في الجسم (storage.ts:55) لا برابط موقّع`
- **الأثر**: فحص Magic Bytes مطبَّق فعلاً (storage.ts:27-53) وهذا صحيح، لكن الوثيقة 9.4 تشترط أيضاً «إعادة ترميز الصورة وتجريد EXIF (مهم لصور التسليم: تحمل إحداثيات منازل العملاء)». صورة تسليم تُرفع اليوم تصل إلى R2 وفيها إحداثيات GPS لباب الزبون، وتُخدَم من `/media/*` بلا أي قيد وصول (worker.ts:173-183).

### لا مسار /app/login مستقلاً — الدخول مدفون داخل شاشة الحساب
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/app/src/App.tsx:1096-1100 لا فرع لـ `/app/login`؛ `SignInCard` (App.tsx:371) يُصيَّر فقط داخل `AccountPage` عند `!me` (App.tsx:740-746). شاشة 14 في docs/05-mobile-ux.md:111 تحدّد المسار `/app/login`.`
- **الأثر**: لا يمكن إرسال رابط دخول مباشر (من رسالة واتساب مثلاً)، ولا إعادة توجيه المستخدم إلى الدخول ثم إرجاعه إلى ما كان يفعله. وحقل OTP بلا لصق تلقائي ولا خانات ستّ ولا زر «أرسل SMS بدلاً منها» بعد 30 ثانية.

### لا مسار /app/wishlist ولا زر إضافة للمفضلة في أي مكان
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: ``Wishlist()` (apps/app/src/App.tsx:531) قسم داخل AccountPage:790، بلا مسار خاص؛ و`POST /me/wishlist` (routes.ts:218) لا يستدعيه أي ملف واجهة — الاستدعاءات الوحيدة هي GET وDELETE (App.tsx:533, 554).`
- **الأثر**: القائمة تعرض ما لا يمكن إضافته: لا سبيل لوضع صنف فيها من صفحة المنتج، فتبقى فارغة دائماً — ومعها يسقط تنبيه التوفّر ومقارنة السعر المبنيان عليها.

### لا شريط تنقّل سفلي في /app فينكسر الإحساس بوحدة التطبيقين
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/app/src/App.tsx:1083-1101 تُصيّر `<header>` والصفحة فقط، بلا مكوّن تنقّل. docs/05-mobile-ux.md:371 يشترط «يظهر في صفحات Astro وفي /app بالمظهر نفسه من packages/ui فلا يُلاحظ المستخدم عبور الحدّ».`
- **الأثر**: من دخل السلة لا يجد طريقاً للرجوع إلى الفئات أو البحث إلا بسهم واحد في الرأس. عبور الحدّ بين Astro وSPA يصير مرئياً، والزبون يشعر أنه غادر المتجر إلى موقع آخر.

### الشريط السفلي أربعة عناصر بدل خمسة، وبلا شارة عدد للسلة
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/site/src/components/BottomNav.astro:4-9 يعرّف أربعة عناصر (الرئيسية، البحث، السلة، حسابي) بلا «الفئات» وبلا شارة. docs/05-mobile-ux.md:371 يحدّد خمسة عناصر ثابتة، و:80 يشترط شارة عدد مُولَّدة على الخادم من كوكي `cart_id`.`
- **الأثر**: لا مدخل للفئات من التنقّل الرئيسي (ولا صفحة فهرس فئات أصلاً: `/c/` غير موجودة)، والزبون لا يعرف أن في سلته شيئاً حتى يفتحها — فتُنسى السلال المتروكة.

### صفحة الفئة بلا «تصفية» ولا «ترتيب»
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/site/src/pages/c/[slug].astro:17 تعليق صريح «وزرّ «تصفية» المعطَّل حُذف»؛ الترتيب مثبَّت بالسعر تصاعدياً في السطر 24 بلا خيار. شاشة 2 في docs/05-mobile-ux.md:99 تطلب زرَّي تصفية وترتيب (48px) يفتحان أوراقاً سفلية.`
- **الأثر**: في كتالوج يخلط الجديد بالمستعمل وGULF بـEURO وسعات مختلفة، غياب الترشيح يعني أن المشتري يتصفّح عشرات البطاقات بحثاً عن نسخة بعينها — على شبكة تُحسب فيها كل صورة.

### الأوراق السفلية غير مُنفَّذة إطلاقاً: أنماط بلا استعمال
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `packages/ui/src/base.css:314-341 تعرّف `.sheet-scrim` و`.sheet` و`.sheet__handle`؛ و`grep -rn 'role="dialog"\|aria-modal\|class="sheet' apps/` = صفر. docs/05-mobile-ux.md:372-379 يجعلها الآلية الوحيدة للمرشحات والترتيب والمتغيّر والعنوان والعملة والمشاركة وإرشاد التثبيت، بست متطلبات a11y إلزامية.`
- **الأثر**: سبعة تدفّقات موصوفة تعتمد كلها على نمط تفاعل واحد لم يُبنَ. وكل ما بُني بديلاً عنه (قوائم select، نماذج مفتوحة داخل الصفحة) يفتقد حصر التركيز وقفل التمرير والإغلاق بـEscape.

### لا رابط تخطّي إلى المحتوى (skip link) في أي واجهة
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/site/src/layouts/Base.astro:86-98 يبدأ الجسم بـ sysbar ثم `<slot />` مباشرة؛ `.sr-only` معرَّف في packages/ui/src/base.css:426 بلا أي استعمال كرابط تخطٍّ. docs/05-mobile-ux.md:539 يدرجه ضمن «إضافات إلزامية».`
- **الأثر**: مستخدم لوحة المفاتيح أو قارئ الشاشة يمرّ على الشريط العلوي والتنقّل في كل صفحة قبل أن يبلغ المحتوى. وهو بند يفشل الفحص الآلي بـaxe المشترط في كل بناء.

### لا aria-live ولا role=alert في اللوحة، وواحد فقط في تطبيق الزبون
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: ``grep -c aria-live apps/admin/src/App.tsx` = 0، وapps/app/src/App.tsx = 1 (عدّاد الكمية، السطر 118). كل رسائل `.err` و`.ok` (مثل apps/admin/src/App.tsx:326-327) نصوص صامتة. docs/05-mobile-ux.md:539 يشترط إعلان تغيّر السلة والمرشحات بـ`aria-live="polite"`.`
- **الأثر**: موظف أو زبون يعتمد قارئ شاشة يضغط «أكّد الطلب» فلا يسمع شيئاً: لا نجاحاً ولا خطأً. النتيجة ضغطة ثانية وثالثة على إجراء مالي.

### لا عنصر <main> ولا h1 لكل شاشة في التطبيقين
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: ``grep -n "<main" apps/app/src/App.tsx apps/admin/src/App.tsx` = صفر؛ المحتوى داخل `<div className="page">` (app App.tsx:1100، admin App.tsx:2438). h1 واحد في الرأس يتبدّل نصه (app App.tsx:1089). docs/05-mobile-ux.md:539 يشترط «h1 واحد لكل صفحة» وترتيب عناوين منطقياً.`
- **الأثر**: لا معالم (landmarks) للتنقّل بقارئ الشاشة داخل SPA، وشاشة الحساب تبدأ بـh2 تحت h1 لا يصفها. مخالفات serious في axe تفشل بوابة البناء المشترطة في القسم 5.9.

### مؤشر الخطوات الثلاث في إتمام الطلب زخرفة: لا خطوة شحن ولا مراجعة
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/app/src/App.tsx:175-177 ثلاث نقاط، اثنتان `data-on` ثابتتان بلا منطق؛ والصفحة نموذج عنوان واحد ثم زر «أكّد الطلب» (App.tsx:237). docs/05-mobile-ux.md:106 يحدّد «العنوان ← الشحن ← المراجعة».`
- **الأثر**: الزبون يؤكّد طلباً دون أن يرى ملخّص ما سيدفعه ولا تعريفة الشحن على محافظته في الشاشة نفسها — في متجر يُدفع فيه نقداً عند الباب، مفاجأة المبلغ عند التسليم هي أول سبب للرفض.

### صفحة تأكيد الطلب بلا زر نسخ ولا زر واتساب ولا عدّاد حيّ
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/app/src/App.tsx:279 رقم الطلب نصّاً بلا زر نسخ؛ :273 `hoursLeft` يُحسب مرة واحدة عند التصيير ويُعرض ثابتاً في السطر 293؛ ولا رابط `wa.me` في الملف كله. شاشة 10 في docs/05-mobile-ux.md:107 تطلب الثلاثة.`
- **الأثر**: الزبون يحتاج نسخ رقم طلبه ليرسله على واتساب — وهو مسار الثقة الأول في هذا السوق (5.5). ونسخ رقم بصيغة TS-2607-000431 يدوياً على هاتف يعني خطأً في خانة.

### السلة بلا مؤقّت الحجز المرن وبلا «تراجع» بعد الحذف
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/app/src/App.tsx:107-144 `CartPage` لا تعرض أي مؤقّت؛ زر «إزالة» (App.tsx:120) يستدعي `setQty(sku, 0)` فوراً بلا شريط تراجع. شاشة 8 في docs/05-mobile-ux.md:105 تطلب مؤقّت 15 دقيقة ظاهراً و«تراجع» 5 ثوانٍ، و§5.13(د) يجعل التراجع لازماً للحذف.`
- **الأثر**: المخزون يُتتبع بالوحدة والقطع نادرة: زبون لا يرى أن حجزه ينتهي بعد ربع ساعة يعود ليجد سلته فارغة بلا تفسير. وحذف بلمسة واحدة بلا تراجع يُفقد صنفاً نادراً كان محجوزاً له.

### أخطاء الشبكة في شاشات الحساب تُبتلع وتُعرض كأنها «لا يوجد شيء»
- **الحال**: دَين · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/app/src/App.tsx:462 و:533 و:571 و:650 كلها `.catch(() => setRows([]))`؛ و:626 و:683 `.catch(() => {})`. docs/05-mobile-ux.md:385 يشترط لكل شاشة «حالة خطأ شبكة بزر إعادة المحاولة».`
- **الأثر**: انقطاع الشبكة يُري الزبون «لا عناوين» و«لا أجهزة مسجّلة» — فيضيف عنواناً موجوداً أصلاً، أو يظن أن جلساته حُذفت. الخطأ الصامت هنا أسوأ من الخطأ الظاهر لأنه يدفع إلى فعل خاطئ.

### لا صفحة /offline ولا عامل خدمة للمتجر
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: ``find apps -name "offline*"` يعيد apps/admin/src/lib/offline.ts فقط؛ و`ls dist-worker/` بلا `offline/` وبلا `sw.js`. شاشة 17 في docs/05-mobile-ux.md:114 و§5.8:505 تشترطان تخزين آخر 20 صفحة منتج مُزارة وصفحة `/offline`.`
- **الأثر**: انقطاع التيار عن برج الاتصال في منتصف التصفّح — وهو الافتراض الذي بُني عليه الفصل كله — يعطي صفحة خطأ المتصفح. ولا يبقى للزبون أثر من المنتجات التي كان يوازن بينها.

### لا شاشات تشغيل يومي: التجهيز والطباعة والتحديث الجماعي وصندوق الاستثناءات وحالة الطوابير
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `لا تبويبات لها في apps/admin/src/App.tsx:2412-2420؛ ولا مسارات `bulk-status` أو `labels` في apps/api/src/http/routes.ts. docs/10-admin.md:125-127 و:154 و:332 تحدّد `/ops/picking` و`/ops/labels` و`/ops/bulk-status` و`/ops/exceptions` و`/ops/jobs`.`
- **الأثر**: هدف «أقل من 4 ساعات من التأكيد إلى التسليم للمندوب» (10.4) مبني على هذه الشاشات. بدونها يُعالَج كل طلب على حدة بلا ملصق مطبوع ولا فاتورة، فلا يصمد التشغيل بعد بضع عشرات الطلبات يومياً.

### لا شاشة تفاصيل طلب واحد في اللوحة ولا ملاحظات داخلية ولا عرض لسجل التدقيق
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: ``OrdersQueue` (apps/admin/src/App.tsx:89) قائمة بطاقات بلا تفاصيل ولا بنود ولا سجل؛ و`GET /admin/audit` (apps/api/src/http/routes.ts:418) لا يستدعيه أي ملف في apps/admin. docs/10-admin.md:72 يوجب «تفاصيل، ملاحظات داخلية، سجل التدقيق».`
- **الأثر**: سجل التدقيق يُكتب ولا يُقرأ من أي شاشة، فتضيع قيمته كلها. ولا مكان لتدوين ما قيل في مكالمة سابقة، فيبدأ كل موظف من الصفر مع الزبون نفسه.

### لا بحث سريع Ctrl/Cmd+K ولا فرز/فلترة محفوظة في المسار ولا إجراءات جماعية
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: ``grep -n "keydown\|metaKey\|useSearchParams" apps/admin/src/App.tsx` = صفر؛ التنقّل أزرار تبويب تستدعي `nav(to)` (App.tsx:2441) بلا معاملات استعلام. docs/10-admin.md:90 يوجب حفظ الفلاتر في المسار وترقيماً بالمؤشر وإجراءات جماعية واختصارات `g o`/`g i`/`g c`/`g s`، و:158 يوجب البحث السريع.`
- **الأثر**: لا سبيل للانتقال من رقم هاتف أو IMEI أو رقم بوليصة إلى الطلب المعني — وهو أول ما يفعله موظف الدعم في كل مكالمة. ولا سبيل لمشاركة رابط قائمة مفلترة بين موظفَين.

### إعدادات المتجر تُخفي بصمت كل مفتاح بلا تسمية، و tax_rate_bp غير موجود أصلاً
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/admin/src/App.tsx:1125 `if (!meta) return null;` داخل حلقة عرض الإعدادات؛ وخريطة `SETTING_AR` (App.tsx:1070-1084) لا تذكر `tax_rate_bp`، وقائمة `KNOWN` في apps/api/src/modules/admin.module.ts:235-249 ترفضه. docs/10-admin.md:88 يدرجه ضمن إعدادات `/settings`.`
- **الأثر**: إعداد يُضاف في الخادم يختفي من الشاشة بلا أثر ولا تحذير، فيظن المالك أنه غير موجود. ولا سبيل لضبط نسبة الرسوم التي تعتمد عليها الفاتورة المطبوعة (10.4).

### لا شاشة محتوى وبانرات (‏/content) رغم أن الصفحة الرئيسية تعتمد بانراً
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `لا تبويب في apps/admin/src/App.tsx:2412-2420 ولا مسار `banners`/`pages`/`posts` في apps/api/src/http/routes.ts؛ ومقالات المدونة ثابتة في الشيفرة (apps/site/src/lib/blog.ts:24 وما بعده). docs/10-admin.md:84 و:375-377 توجب الوحدة بنافذة زمنية ونص بديل إلزامي ومعاينة على 390px.`
- **الأثر**: كل تغيير في بانر أو مقال أو صفحة ثابتة يحتاج مبرمجاً وإعادة نشر — فتتجمّد واجهة المتجر التسويقية، وهي أساس اكتساب الزيارات العضوية الموصوف في القسم 12.

### لا تفاصيل تذكرة ولا ردّ ولا تقييم خدمة في شاشة الدعم
- **الحال**: جزئي · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/app/src/App.tsx:1049-1066 تعرض قائمة تذاكر نصية غير قابلة للنقر؛ و`grep -rn "csat\|tickets/.*reply" apps/app/src` = صفر رغم وجود `POST /support/tickets/:ticketNo/reply` و`/csat` (apps/api/src/http/routes.ts:172, 176).`
- **الأثر**: الزبون يفتح تذكرة ثم لا يستطيع قراءة الرد عليها ولا الردّ بدوره — فيعود إلى واتساب، وتصير التذاكر سجلاً من طرف واحد لا قناة دعم. ومؤشر رضا العملاء لا يُجمع أبداً.

### لا زر «أعلمني عند التوفّر» ولا تنبيه سعر ولا إشعارات متصفح في أي واجهة
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: `apps/site/src/pages/p/[slug].astro:180 عند `stock === 0` الزر معطَّل بنص «نفد المخزون» وحده؛ و`grep -rn "/alerts/\|push/subscribe" apps/site/src apps/app/src` = صفر رغم وجود `/alerts/stock` و`/alerts/price` و`/push/subscribe` (apps/api/src/http/routes.ts:180, 187, 198).`
- **الأثر**: كل زائر يصل إلى جهاز نافد يُفقد نهائياً بلا وسيلة لاستعادته، رغم أن مهمة الكرون التي تفحص التنبيهات كل خمس دقائق تعمل فعلاً (apps/api/src/worker.ts:229) على جدول لا يدخله أحد.


## منخفضة (10)

### إعادة الفهرسة مسارٌ يعدّ ولا يفعل، وبند جاهزيةٍ مبنيّ عليه
- **الحال**: دَين · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/api/src/modules/search.module.ts:54-57 `async reindex() { const docs = await this.documents(); return { engine: 'd1', indexed: docs.length, note: 'البحث يقرأ الكتالوج مباشرة — لا فهرس منفصل' }; }` وهو مسجَّل في routes.ts:76. وبند TECH_REINDEX_ON_DEPLOY في readiness.module.ts:40 يفترض فهرسةً تجري عند النشر — ولا خطوة في deploy.yml تناديه.`
- **الأثر**: الردّ صادق في نصّه، لكن بند الجاهزية الذي يعتمد عليه سيُؤشَّر ناجحاً على عملٍ لا يقع. البند يجب أن يُحذف أو يُعاد تعريفه، لا أن يُمرَّر.

### اعتماديات وأدوات ميتة من عهد PostgreSQL ما زالت في الشجرة وفي ملف القفل
- **الحال**: دَين · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *code*
- **الدليل**: `apps/api/package.json:16 `"@prisma/adapter-pg": "^6.19.3"` بلا أي استيراد (`grep -rn "adapter-pg\|PrismaPg" apps/api/src` فارغ). وapps/api/package.json:11 السكربت `migration:new` يكتب إلى `prisma/migrations/…` ثم ينادي `prisma/apply-constraints.mjs` — و`ls apps/api/prisma/` يبيّن أن مجلد migrations غير موجود أصلاً (الترحيلات في apps/api/migrations).`
- **الأثر**: وزنٌ في حزمة الـWorker بلا مقابل، وسكربتٌ مكسور ينتظر من ينادِيه ليكتشف أنه لا يعمل. وشيفرةٌ لغرضٍ انقضى تُحذف — وهي القاعدة التي كتبها المشروع لنفسه في تعليق storage.ts:6-9.

### سجلّ التدقيق بلا استبقاء ولا تصدير ولا حماية من الحذف
- **الحال**: دَين · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *infra*
- **الدليل**: `26 موضع auditLog.create في apps/api/src/modules/، ومسار قراءة واحد في apps/api/src/http/routes.ts:416. ولا مهمة تدوير ولا تصدير ولا نسخ خارج القاعدة في أي مكان. واستعلام الإنتاج: SELECT count(*) FROM audit_logs ⇒ 0. و docs/09-security.md:215 يوجب استبقاءً خمس سنوات للسجلّ التشغيلي.`
- **الأثر**: سجلٌّ يعيش في الجدول نفسه الذي يراقبه، بلا نسخة خارجه: من يملك صلاحية الحذف يمحو أثره معه. وخمس سنوات استبقاءٍ موعودة في وثيقة الأمن بلا آلية واحدة تحقّقها.

### المستودع الوحيد مبذور بلا عنوان ولا نقطة استلام
- **الحال**: غائب · **ينفّذه**: المالك · **؟ غير مُدقَّق** · *owner*
- **الدليل**: `apps/api/prisma/seed-d1.mjs:69-73 warehouses بحقول code وname وgovernorate فقط · docs/18-content-settings.md:131 يطلب جدول branches بالحي والشارع والمعلم والهاتف والساعات — وهو غير منفَّذ`
- **الأثر**: سقفُ الطلب المرتفع يُحال إلى «استلام من المعرض» (نصّ اللوحة في App.tsx:1073) ولا معرض بعنوان في النظام يُحال إليه.

### /help/faq يعطي 404 رغم أنه المسار المذكور في مصفوفة الاكتمال
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `curl -o /dev/null -w '%{http_code}' https://talisham.com/help/faq → 404 ؛ الشرائح المتاحة في apps/site/src/lib/help.ts هي how-to-order وpricing-in-syp وcheck-imei وreturns وwarranty وshipping ؛ 19.1 صفّ الدعم يذكر /help/faq و/help/articles`
- **الأثر**: مقالات المساعدة منشورة فعلاً وجيّدة، لكن الرابط الموعود في الوثيقة يسقط. وبند CONTENT_HELP_PUBLISHED يُؤشَّر بإثبات، وإثباتٌ برابط 404 لا يصلح.

### لا مسار وارد لإيصالات تسليم الرسائل من مزوّد القناة
- **الحال**: غائب · **ينفّذه**: المبرمج · **✔ مُدقَّق** · *readiness*
- **الدليل**: `لا POST /webhooks/messaging/{provider} في apps/api/src/http/routes.ts ؛ docs/04-api.md:475 يعرّفه لـ{whatsapp, sms} بقائمة IP وتوقيع وردّ دون 500 مللي ثانية`
- **الأثر**: بلا إيصال تسليم لا يُعرف هل وصل رمز الدخول أم لا، فيُنسب الفشل إلى الزبون. وهو شرط عملي لتمرين القناة الاحتياطية الأسبوعي المنصوص عليه في 9.12.

### `POST /push/unsubscribe` بلا مصادقة — أي أحد يعطّل اشتراك أي جهاز يعرف عنوانه
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/http/routes.ts:199 — `app.post(`${P}/push/unsubscribe`, async (c) => ...)` بلا حارس، و apps/api/src/modules/push.module.ts:101-106 `updateMany({ where: { endpoint } })` بلا ربط بالمستخدم`
- **الأثر**: عنوان الاشتراك ليس سرّاً بالضرورة، وتعطيله يقطع عن الزبون إشعارات «خرج للتوصيل» و«فشل التسليم» — وهي في الفصل 20.6 غير قابلة للإيقاف عمداً، لأن فوات المندوب يكلّف شحنة ذهاباً وإياباً.

### `boot-guard.ts` شيفرة ميتة غير مستوردة، وتستدعي `PrismaClient` لا يعمل داخل Worker
- **الحال**: دَين · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *security*
- **الدليل**: `apps/api/src/common/boot-guard.ts:1 `new PrismaClient()` (السطر 9) — و`grep -rn "boot-guard\|assertNoPublishedDemoInProd" apps/api/src` لا يعطي سوى الملف نفسه`
- **الأثر**: الوثيقة والتعليق داخل الملف يصفانه بأنه «بوابة الإقلاع (الفصل 7 §7.10): فشل صريح عند التشغيل أرحم من متجر يعرض بضاعة لا وجود لها». وهو لا يعمل: لا يُستدعى، ولن يعمل لو استُدعي لأن الـWorker يبني Prisma من ربط D1 لا من `new PrismaClient()`. وجوده يوحي بحماية غير قائمة — وهذا أسوأ من غيابه.

### لا رموز تصميم مشتركة ولا حارس زجاج ولا حزمة i18n كما تصفها الوثيقة
- **الحال**: دَين · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: ``ls packages/ui/src` = base.css, index.ts, money.ts, money.test.ts, tokens.css — لا `theme.css` (docs/05:32) ولا `glass.css` (:161) ولا `glass-guard.ts` (:288) ولا `store.ts` (:78)؛ و`ls packages/` = types, ui فقط، بلا `packages/i18n` (:404, :408).`
- **الأثر**: إطفاء الزجاج التكيّفي على الأجهزة الضعيفة (deviceMemory ≤ 2، saveData، هبوط الإطارات) غير مُنفَّذ، وهو الاحتياط الذي بُني القسم 5.4 كله حوله لسوق أغلب أجهزته ضعيفة. والتنسيق الرقمي والنقدي مكرّر يدوياً في ثلاثة أماكن بدل مصدر واحد.

### لا وضع «توفير البيانات» ولا شريطه رغم إفراد قسم كامل له
- **الحال**: غائب · **ينفّذه**: المبرمج · **؟ غير مُدقَّق** · *ui*
- **الدليل**: ``grep -rn "saveData\|data-saver\|effectiveType" apps/` = صفر؛ ولا كوكي `saver` يُقرأ في apps/api/src/worker.ts. docs/05-mobile-ux.md:559-575 يحدّد تفعيلاً تلقائياً وتبديلاً يدوياً في /app/account وتسع سلوكيات مختلفة وشريطاً تفسيرياً.`
- **الأثر**: مستخدم على 2g أو برصيد بيانات محدود — وهو الجمهور المستهدف صراحةً — يتلقّى الصفحة كاملة بجودة الصور القصوى. والشاشة الوحيدة التي يُفترض أن تحمل المبدّل (/app/account) لا تحمل أي إعداد مظهر.

