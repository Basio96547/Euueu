<div dir="rtl">

# تالي شام — متجر إلكتروني سوري متخصص في الهواتف الجوالة

> متجر إلكتروني عربي (RTL) متخصص في بيع الهواتف الجوالة وملحقاتها داخل سوريا، مبني بمنهج **الجوال أولاً (Mobile-First)** على شبكات بطيئة وأجهزة متواضعة، بواجهة **Astro** لصفحات البحث وتطبيق تفاعلي للسلة والحساب، ونظام تصميم **Liquid Glass**، و**الدفع عند الاستلام** طريقةً وحيدة.

**حالة المشروع:** التنفيذ جارٍ. المستودع يحتوي الخطة المرجعية في عشرين فصلاً بمجلد [`docs/`](docs/)، ومكدساً عاملاً: موقع Astro، وواجهة NestJS، وتطبيق السلة، ولوحة الإدارة وواجهة المندوب — راجع [تشغيل المشروع](#تشغيل-المشروع).

| البند | القيمة |
| --- | --- |
| الاسم والنطاق | تالي شام · `talisham.com` · `api.talisham.com` · `admin.talisham.com` |
| السوق | سوريا — دمشق وريفها، حلب، حمص، حماة، اللاذقية، طرطوس أولاً، ثم بقية المحافظات |
| التخصص | الهواتف الجوالة والملحقات (جديد، مفتوح العلبة، مجدد، مستعمل بإفصاح) |
| منهج التصميم | الجوال أولاً + PWA + RTL + نظام Liquid Glass |
| الواجهة | Astro 5 لصفحات الـSEO (صفر JavaScript + جزر React) · React SPA للتطبيق ولوحة الإدارة |
| الخلفية | NestJS · PostgreSQL · Prisma · Redis · Meilisearch · تخزين متوافق مع S3 |
| النشر | Cloudflare (Pages/Workers) مع خطة بديلة موثّقة وقابلية نقل كاملة بـ Docker |
| الدفع | **الدفع عند الاستلام فقط** (نقداً بالليرة السورية) |
| المستودع والحزم | `talisham` · `@talisham/*` |

---

## نموذج الدفع: عند الاستلام فقط

لا توجد بوابة دفع إلكتروني في هذا المشروع، ولا خطوة دفع في مسار إتمام الطلب. العميل يطلب، ويتلقى **تأكيداً** (زر عبر واتساب، وتُصعَّد إلى مكالمة عند عدم الرد)، ثم يستلم جهازه ويدفع نقداً للمندوب أو في مكتب النقل.

```ts
// packages/types/src/order.ts
export type PaymentMethod = 'COD'                     // القيمة الوحيدة اليوم
export type PaymentStatus = 'PENDING' | 'COLLECTED' | 'PARTIAL' | 'REFUNDED'
```

- إضافة طريقة دفع مستقبلاً = قيمة جديدة في `payment_method` وخطوة إضافية في التدفق، دون إعادة هيكلة.
- حالة الطلب (تنفيذ) منفصلة عن حالة التحصيل (مال)، فلا يختلط تتبّع الشحنة بتتبّع النقد.
- التحصيل النقدي يُطابَق يومياً مع المندوبين ومكاتب النقل في `cash_settlements`، والفروقات تُرصد آلياً.

---

## القرارات المرجعية الموحّدة

هذه القرارات مُلزِمة لكل الفصول، وأي تعارض يُحسم لصالح ما هنا:

| الموضوع | القرار | المرجع |
| --- | --- | --- |
| التسعير | مرجع بالدولار `price_usd_cents` (`BIGINT`)، والعرض بالليرة السورية عبر `fx_rates` بسعر يحدّثه المدير | [الفصل 7](docs/07-catalog.md) |
| صلاحية سعر الصرف | لكل سعر `valid_until`؛ وعند تقادمه يتوقف استقبال الطلبات تلقائياً أو يُطبَّق هامش أمان — لا بيع بسعر ميت | [الفصل 7](docs/07-catalog.md) |
| تثبيت السعر | `orders.fx_rate` و`orders.total_syp` يُثبَّتان عند التأكيد وصلاحيتهما 48 ساعة ثم يُعاد التسعير | [الفصل 8](docs/08-orders.md) |
| الضريبة | لا ضريبة قيمة مضافة؛ `tax_rate_bp` قابل للتهيئة وقيمته الافتراضية صفر | [الفصل 7](docs/07-catalog.md) |
| المعرّفات | `UUIDv7` داخلي، ومعرّف عام منفصل غير زمني لكل مورد يظهر في رابط عام | [الفصل 3](docs/03-data-model.md) |
| كمية المخزون | `inventory_levels(on_hand, reserved, …)`، والمتاح = `on_hand − reserved`، والتطابق مع وحدات الأجهزة مفروض داخل المعاملة | [الفصل 3](docs/03-data-model.md) |
| وحدات الأجهزة | `device_units` يحمل IMEI والحالة والكفالة ورابط بند الطلب | [الفصل 3](docs/03-data-model.md) |
| مواصفات السوق | `device_origin` و`part_code` و`dual_sim` و`warranty_type` و`warranty_months` و`condition` إلزامية على مستوى المتغيّر | [الفصل 3](docs/03-data-model.md) |
| حالات الطلب | `PENDING_CONFIRMATION → PROCESSING → SHIPPED → OUT_FOR_DELIVERY → DELIVERED` وما يتفرع عنها | [الفصل 8](docs/08-orders.md) |
| رقم الطلب | `TS-YYMM-NNNNNN` مع قيد تحقق في قاعدة البيانات | [الفصل 8](docs/08-orders.md) |
| مُهَل الحجز | حجز أوّلي قصير عند إنشاء الطلب، يُمدَّد بعد التأكيد؛ ونافذة التأكيد 48 ساعة | [الفصل 7](docs/07-catalog.md) |
| العنوان | محافظة + مدينة + حي + شارع + **معلم قريب إلزامي** + رقمان للتواصل | [الفصل 3](docs/03-data-model.md) |
| الشحن | مندوبو المتجر داخل المدن، ومكاتب النقل بين المحافظات، والبريد — بتتبّع يدوي لا webhooks | [الفصل 8](docs/08-orders.md) · [17](docs/17-delivery-ops.md) |
| OTP والإشعارات | واتساب قناة أساسية، وSMS احتياطي يُختبر أسبوعياً، وأرقام `+963` | [الفصل 9](docs/09-security.md) |
| حدود المعدل | جدول الفصل 9 هو المصدر الوحيد، ولا تُكرَّر القيم في أي فصل آخر | [الفصل 9](docs/09-security.md) |
| ميزانيات الأداء | جدول الفصل 5 هو المصدر الوحيد، والقياس المرجعي على 3G بطيء لا على 4G | [الفصل 5](docs/05-mobile-ux.md) |
| نظام التصميم | التصميم الصلب هو الأساس، وLiquid Glass تحسين تدريجي يُفعَّل عند توفر القدرة | [الفصل 5](docs/05-mobile-ux.md) |
| الصيانة | إصلاح تلقائي أسبوعي، وترقية مكتبات كل شهرين عبر Renovate ببوابات قبول | [الفصل 11](docs/11-devops.md) |

---

## الفصول

### الأساس

| # | الفصل | ماذا يجيب |
| --- | --- | --- |
| 1 | [نظرة عامة والنطاق والجمهور والمتطلبات](docs/01-overview.md) | لماذا المشروع، لمن، وما الذي سيُبنى وما لن يُبنى |
| 2 | [المعمارية التقنية والمكدس وهيكل المستودع](docs/02-architecture.md) | كيف تتركب القطعة التقنية وكيف تُشغّل المشروع محلياً |
| 3 | [نموذج البيانات ومخطط قاعدة البيانات](docs/03-data-model.md) | الجداول والعلاقات ونمذجة متغيرات الجوالات وIMEI |
| 4 | [تصميم واجهة برمجة التطبيقات (API)](docs/04-api.md) | كل المسارات وصيغ الطلب والاستجابة والأخطاء |

### تجربة العميل

| # | الفصل | ماذا يجيب |
| --- | --- | --- |
| 5 | [تجربة الجوال أولاً والتصميم والأداء](docs/05-mobile-ux.md) | الشاشات ونظام Liquid Glass وميزانية الأداء وإمكانية الوصول |
| 6 | [البحث والتصفية والمقارنة والتوصيات](docs/06-search.md) | البحث العربي والمرشحات والمقارنة بين الأجهزة |
| 14 | [حساب العميل وخدمة العملاء والدعم](docs/14-account-support.md) | منطقة الحساب، التتبع بلا حساب، الأسئلة والمراجعات، نظام التذاكر |
| 15 | [ما بعد البيع: الكفالة ومركز الصيانة](docs/15-aftersales.md) | بطاقة الكفالة الرقمية، سير المطالبة، قطع الغيار، مطالبة المورد |
| 20 | [نظام الإشعارات: الموقع وتطبيق المتصفح](docs/20-notifications.md) | أسطح الإشعار، إشعارات المتصفح، وجرد كل حدث يستحق إشعاراً |

### التشغيل والإدارة

| # | الفصل | ماذا يجيب |
| --- | --- | --- |
| 7 | [الكتالوج وإدارة المخزون والتسعير](docs/07-catalog.md) | إدخال المنتجات، منع البيع الزائد، التسعير بالدولار والعرض بالليرة |
| 8 | [رحلة الشراء والطلبات والشحن والإرجاع](docs/08-orders.md) | من السلة إلى التسليم والتحصيل النقدي والمرتجعات |
| 10 | [لوحة التحكم الإدارية والتقارير](docs/10-admin.md) | إدارة المتجر اليومية، واجهة المندوب، التسويات، التقارير |
| 16 | [المشتريات والموردون والتكلفة والتقارير المالية](docs/16-procurement.md) | أوامر الشراء، التكلفة الشاملة، تكلفة البضاعة المباعة، الربحية |
| 17 | [إدارة التوصيل: المندوبون والمناطق والعمولات](docs/17-delivery-ops.md) | المناطق، تعيين الطلبات، سقف النقد، العمولات، أداء المندوبين |
| 18 | [المحتوى والسياسات وإعدادات المتجر](docs/18-content-settings.md) | ما يتغيّر بلا نشر كود: البانرات، السياسات، الإعدادات، وضع الطوارئ |

### البنية والنمو والمرجع

| # | الفصل | ماذا يجيب |
| --- | --- | --- |
| 9 | [الحسابات والمصادقة والأمان والصلاحيات](docs/09-security.md) | دخول OTP عبر واتساب، الأدوار، OWASP، حماية البيانات |
| 11 | [البيئات وCI/CD والاختبارات والمراقبة والصيانة](docs/11-devops.md) | البناء والنشر والمراقبة ودورة الصيانة التلقائية |
| 12 | [النمو وخارطة الطريق والمخاطر](docs/12-growth.md) | SEO على Astro، قنوات النمو، المراحل الزمنية، المخاطر |
| 13 | [دراسة الواقع السوري — ما يناسب وما لا يناسب](docs/13-syria-reality.md) | الأساس الميداني الذي بُنيت عليه القرارات، ودرجات الثقة فيه |
| 19 | [مصفوفة الاكتمال](docs/19-completeness.md) | كل ما يفعله العميل وكل ما تفعله الإدارة، وما استُبعد عمداً |

---

## كيف تقرأ هذه الخطة

- **إن كنت صاحب المشروع**: ابدأ بالفصل 1، ثم 13 (لماذا هذه القرارات تناسب سوريا)، ثم 19 (ماذا سأحصل عليه بالضبط).
- **إن كنت مطوّراً**: ابدأ بالفصل 2، ثم 3 و4، ثم الفصل الخاص بما ستبنيه.
- **إن كنت مسؤول تشغيل**: الفصول 8 و10 و16 و17 هي عملك اليومي.

الخطة وثيقة حيّة: كل قرار فيها قابل للمراجعة، لكن أي تعديل يجب أن ينعكس في جدول «القرارات المرجعية الموحّدة» أعلاه وفي كل فصل يعتمد عليه.

</div>

---

## تشغيل المشروع

المتطلبات: Node 22، pnpm 10، Docker (اختياري — الموقع يعمل بدونه).

```bash
pnpm install

# الواجهة وحدها: تعمل من بذرة الكتالوج مباشرة، بلا قاعدة بيانات
pnpm --filter @talisham/site dev        # http://localhost:4321

# المكدس الكامل
cp .env.example .env
pnpm dev:up                              # postgres · redis · meilisearch · minio · mailpit
pnpm db:migrate && pnpm db:seed          # القيود والمحفِّزات داخل الهجرة

# Worker واحد يخدم كل شيء كما في الإنتاج
pnpm run build:worker                    # الموقع + التطبيق + اللوحة + نص الـWorker
pnpm run worker:dev                      # على http://localhost:8787
```

### الاختبارات

```bash
pnpm test          # 76 اختبار وحدة: الحساب النقدي، تطبيع العربية، مهل الدعم، كلمات السرّ
pnpm test:e2e      # 45 فحصاً على واجهة تعمل وقاعدة حقيقية

# الفحص نفسه على الـWorker كما يعمل في الإنتاج
pnpm run build:worker && pnpm run worker:dev
API_BASE=http://127.0.0.1:8787/api/v1 pnpm run test:e2e
```

### حالة التنفيذ

| المكوّن | الحالة |
| --- | --- |
| مساحة العمل (pnpm + Turborepo) وDocker Compose | جاهز |
| `packages/ui` · `packages/types` | جاهز |
| `apps/api` — NestJS: الكتالوج، سعر الصرف، السلة، الطلبات، الإدارة، المندوب، البحث، الكفالة، المشتريات | جاهز ومختبَر من طرف إلى طرف |
| المصادقة — رمز لمرة واحدة، JWT، أدوار، إبطال الجلسات، حدّ المعدل | جاهز |
| كنّاس الحجوزات المنتهية | جاهز — كل دقيقة، ويدوياً من اللوحة |
| `apps/site` — Astro: الرئيسية والفئة وصفحة المنتج | جاهز · 35 صفحة · صفر JavaScript · يُبنى من الواجهة الحية وإلا من البذرة |
| `apps/app` — السلة وتعديل الكميات وإتمام الطلب والتتبع والحساب وطلباتي | جاهز |
| `apps/admin` — المؤشرات والطلبات والكتالوج (تحويل التجريبي إلى حقيقي) وسعر الصرف والمندوب | جاهز |
| **Worker واحد** يخدم الموقع والتطبيق واللوحة والواجهة البرمجية | جاهز — `/api/v1` و`/app` و`/admin` وما عداه الموقع، على أصل واحد بلا CORS |
| الطبقة الخلفية | **Hono بدل NestJS** — الخدمات كما هي، وجدول مسارات واحد يعمل على Worker وعلى Node بالشيفرة نفسها |
| قاعدة البيانات على Worker | Prisma بمحوّل سائق ومترجم بلا Rust · اتصالٌ لكل طلب (كائنات الشبكة لا تعبر حدود الطلب في workerd) |
| المهام الدورية | Cron كل دقيقة: كنس الحجوزات · تنبيهات كل خمس دقائق · حذف الحسابات المستحقّة يومياً |
| النشر | عبر GitHub Actions إلى `main` · [دليل النشر](docs/DEPLOY.md) — ينقص **سرّ `DATABASE_URL`** وحده لتعمل الواجهة |
| صفحات الموقع | 62 صفحة: البحث · العلامات · المدونة · السياسات الخمس · خريطة الموقع · المقارنة — صفر JavaScript |
| المشتريات | **جداول حقيقية**: موردون وأوامر شراء وبنودها بدل التخزين في سجل التدقيق |
| الجاهزية (الفصل 19 §19.5) | جاهز — 32 بنداً وصفوف المهام الدورية، و«جاهز للإطلاق» حاصلُ جمع |
| الحساب | العناوين · الرغبات · الكفالات · تفضيلات الإشعار · الأجهزة · حذف الحساب |
| الجلسات | صفٌّ لكل دخول — يُسقَط جهاز واحد بدل إخراج صاحبه من كل أجهزته |
| التقارير | الأرباح والخسائر · المخزون الراكد · تقادمه · العمولات · بطاقات أداء المندوب والمورد |
| المندوبون والمناطق واقتراح التعيين (الفصل 17) | جاهز |
| عروض الكمية (الفصل 7 §7.7) | جاهز |
| الحزم (bundles) — النوع الثالث من العروض | جاهز |
| Redis: سائق مزدوج — الذاكرة افتراضاً وRedis عند ضبط `REDIS_URL` | جاهز |
| صورة إنتاج ومكدّس `docker-compose.prod.yml` كامل | جاهز |
| Meilisearch: يعمل ومُختبَر، والمسار الاحتياطي يعطي النتائج نفسها | جاهز |
| إشعارات واتساب الفعلية | القناة مكتوبة؛ تعمل بالمحاكاة حتى يُضبط `WHATSAPP_PROVIDER_TOKEN` |
| التسويات النقدية اليومية والمطابقة وفصل الواجبات | جاهز |
| المرتجعات والاسترداد النقدي بفحص IMEI | جاهز |
| تذاكر الدعم بمهل ضمن ساعات العمل ومؤشرات | جاهز |
| التقييمات وأسئلة المنتج وطابور الإشراف | جاهز |
| الكوبونات بالدولار وحدود الاستخدام | جاهز |
| تنبيهات التوفّر وانخفاض السعر | جاهز |
| واجهة المندوب دون اتصال: طابور مزامنة وعامل خدمة | جاهز |
| مطالبات الكفالة في جدول حقيقي بدل سجل التدقيق | جاهز |
| المصادقة بكلمة سرّ للموظّفين + قفل بعد خمس محاولات | جاهز |
| **إدارة الكتالوج**: إنشاء وتعديل المنتجات والمتغيّرات والأسعار | جاهز |
| رفع الصور: قرص محلي افتراضاً وS3 عند ضبطه، وفحص النوع من المحتوى | جاهز |
| شاشة إعدادات المتجر، والإعدادات مفروضة في الخادم لا في الواجهة | جاهز |
| إشعارات المتصفح (VAPID) — تحتاج مفاتيح لتعمل | جاهز |
| مركز المساعدة: ست صفحات ساكنة بـFAQPage JSON-LD | جاهز |
| التقييمات والأسئلة تظهر في صفحات المنتج الساكنة | جاهز |
| Renovate كل شهرين + فحص صيانة أسبوعي يفتح مسألة عند الفشل | جاهز |
| تحقق مستمر على كل دفعة (`ci.yml`) | جاهز |
| الكوبونات موصولة بالسلة والطلب مع عدّاد ذرّي | جاهز |
| الاختبارات: 76 اختبار وحدة + 45 فحصاً من طرف إلى طرف | جاهز — والفحص يُشغَّل على الـWorker نفسه (`API_BASE=…`) |
| الفحص يهيّئ شرطه بنفسه | جاهز — كان يفترض حالةً هيّأها أحدٌ يدوياً فيمرّ عنده ويفشل على قاعدة نظيفة |
| `prisma migrate deploy` من الصفر | **أُصلح** — كان يفشل عند الترحيل الثاني، وهو ما يقع عند أول نشر فعلي |
| هجرات Prisma (بدل `db push`) والقيود مدمجة فيها | جاهز |

**الدخول إلى لوحة التحكم:** طريقتان — كلمة سرّ أو رمز واتساب.

```bash
# إنشاء حساب إداري بكلمة سرّ (تُقرأ من المدخل لا من سطر الأوامر)
pnpm --filter @talisham/api grant-role +9639XXXXXXXX ADMIN "الاسم" --password
```

كلمة السرّ للموظّفين وحدهم: الزبون يدخل برمز واتساب ولا كلمة سرّ له. ووجودها
ضرورة لا رفاهية — الاعتماد على الواتساب وحده يعني أن انقطاع مزوّد الرسائل
يقفل لوحة التحكم في وجه صاحبها.

التجزئة `scrypt` بملح لكل حساب، وخمس محاولات خاطئة تقفل الحساب ربع ساعة،
ورسالة الفشل واحدة مهما كان السبب حتى لا يصير نموذج الدخول أداةَ تعداد.
تسجيل الدخول من الواجهة يُنشئ حساب زبون فقط — الصلاحية الإدارية لا تُمنح إلا من الخادم.

### ما جرى التحقق منه فعلياً

شُغّل النظام كاملاً على PostgreSQL 16 حقيقي وأُثبتت الرحلة من طرف إلى طرف:

| الفحص | النتيجة |
| --- | --- |
| بذر الكتالوج | 22 منتجاً · 24 متغيّراً · 7 تعريفات شحن |
| حساب المستحق النقدي | 16,600 سنتاً × 12,850 = 2,133,100 ← **2,133,000 ل.س** وفرق تقريب −100 مخزَّن |
| رفض الطلب على منتج تجريبي | `409 DEMO_PRODUCT_NOT_ORDERABLE` |
| مفتاح التفرّد على إنشاء الطلب | إعادة الإرسال أعادت `TS-2607-000001` نفسه ولم تنشئ طلباً ثانياً |
| انتقال حالة غير مسموح | `409 INVALID_TRANSITION` عند محاولة التسليم قبل الشحن |
| دورة الحالات الكاملة | تأكيد ← تجهيز ← شحن ← خروج للتوصيل ← تسليم وتحصيل |
| مفتاح التفرّد على التحصيل | إعادة الإرسال لم تحصّل مرتين |
| **محفِّز تطابق المخزون** | رفض الإيداع: «انحراف مخزون: 1 وحدة بحالة IN_STOCK مقابل on_hand = 8» |
| **توقّف البيع عند تقادم سعر الصرف** | `503 FX_STALE_HALT` بعد 13 ساعة من انتهاء الصلاحية |
| **هامش الأمان في فترة السماح** | 12,850 ← 13,236 تلقائياً (‏3%) |
| بناء الموقع | صفر ملفات JavaScript · 25 كيلوبايت لأول زيارة |
| بناء التطبيقين | 65 كيلوبايت مضغوطاً لكل منهما (الميزانية 170) |
| البحث العربي | «ايفون» و«آيفون» و«أيفون» ← نتيجة واحدة · «جوال» و«موبايل» ← فئة الهواتف · البحث برمز النسخة `ZA/A` |
| التكلفة الشاملة | بضاعة 1,730$ + تكاليف 200$ ⇒ معامل 0.115607 · 128$ ← **142.80$** للوحدة |
| فحص Luhn على IMEI | قُبلت 3 وحدات ورُفضت واحدة |
| حارس الاستلام | `400 DEMO_PRODUCT_NOT_RECEIVABLE` — لا بضاعة حقيقية في منتج تجريبي |
| دورة الكفالة | تفعيل عند التسليم ← تحقق عام بالـIMEI (365 يوماً) ← مطالبة ← سبع محطات ← إغلاق |
| رفض قفزة في المطالبة | `409 INVALID_TRANSITION` عند `OPENED → DIAGNOSING` |
| **إدارة الكتالوج** | إنشاء منتج ونشره · رمز تخزين مأخوذ `SKU_TAKEN` · سعر مشطوب أقل `COMPARE_PRICE_INVALID` |
| رفع الصور | PNG رُفعت وخُدمت وحُذفت · سكربت متنكّر بامتداد صورة `MEDIA_TYPE_UNSUPPORTED` |
| الإعدادات مفروضة في الخادم | إيقاف المتجر `STORE_PAUSED` · الرقة `GOVERNORATE_NOT_SERVED` · مفتاح مجهول `SETTING_UNKNOWN` |
| **مطابقة المنطقة** | «المزة» طابقت `DMS-W1` بالتطبيع · اقتراح المندوبين رتّبهم بالأولوية 1 قبل 5 |
| إيقاف المندوب | أُسقط من الاقتراحات فوراً · والإيقاف بلا سبب `SUSPENSION_REASON_REQUIRED` |
| **عرض الكمية** | قطعتان بلا خصم · ثلاث بخصم 10% (7,500 ← 6,750) · شريحة فئة 5% (188,000 ← 178,600) |
| **الحزم** | 94,900 ← 91,900 عند اكتمال مفرداتها · أغلى من مفرداتها `BUNDLE_NOT_CHEAPER` · صنف واحد `BUNDLE_NEEDS_TWO` |
| **Redis** | الفحص كاملاً (45/45) على السائقين: الذاكرة وRedis — بالنتائج نفسها |
| **البناء المترجَم** | `node dist/main.js` أقلع وردّ على `/health` وخدم الكتالوج |
| **Meilisearch** | فُهرس 23 وثيقة · «ايفون» و«آيفون» و«جوال» و«شاحن» و`ZA/A` — والمحركان يعطيان العدد نفسه في كل استعلام |
| Redis متوقف والمتجر مضبوط عليه | أقلع على الذاكرة وردّ على `/health` بدل أن يعلّق |
| مسار إداري بلا رمز | `401 UNAUTHENTICATED` |
| مسار إداري برمز زبون | `403 FORBIDDEN` مع `{required, actual}` |
| إبطال الجلسات | بعد `revoke-all` صار الرمز نفسه `401 SESSION_REVOKED` |
| تغيير الدور يُبطل الرموز | ترقية `COURIER → OPS_MANAGER` رفعت `token_version` وأسقطت الجلسات |
| تجديد الرمز | رمز تحديث صالح ⇐ رمز جديد · رمز وصول في مكانه ⇐ `TOKEN_INVALID` |
| حدّ المعدل | `RateLimit-Remaining` ينقص على الكتالوج · `429 RATE_LIMITED` عند التجاوز |
| **الحجز المرن يخصم فعلاً** | إضافة للسلة رفعت `reserved` 0←1 · رفع الكمية خصم الفرق فقط · سلة ثانية على آخر قطعة ⇐ `OUT_OF_STOCK` |
| حذف صنف من السلة | `reserved` عاد إلى الصفر وسطر `RELEASE` في الدفتر |
| **ترقية الحجز لا إضافته** | إنشاء الطلب حوّل `SOFT_HOLD` إلى `ORDER_HOLD` دون خصم مضاعف |
| **كنّاس الحجوزات** | حرّر 28 حجزاً منتهياً وأعاد الكميات إلى المتاح |
| **التسليم هو لحظة البيع** | `on_hand` 2←1 · الوحدة `IN_STOCK → SOLD` · سطر `SALE −1` · عدد الوحدات = `on_hand` |
| تفعيل الكفالة لا يحرّك المخزون | `on_hand` بقي 1 · التحقق العام أعاد 365 يوماً |
| تحويل منتج تجريبي إلى حقيقي | صُفِّر مخزونه الوهمي (6) · إعادة التحويل `PRODUCT_ALREADY_REAL` · وعليه حجز `PRODUCT_HAS_HOLDS` |
| نسبة الطلب لصاحبه | طلب بهوية داخل ظهر في «طلباتي» · وطلب ضيف ظهر لصاحبه حين سجّل دخوله بالرقم نفسه |
| **التسوية النقدية** | صف اليوم أُنشئ لحظة التحصيل · عمولة 5% محسوبة · صافي المتجر مطروحاً منه · مطابَقة ← مُقفَلة |
| فصل الواجبات | المحصِّل مُنع من مطابقة تسويته `SEGREGATION_OF_DUTIES` |
| تحصيل أكثر من المستحق | `OVERCOLLECTION` — فائضٌ في يد المندوب لا سطر له في أي دفتر |
| تحصيل جزئي بلا سبب | `PARTIAL_REASON_REQUIRED` |
| نقد يصل بعد الإقفال | يُعيد فتح صف يومه ويُسجَّل في `audit_logs` بدل أن يُضاف صامتاً لدفتر مُوقَّع |
| **الإرجاع** | نافذة 7 أيام من التسليم · إرجاع ثانٍ مرفوض · قفزة حالة مرفوضة · رفض بلا سبب مرفوض |
| IMEI غير مطابق | `IMEI_MISMATCH` ورفض فوري — جهازٌ غير الذي بِيع لا يُقبل |
| إرجاع مكتمل | الوحدة عادت `IN_STOCK` · `on_hand` 4←5 · استرداد أُنشئ · صُرف مرة واحدة والثانية `INVALID_TRANSITION` |
| **الكوبونات** | سقف الخصم مُطبَّق (10% من 500$ ← 25$) · حد أدنى · رمز مجهول · نسبة خارج المدى |
| **التذاكر** | مهل ضمن ساعات العمل · الملاحظة الداخلية لا يراها العميل · تقييم الخدمة · المؤشرات |
| **التنبيهات** | تنبيه توفّر لمنتج متوفر مرفوض · تنبيه سعر يحتاج حساباً |

### ما تبقّى فعلاً

| البند | الحالة |
| --- | --- |
| **قاعدة بيانات للإنتاج** | القرار الوحيد المتبقّي: PostgreSQL مُدار (طبقة مجانية) ثم `wrangler secret put DATABASE_URL`. وD1 يعني إعادة كتابة طبقة البيانات وفقدان المعاملات التفاعلية والمحفِّزات — أي عودة العيوب الأربعة التي كشفها الفحص |
| مفاتيح واتساب الفعلية | القناة مكتوبة ومختبَرة بالمحاكاة؛ تُرسِل فور ضبط `WHATSAPP_PROVIDER_TOKEN` |
| مفاتيح VAPID | الاشتراك يعمل، والإرسال ينتظر المفاتيح |
| صور المنتجات | R2 أو أي تخزين S3 — لا قرص محلياً في Worker |
| Meilisearch مُستضاف | المسار الاحتياطي على PostgreSQL يعطي النتائج نفسها |
| بنود الجاهزية الاثنان والثلاثون | تُؤشَّر من `/admin/readiness` — وهي عمل صاحب المتجر لا عمل الشيفرة |

### انحرافات موثَّقة عن الخطة

| البند | الخطة | التنفيذ | السبب |
| --- | --- | --- | --- |
| Tailwind 4 | رموز عبر `@theme` | متغيرات CSS خالصة في `packages/ui` | النظام قائم على الرموز أصلاً، وإسقاط الاعتماد يقلّل الحزمة وزمن البناء |
| TanStack Router | موجّه كامل | موجّه صغير على History API | ثمانية مسارات لا تبرّر تكلفته — يُستبدل عند التوسّع |
| حقن التبعيات | ضمني بالأنواع | `@Inject()` صريح | `tsx` لا يُصدر `design:paramtypes`، والحقن الصريح يعمل مع أي مترجم |
| رموز الدخول ومهل الاستجابة | Redis | خرائط في الذاكرة | البنية نفسها (مفتاح ← سجل بمهلة) — تُبدَّل بـ Redis عند تشغيل أكثر من نسخة |
| جدولة الكنس | `@nestjs/schedule` | `setInterval` في دورة حياة الوحدة | مهمة واحدة لا تبرّر اعتماداً جديداً |

### عيوب وُجدت أثناء الفحص وأُصلحت

فحص الدورة الكاملة على قاعدة حقيقية كشف أربعة عيوب في المخزون، كلٌّ منها كان
سيظهر في المتجر على شكل بيع ما لا يُملَك أو حجب ما يُملَك:

| العيب | الأثر لو بقي | الإصلاح |
| --- | --- | --- |
| الحجز المرن يُنشئ صفّاً ولا يخصم من `reserved` | ثلاثة زبائن يظنّون آخر جهاز لهم، ويُخذل اثنان بعد الطلب | الصف والعدّاد في معاملة واحدة، والفرق فقط عند تعديل الكمية |
| لا كنّاس للحجوزات المنتهية | كل سلة مهجورة تحبس بضاعتها إلى الأبد حتى يقول المتجر «نفدت» والرفّ ممتلئ | `ReservationSweeper` كل دقيقة + سطر `RELEASE` لكل تحرير |
| إنشاء الطلب يضيف حجزاً فوق الحجز المرن | خصم مزدوج يحجب البضاعة ربع ساعة بعد كل طلب | ترقية الصف في مكانه وخصم الفرق |
| التسليم لا يُنقص `on_hand` ولا يختم الوحدات | المتجر يعرض للبيع أجهزةً سلّمها بالفعل | التسليم صار نقطة البيع: تحرير الحجز، خصم `on_hand`، ختم الوحدات `SOLD`، سطر `SALE` |

وأُزيل خصم المخزون من تفعيل الكفالة لأنه صار في التسليم — وإبقاؤه كان سيخصم الجهاز مرتين.

وكشف بناء ما بعد البيع ثلاثة أخرى:

| العيب | الأثر لو بقي | الإصلاح |
| --- | --- | --- |
| المطابقة تخصم فرق التقريب من فرق المندوب | كل مندوب يظهر مديناً أو دائناً بمئات الليرات يومياً لم تمرّ بيده | الفرق = المقبوض − المستحق فقط، وفرق التقريب بندٌ محاسبي لا اتهام |
| نقد يصل بعد إقفال يومه يُضاف صامتاً | الدفتر يتغيّر بعد التوقيع عليه: لا الرقم القديم صحيح ولا الجديد مُراجَع | إعادة فتح صف اليوم وتسجيلها في `audit_logs` |
| حالة مطالبة الكفالة تُستنتج من آخر سطر في سجل التدقيق | مسح آلاف السطور لمعرفة حالة واحدة، وضياعها لو نُظّف السجل | جدول `warranty_claims` حقيقي، والسجل للتدقيق وحده |
